---
title: "ProsoCodec: Prosody-Oriented Speech Codec for Voice Conversion"
title_zh: ProsoCodec：用于语音转换的面向韵律的语音编解码器
authors: "Jeongsoo Choi, Ji-Hoon Kim, Shujie Hu, Joon Son Chung"
date: 2026-06-20
pdf: "https://arxiv.org/pdf/2606.21888v1"
tags: ["query:speech-audio"]
score: 9.0
evidence: 使用面向韵律的编解码器进行语音转换
tldr: 现有神经语音编解码器将内容、说话人和韵律纠缠，不利于语音转换中的韵律保留。ProsoCodec将韵律建模为条件残差，编码器和解码器以文本和说话人嵌入为前缀token，使离散瓶颈捕获与内容、说话人无关的韵律变化。采用低频mel带和配对相同说话人话语训练。实验表明，该方法显著提升韵律保留度并减少源音色泄漏，为韵律敏感的下游任务提供有效基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21888v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 695, \"height\": 737, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21888v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 829, \"height\": 340, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21888v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1408, \"height\": 397, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21888v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 830, \"height\": 273, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21888v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 830, \"height\": 316, \"label\": \"Table\"}]"
motivation: 现有神经编解码器学习整体表征，阻碍需要韵律保留或迁移的语音转换任务。
method: 以文本和说话人嵌入为前缀token，条件化编解码器，将韵律建模为条件残差，并用低频mel带和配对话语训练。
result: 在语音转换上，ProsoCodec改善了韵律保留并减少了源音色泄漏。
conclusion: ProsoCodec通过韵律导向的编解码设计有效分离韵律，提升下游任务性能。
---

## 摘要
神经语音编解码器高效压缩语音并成为语音生成的基础，但它们通常学习为整体表示，交织了语言内容、说话人身份和韵律。虽然这种设计对于零样本人声克隆有效，但它阻碍了需要韵律保留或迁移的下游任务，如语音转换。为了解决这个问题，我们引入了ProsoCodec，一种面向韵律的语音编解码器，它将韵律建模为条件残差而不是解耦流。具体来说，通过将编码器和解码器条件设置为文本和说话人嵌入作为前缀标记，离散瓶颈被鼓励捕获未被内容和说话人解释的韵律变化。为了进一步保留韵律，我们使用低频梅尔频带并在配对的同说话人话语上训练模型。语音转换实验表明，韵律保留得到改善，源音色泄漏减少。

## Abstract
Neural speech codecs efficiently compress speech and have become a foundation for speech generation, but they are typically learned as holistic representations that intertwine linguistic content, speaker identity, and prosody. While this design is effective for zero-shot voice cloning, it hinders downstream tasks that require prosody preservation or transfer, such as voice conversion. To address this, we introduce ProsoCodec, a prosody-oriented speech codec that models prosody as a conditional residual rather than as a disentangled stream. Specifically, by conditioning both the encoder and decoder on text and speaker embeddings as prefix tokens, the discrete bottleneck is encouraged to capture prosodic variation not explained by content and speaker. To further preserve prosody, we use the low-frequency mel band and train the model on paired same-speaker utterances. Experiments on voice conversion show improved prosody preservation and reduced source-timbre leakage.

---

## 论文详细总结（自动生成）

### 论文总结：ProsoCodec: Prosody-Oriented Speech Codec for Voice Conversion

#### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：现有神经语音编解码器（如Encodec、WavTokenizer）学习的是**整体表征**，将语言内容、说话人身份和韵律纠缠在一起。这种设计对于零样本人声克隆有效，但对于需要**仅保留或迁移韵律**的下游任务（如语音转换）却构成障碍——因为韵律并非独立属性，它与内容和说话人密切相关，强行解耦会丢失说话人条件化的韵律细节。
- **研究动机**：希望设计一个面向韵律的编解码器，能够在**不依赖手工艺韵律监督或对抗解耦**的前提下，显式地将韵律建模为**条件残差**（即内容+说话人无法解释的部分），从而更好地服务于语音转换（保留源话语的韵律、替换为目标说话人音色）。
- **整体含义**：该方法为韵律敏感的语音生成提供了一个新型基础模块，可被应用于语音转换、文本到语音合成、对话系统等。

#### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：将韵律视为**条件于文本和说话人身份的残差**。通过将编码器和解码器以文本和说话人嵌入作为前缀令牌进行条件化，并使用离散量化瓶颈压缩表示，迫使潜在表示集中于捕获“未被内容与说话人解释”的韵律变化。
- **关键技术细节**：
  - **显式文本和说话人先验**：使用预训练ASR（Qwen3-ASR）获取文本转录，经token化、嵌入和MLP处理得到文本嵌入 \(e_{txt}\)；使用预训练SV（CAM++）提取说话人嵌入 \(e_{spk}\)。
  - **先导编码器**：将 \(e_{spk}\), \(e_{txt}\) 作为前缀与mel频谱（仅使用低频带）拼接，通过Transformer编码器得到引导后的特征 \(h'\)。
  - **量化器**：采用**二值球面量化（BSQ）**，将连续特征映射到二值空间（\(\{-1,1\}^{T' \times d}\)），形成离散令牌序列以强制信息瓶颈。
  - **基于扩散的解码器**：基于扩散Transformer（DiT），输入包含：参考mel频谱（全频带，带随机掩码）、量化后的源令牌（插值到相同长度）、以及相同的文本和说话人先验。使用**条件流匹配损失**训练，优化目标为预测从噪声到干净mel的速度场。
  - **双话语训练策略**：训练时从**同说话人**中采样两段不同话语，一个作为提示（prompt），一个作为源（source），从而防止模型在生成时简单地复制提示中的风格（避免“提示风格泄漏”）。
  - **低频带输入**：编码器只使用mel频谱的**低频部分**（偏置模型关注韵律线索，因为韵律主要存在于低频），解码器提示则使用全频带以保留说话人音色。

#### 3. 实验设计：数据集、基准、对比方法
- **数据集**：
  - 训练：**LibriTTS**（585小时，24kHz，2456个说话人）。
  - 评估：**LibriTTS test-clean / test-other** 以及 **VCTK**（跨域泛化）。每个评估集随机采样1000条2-8秒话语。
- **基准**：不使用特定基准，而是与多个开源SOTA方法比较。
- **对比方法**：DDDM-VC、UniAudio、HierSpeech++、FACodec、Seed-VC、Vevo（均为语音转换或相关框架）。
- **评估指标**：
  - 客观：WER（内容保留）、SIM_r（与参考说话人相似度）、SIM_s（与源说话人相似度，越低越好）、f0 RMSE（韵律保留）、UTMOS（自动自然度评估）。
  - 主观：S-MOS（说话人相似度）、P-MOS（韵律相似度）、N-MOS（自然度），均采用5点Likert量表，15名听众，每子集10个样本。
- **消融实验**（Table 2）：逐个移除低频mel、双话语训练、说话人条件、文本条件。
- **瓶颈配置消融**（Table 3）：不同帧率（25/12.5/6.25 Hz）、码本大小、比特率，以及零-shot TTS基线（去掉编解码令牌）。

#### 4. 资源与算力
- **文中未明确说明**具体的GPU型号、数量、训练时长。只提到使用**AdamW优化器**，学习率2e-5，训练150k步，全局批次大小160秒。因此无法给出具体算力细节。

#### 5. 实验数量与充分性
- **实验数量**：主要实验1组（与6个基线对比，含客观+主观），消融实验2组（Table 2: 5配置；Table 3: 6配置 + TTS基线），加上Pitch轮廓定性分析（Fig 2）。
- **充分性**：实验覆盖了内容、音色、韵律三个维度的全面评估，包括客观和主观。消融实验设计了多个关键组件的移除，验证了每个部件的作用。瓶颈配置消融探讨了比特率与转换质量的权衡。对比方法来自不同年份、不同流派（扩散、LLM、解耦等），基本公平。**主观评价样本量较小**（每子集10个样本×15听众），但属于常规做法。
- **局限**：未在更多语言、噪声环境或低资源场景下测试；未与更多基于特征解耦的最新方法比较（如HuBERT+Resynthesis）。

#### 6. 论文的主要结论与发现
- **主要结论**：ProsoCodec在**内容保持（WER最低）、说话人相似度（SIM_r最高）、源音色泄漏抑制（SIM_s最低）** 以及**韵律保留（f0 RMSE最低）** 上均优于现有方法。主观MOS显示韵律相似度和自然度均达到或接近最佳。
- **关键发现**：
  - 强重建不一定带来好转换；降低比特率可减少源音色泄漏。
  - 移除文本条件导致内容丢失（WER剧升至86.6%）；移除说话人条件导致音色泄漏加剧；移除双话语训练导致韵律保留下降；移除低频mel导致多项指标退化。
  - 零-shot TTS（无编解码令牌）无法准确恢复源韵律（pitch偏离源话语）。

#### 7. 优点：方法或实验设计上的亮点
- **方法论亮点**：
  - 独创性地将韵律建模为**条件残差**，避免了复杂的对抗解耦或手工韵律标注。
  - **双话语训练策略**简单而有效，显著减轻提示风格泄漏问题。
  - 利用**低频偏置**引导模型关注韵律线索，设计简洁合理。
  - 基于扩散的解码器结合流匹配，实现高保真重建。
- **实验设计亮点**：
  - 同时采用客观（WER, SIM, RMSE）和主观（MOS）多维度评估。
  - 消融实验全面覆盖了每个设计组件。
  - 分析了比特率与转换能力的权衡，提供了实际部署的选择依据。

#### 8. 不足与局限
- **对先验模型的依赖**：依赖于外部ASR和SV系统（Qwen3-ASR, CAM++），这些模型本身可能存在偏差或错误，特别是在非英语或多方言场景下表现可能下降。
- **数据局限**：仅在LibriTTS（英语阅读语音）上训练，未在自然对话、带噪声或情绪变化的语音上评估。
- **低频偏置的潜在副作用**：可能丢弃了某些与韵律相关的高频线索（如声门化），影响泛化能力。
- **主观评价规模**：听众数量（15人）和样本量（10个/条件）偏小，置信区间较宽。
- **未与其他残差编码方案比较**：没有与直接预测残差（如pitch contour残差）的方法进行对比。
- **计算效率**：基于Transformer+DiT，推理需要多步采样（32步），实时性可能受限。

（完）
