---
title: Reference-Driven Multi-Speaker Audio Scene Generation from In-the-Wild Priors
title_zh: 基于参考的多说话人音频场景生成：从野外先验学习
authors: "Michael Finkelson, Daniel Segal, Eitan Richardson, Shahar Armon, Nani Goldring, Poriya Panet, Nir Zabari, Benjamin Brazowski, Or Patashnik, Yoav HaCohen"
date: 2026-06-17
pdf: "https://arxiv.org/pdf/2606.19325v1"
tags: ["query:speech-audio"]
score: 9.0
evidence: 多说话人对话的文字到音频场景生成
tldr: 现有多说话人对话系统依赖结构化监督（逐轮标签或说话人嵌入），仅生成干净语音，缺乏真实环境纹理。本文提出 ScenA，利用预训练的文本到音频流匹配模型，以多个参考声音和自由形式自然语言提示直接生成多说话人音频场景，继承背景噪声、重叠对话等自然特性。针对训练中模型通过声学相似性绕过文本提示的“参考捷径”问题，采用高噪声偏置的时间步分布强制依赖文本。在 CoVoMix2-Dialogue 上，ScenA 在说话人绑定指标上超越现有系统，生成包含重叠语音、情感发声和环境音的丰富对话。结果表明通用音频模型结合自由形式场景描述优于仅语音结构化管道。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.19325v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1434, \"height\": 316, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19325v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1425, \"height\": 457, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19325v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1347, \"height\": 626, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19325v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1301, \"height\": 429, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19325v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1323, \"height\": 307, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19325v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1320, \"height\": 285, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.19325v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1126, \"height\": 280, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19325v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1128, \"height\": 282, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19325v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 564, \"height\": 206, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19325v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1247, \"height\": 251, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19325v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1125, \"height\": 174, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19325v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1193, \"height\": 206, \"label\": \"Table\"}]"
motivation: 现有方法需逐轮结构化监督且仅产出干净语音，无法再现真实对话的噪声、重叠和环境纹理。
method: 利用大规模野外数据预训练的文本到音频流匹配模型，拼接参考潜在向量并加入身份感知位置编码；采用高噪声偏置分布避免参考捷径。
result: 在 CoVoMix2-Dialogue 上，ScenA 的说话人绑定指标（如说话人混淆率）显著优于基线，且生成音频包含自然重叠语音和环境音。
conclusion: 通用音频模型配合自由形式场景描述能有效生成多说话人真实对话，无需结构化管道约束。
---

## 摘要
现有的多说话人对话系统通过结构化监督将说话人与话语绑定：每轮标签、多流转录或可学习的说话人嵌入。这些系统在仅语音的流水线中运行，生成干净的语音序列，缺乏真实对话的环境质感。我们采取不同的方法。我们的方法ScenA将文本到音频流匹配基础模型（在大规模野外数据上预训练）直接条件于多个参考语音和一个描述整个多说话人音频场景的自由形式自然语言提示。利用这样的基础模型使我们能够继承其生成自然、非演播室音频的能力：背景噪声、房间声学、重叠对话和自发副语言事件，同时添加多说话人控制，无需任何每轮结构。具体而言，参考潜在向量被拼接进模型的令牌序列，并通过轻量级身份感知位置编码进行区分。然而，我们发现这种方法的一个关键障碍：“参考捷径”。在标准噪声计划的训练过程中，模型可以通过与噪声目标的声学相似性识别匹配的参考，完全绕过文本提示。我们通过高噪声偏置的时间步分布解决这一问题，迫使模型依赖文本提示进行说话人分配。我们在CoVoMix2-Dialogue基准上评估ScenA，显示其在说话人绑定指标上优于现有多说话人系统，同时生成包含重叠语音、情感发声和环境声音的丰富对话音频。我们的结果证明了使用通用音频模型（条件于自由形式场景描述）优于通过仅语音流水线传递结构化对话脚本的优势。

## Abstract
Existing multi-speaker dialogue systems bind speakers to utterances through structured supervision: per-turn tags, multi-stream transcriptions, or learnable speaker embeddings. These systems operate within speech-only pipelines that produce clean vocal sequences without the ambient texture of real conversations. We take a different approach. Our method, ScenA, conditions a text-to-audio flow-matching foundation model, pretrained on large-scale in-the-wild data, directly on multiple reference voices and a free-form natural language prompt that describes an entire multi-speaker audio scene. Leveraging such a foundational model allows us to inherit its capacity for natural, non-studio audio: background noise, room acoustics, overlapping dialogue, and spontaneous paralinguistic events, while adding multi-speaker control without any per-turn structure. Concretely, reference latents are concatenated into the model's token sequence and distinguished by lightweight identity-aware positional encodings. However, we identify a critical obstacle to this approach: the \textit{Reference Shortcut}. During training under standard noise schedules, the model can identify the matching reference by acoustic similarity to the noisy target, bypassing the text prompt entirely. We address this with a high-noise-biased timestep distribution that forces the model to rely on the text prompt for speaker assignment. We evaluate ScenA on the CoVoMix2-Dialogue benchmark, showing that it outperforms existing multi-speaker systems on speaker-binding metrics while generating rich conversational audio with overlapping speech, emotional vocalizations, and ambient sound. Our results demonstrate the advantage of using a general-purpose audio model conditioned on a free-form scene description, rather than passing structured dialog scripts through a speech-only pipeline.

---

## 论文详细总结（自动生成）

# 基于参考的多说话人音频场景生成：从野外先验学习（论文总结）

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **现有局限**：现有多说话人对话生成系统（如对话TTS）依赖结构化监督（每轮说话人标签、多流转录、可学习说话人嵌入），且仅在仅语音的流水线中工作，输出干净的语音序列，缺乏真实对话中的环境纹理（背景噪声、房间声学、重叠对话、自发副语言事件如笑声、叹息等）。
- **研究动机**：希望利用大规模野外数据预训练的通用文本到音频流匹配模型，直接基于多个参考语音和一个自由形式自然语言提示生成多说话人音频场景，从而继承其自然、非演播室音频的能力，同时实现无需每轮结构的多说话人控制。
- **整体含义**：证明通过精心设计的训练策略（高噪声偏置时间步分布），即使没有结构化接口，通用音频模型也能通过自由形式提示实现准确的说话人绑定，并生成更丰富的对话场景。

## 2. 论文提出的方法论：核心思想、关键技术细节
### 核心思想
- 基于预训练的音频扩散变压器（LTX-2的音频流），通过拼接参考潜变量和轻量级身份感知位置编码，使模型能够以自由形式自然语言提示驱动多说话人音频场景生成，无需逐轮标签或身份编码器。

### 关键技术细节
1. **参考条件机制（Latent Concatenation）**：
   - 目标音频经VAE编码为z0，参考语音编码为rk，输入序列为 [zt; r1; ...; rK]（zt是噪声目标，rK保持干净）。
   - 每个参考槽位k使用一个学习到的嵌入ek加到所有令牌上，目标令牌没有额外嵌入。
   - 文本提示通过交叉注意力条件化模型，提示中以“reference 1”、“reference 2”等提及参考。

2. **参考捷径问题（Reference Shortcut）**：
   - 在标准噪声计划（如logit-normal分布）训练下，模型可以通过噪声目标与参考的声学相似性直接匹配参考，绕过文本提示。训练损失虽低，但推理时从纯噪声开始，捷径不可用，导致说话人绑定失败。
   - 通过一个探针分类器证实：在t<0.58时准确率≥98%，直至t≈0.93时仍>75%，说明捷径在大部分噪声水平可行。

3. **解决方案：高噪声偏置时间步分布**：
   - 使用Beta(α,1)+Uniform混合分布：p(t) = (1-λ) Beta(t; α,1) + λ Uniform(t; ε,1)，将训练质量集中在高噪声（t接近1）区域，迫使模型依赖文本提示进行说话人绑定。
   - 默认参数：α=5, λ=0.1, ε=0.05。

4. **辅助训练增强**：
   - 对抗性参考注入：在训练时填入未在提示中提及的干扰参考，强制模型按文本绑定。
   - 槽位打乱：打乱参考在序列中的顺序并重写提示中的“reference k”，减少位置偏置。采用前10k步固定顺序、之后打乱的课程学习。

### 公式与算法说明（文字描述）
- 训练目标：标准流匹配速度预测损失 L = E[||fθ(zt, t, c) - v||²]，其中v = ε - z0，t从设计的混合分布p(t)采样。
- 推理：从t=1纯噪声开始，迭代去噪，文本提示和参考潜变量共同引导生成完整音频场景。

## 3. 实验设计：数据集、基准、对比方法
### 数据集与基准
- **CoVoMix2-Dialogue-20S**：从CoVoMix2对话测试集筛选出291个时长≤20秒的对话，使用LibriSpeech test-clean作为参考语音，DailyDialog转录为“reference 1/reference 2”格式的提示。
- **CoVoMix2-Dialogue-Wild Ref**：从中抽取50个对话，替换参考为30个野外录音（含噪声、背景音乐、街道环境等），共100个样本，用于测试鲁棒性。
- 评估指标：cpWER（说话人感知词错误率）、cpSIM（逐说话人余弦相似度）、ACC（词级说话人准确率）、WER、SIM-O、UTMOS、SQUIM。

### 对比方法
- MOSS-TTSD、VibeVoice-1.5B/7B、ZipVoice-Dialog、Dia（Nari Labs）。所有基线使用默认设置。

### 消融实验
1. **噪声计划消融**：对比三种logit-normal分布与ScenA的Beta+Uniform混合，验证高噪声偏置对绑定指标的必要性。
2. **参考槽位编码消融**：对比无位置编码、RoPE变体、加法嵌入。
3. **训练配方消融**：对抗性参考注入、槽位打乱策略（无打乱/始终打乱/课程打乱）。

### 人类评估
- 对两个子集的混合样本进行A/B偏好测试，ScenA与每个基线配对，评估员盲选。

## 4. 资源与算力
- **硬件**：16 × NVIDIA GB200 GPU。
- **训练步数**：20,000步，全局batch size 128。
- **训练时间**：约24小时。
- **优化器**：AdamW（β1=0.9, β2=0.95, weight decay 0.01），峰值学习率1×10⁻⁴，1000步线性预热，常数保持。EMA decay 0.9999。
- 论文明确给出了算力信息。

## 5. 实验数量与充分性
- **主要指标表**：在20S上对比6个指标（cpWER、cpSIM、ACC、WER、SIM-O、UTMOS、SQUIM），在Wild Ref上重复对比。
- **三个消融实验**：噪声计划（4种分布）、槽位编码（3种）、训练配方（4种），均报告全部指标。
- **人类评估**：5个对比（每个基线一次），报告偏好百分比和显著性。
- 实验充分覆盖了绑定指标、自然度指标、鲁棒性、各因素影响。对比方法包括当前代表性系统（开源闭源混合），公平性可接受。消融设计系统，结论清晰。

## 6. 论文的主要结论与发现
- ScenA在说话人绑定指标（cpWER、cpSIM、ACC）上优于所有对比基线，在WER上最佳，SIM-O持平最佳，SQUIM接近最佳。
- 在野外参考场景下，ScenA的cpSIM保持在0.42以上，高于所有基线的0.40以下，展示出更强的鲁棒性。
- 人类评估中，ScenA被显著偏好（对所有基线的胜率从59.8%到84.6%，p<0.05~0.001）。
- 消融表明：高噪声偏置时间步分布对绑定性能至关重要；加法槽位嵌入优于RoPE和基线；对抗性参考注入和课程打乱进一步提升绑定。
- 证实了“参考捷径”的存在并提供了一个简单有效的解决方案。

## 7. 优点
- **方法简洁**：无需结构化标签或额外身份编码器，仅靠自由形式文本提示和参考潜变量拼接实现多说话人控制。
- **充分利用基础模型能力**：继承预训练模型对自然音频（环境音、重叠、副语言事件）的建模能力。
- **解决新颖问题**：识别并量化了参考捷径，并提出针对性方案（高噪声偏置分布），而且该诊断可能通用适用于其他参考条件化流匹配模型。
- **实验扎实**：包含标准基准、野外鲁棒性测试、多组消融、人类评估，结论可靠。
- **开源潜力**：提供了项目页面和清晰实现细节。

## 8. 不足与局限
- **时长限制**：继承自骨干模型LTX-2，生成音频最长20秒，无法处理更长对话。
- **参考数量限制**：Kmax=3个参考说话人，超过时需要扩展或使用参考压缩器。
- **流匹配范式固有缺陷**：生成时长需在采样前预设，无法动态调整。
- **自然度指标分歧**：UTMOS评分较低（可能由于骨干模型训练于视频音轨而非演播室语音），SQUIM评分较好，说明自然度评估存在偏差。
- **未跨模态验证**：论文指出参考捷径诊断应适用于图像和视频，但未在那些领域实验验证。
- **消融设置**：部分训练配方消融结果（始终打乱导致崩溃）表明课程学习的关键性，但对鲁棒性的理解仍需更广范围的调参。

（完）
