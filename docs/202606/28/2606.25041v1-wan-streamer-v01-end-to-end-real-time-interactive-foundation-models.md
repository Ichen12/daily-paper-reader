---
title: "Wan-Streamer v0.1: End-to-end Real-time Interactive Foundation Models"
title_zh: Wan-Streamer v0.1：端到端实时交互基础模型
authors: "Lianghua Huang, Zhifan Wu, Wei Wang, Yupeng Shi, Mengyang Feng, Junjie He, Chenwei Xie, Yu Liu, Jingren Zhou, Ang Wang, Bang Zhang, Baole Ai, Chen Liang, Cheng Yu, Chongyang Zhong, Jinwei Qi, Kai Zhu, Pandeng Li, Peng Zhang, Wenyuan Zhang, Xinhua Cheng, Yitong Huang, Yun Zheng, Zoubin Bi"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.25041v1"
tags: ["query:speech-audio"]
score: 8.0
evidence: 包含ASR和TTS的端到端实时交互模型
tldr: 传统级联视听交互系统依赖独立的VAD、ASR、TTS等模块，导致高延迟和误差累积。本文提出Wan-Streamer，一个原生流式端到端交互基础模型，在单一Transformer中交织视觉、音频、文本的输入输出token，通过块因果注意力实现增量流式处理。模型侧响应延迟约200ms，总交互延迟约550ms，支持160ms流式单元和25fps帧率。这是首个统一端到端的多模态流式交互模型，实现亚秒级全双工视听通信。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25041v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1700, \"height\": 799, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25041v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1708, \"height\": 458, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25041v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1725, \"height\": 897, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25041v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1728, \"height\": 1201, \"label\": \"Table\"}]"
motivation: 现有级联视听交互系统延迟高且误差累积，缺乏统一端到端流式模型。
method: 设计单Transformer，交织多模态输入输出token，采用块因果注意力和因果编码器实现原生流式处理。
result: 模型侧响应延迟200ms，总交互延迟550ms，支持160ms流式单元和25fps帧率。
conclusion: Wan-Streamer作为统一端到端流式模型，显著降低延迟，为实时全双工视听交互奠定基础。
---

## 摘要
我们提出了Wan-Streamer，一个原生流式、端到端的交互基础模型，从头设计用于实时、低延迟、全双工的音视频交互。Wan-Streamer在单个Transformer中无缝建模语言、音频和视频作为输入和输出，其中序列表示为交织的视觉、音频和文本输入标记，以及视觉、音频和文本输出标记，通过块因果注意力进行协调以实现增量流式。与依赖独立VAD、ASR、语言、TTS、音频驱动动画或视频生成模块的级联交互系统不同，Wan-Streamer不依赖外部语言、语音、虚拟形象或视频生成模块：感知、推理、生成、响应时序、话轮管理和跨模态同步在一个统一模型中联合学习，减少了流水线延迟和错误累积。为支持自然的音视频响应能力，我们围绕流式能力重新设计了整个堆栈，包括因果编码器、因果解码器、块因果注意力和低延迟多模态标记调度，使得在25 fps下流式单元短至160毫秒。Wan-Streamer实现了约200毫秒的模型侧响应延迟，结合350毫秒双向网络延迟后总交互延迟约550毫秒，支持亚秒级双工音视频通信。这些结果使Wan-Streamer成为一个统一的、端到端的多模态交互基础模型，适用于低延迟流式交互。

## Abstract
We present Wan-Streamer, a native-streaming, end-to-end interactive foundation model designed from the ground up for real-time, low-latency, full-duplex audio-visual interaction. Wan-Streamer seamlessly models language, audio, and video as both input and output within a single Transformer, where the sequence is represented as interleaved visual, audio, and text input tokens together with visual, audio, and text output tokens, coordinated by block-causal attention for incremental streaming. Unlike cascaded interactive systems that rely on separate VAD, ASR, language, TTS, audio-driven animation, or video-generation modules, Wan-Streamer does not rely on external language, speech, avatar, or video-generation modules: perception, reasoning, generation, response timing, turn management, and cross-modal synchronization are learned jointly within one unified model, reducing pipeline latency and error accumulation. To support natural audio-visual responsiveness, we redesign the entire stack around streamability, including causal encoders, causal decoders, block-causal attention, and low-latency multimodal token scheduling, enabling streaming units as short as 160 ms at 25 fps. Wan-Streamer achieves approximately 200 ms model-side response latency and approximately 550 ms total interaction latency when combined with 350 ms bidirectional network latency, supporting sub-second duplex audio-visual communication. These results position Wan-Streamer as a unified, end-to-end, multimodal interactive foundation model for low-latency streaming interaction.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **现有问题**：真实人类交互是流式、全双工的（边看边听边说，感知与表达重叠）。当前大多数视听交互系统采用级联架构，独立使用 VAD、ASR、LLM、TTS、音频驱动动画、视频生成等模块，导致模块边界等待时间、错误累积、识别与同步误差、话轮管理困难，且难以实现多模态间的原生同步。
- **研究目标**：构建一个原生流式、端到端、全双工的音视频交互基础模型，在单一 Transformer 中统一建模语言、音频、视频的输入与输出，实现低于秒级的交互延迟。
- **整体含义**：Wan-Streamer 是首个将感知、推理、生成、响应时序、话轮管理和跨模态同步在一个模型中联合学习的系统，无需外部模块，为实时全双工视听交互提供了新的范式。

## 2. 论文提出的方法论

### 核心思想
- 采用**单 Transformer**，将用户观测（语言、音频、视频输入 token）与代理响应（输出 token）交织为因果序列，通过**块因果注意力**实现增量流式生成。
- 所有组件均为**因果设计**：因果 VAE（音视频）、因果编码器、因果解码器、块因果注意力、低延迟多模态 token 调度。
- 训练目标：语言部分用交叉熵预测 token，音视频部分用**条件流匹配**（conditional flow matching）在连续潜空间中联合生成。

### 关键技术细节
1. **数据表示**：输入包含用户端文本、音频、视频；输出包含代理端文本、音频、视频。所有数据按 streaming unit（160ms，25fps）组织。
2. **模型训练三阶段**：
   - **第一阶段：独立任务预训练**。从语言模型初始化，混合理解任务（图像/音频/视频理解、ASR、TTS、对话）与生成任务（图像/音频/视频/联合生成）。
   - **第二阶段：端到端交互训练**。使用全双工交互数据（文本、音频、视频在输入输出侧交织），学习响应时序、主动聆听、打断处理、长上下文一致性。
   - **第三阶段：低延迟蒸馏**。将带 classifier-free guidance (CFG) 和多步求解器的强教师模型蒸馏为高效学生模型，并使用滚动蒸馏（rolling distillation）结合 self-forcing 与分布匹配，减少长序列滚动时的训练-测试不匹配。
3. **推理架构：Thinker-Performer 分离**：
   - **Thinker**：托管因果编码器、短 token-causal Transformer（语言预测与状态更新）、KV cache 构建、因果解码器。
   - **Performer**：只运行流匹配求解器生成下一帧音视频潜变量。
   - 流程：每步中，Thinker 编码当前用户观测、更新 KV cache、解码上一帧响应进行发射；同时接收 Performer 返回的上一帧潜变量，发送当前 KV cache 给 Performer。Performer 使用完整历史 KV 运行流匹配生成下一帧潜变量。
   - 通过重叠编码、推理、解码、通信，实现约 200ms 模型侧响应延迟。

## 3. 实验设计

### 数据集/场景
- 论文未给出具体数据集名称，但描述了数据来源：包括理解数据（图像、音频、视频理解，文本对话，ASR，TTS，音频对话等）和生成数据（图像、音频、视频生成，联合音视频生成）以及端到端双工交互数据。具体构成未公开。

### Benchmark
- 主要对比**响应延迟**（response latency）和**交互延迟**（total interaction latency）。
- 对比的系统分两类：
  - **语音/全模态对话系统**（表1）：包含 Doubao Realtime Voice、Seeduplex、GPT-4o Realtime API、Hume EVI 3、Gemini Live API、Sesame、Moshi、Qwen3/3.5-Omni、MiniCPM-o 4.5 等。Wan-Streamer 的报告数字：模型侧约200ms，总延迟（含350ms网络）约550ms，并输出25fps视频。其他系统大多只报告部分延迟（如首个 token、端点检测），且多数不支持同步视觉输出。
  - **视觉代理/流式虚拟形象/音视频生成系统**（表2）：包含 Body of Her、MIDAS、U-Mind、X-Streamer、LPM 1.0、MAViD、M.I.O 等完整交互系统，以及 VASA-1、TalkingMachines、StreamAvatar、AvatarForcing、LiveTalk、Hallo-Live、OmniForcing 等渲染/生成组件。Wan-Streamer 的25fps、550ms总延迟覆盖完整音视频感知-响应环路。

### 对比方法
- 与多个近期商业和研究系统进行定性比较，但未进行定量性能指标（如 FID、MOS 等）的对比，只聚焦于延迟。

## 4. 资源与算力
- **论文未明确说明**训练所用 GPU 型号、数量、时长。仅提及在推理时使用双 GPU（thinker + performer），并使用了 CUDA graph capture、编译、优化内核和 KV cache 交换。

## 5. 实验数量与充分性
- **实验数量**：主要报告了延迟对比（表1、表2）以及自然性、打断处理等定性讨论。没有提供消融实验定量分析（如模块去除、流式单元大小影响、蒸馏效果量化等）。
- **充分性评价**：从延迟角度，实验是充分的——清晰区分了模型侧延迟与端到端延迟，并对比了多种现有系统。但缺点在于：
  - **缺乏定量质量评估**：未报告生成音视频的客观质量指标（如 FID、MOS、同步误差）。
  - **缺乏用户研究**：未报告真实用户交互体验的评估。
  - **分辨率限制**：当前 v0.1 仅验证了 192p 输出分辨率，扩展性未充分证明。
  - 因此实验虽能说明延迟优势，但不足以全面验证模型在实际应用中的自然度和可靠性。

## 6. 论文的主要结论与发现
- Wan-Streamer 实现了 **~200ms 模型侧响应延迟**和 **~550ms 总交互延迟（含网络）**，显著低于许多现有级联系统。
- 通过统一端到端模型，能够**原生同步语音和视觉输出**（口型、面部表情、节奏），实现连贯的全双工行为（打断、主动对话）。
- 在非说话时段可维持自然的空闲行为（呼吸、微表情），聆听时能发出非语言反馈（注视、点头）。
- 这些结果证明：实时多模态交互系统应从头设计为原生全双工系统，而非后组装级联模块。

## 7. 优点
1. **创新性架构**：首次将语言、音频、视频的输入和输出完全统一在一个因果 Transformer 中，实现端到端流式处理，无需外部模块。
2. **系统低延迟**：通过 Thinker-Performer 分离、KV cache 交换、因果编解码器，达到亚秒级交互延迟。
3. **多模态同步**：由于音视频潜变量由同一上下文联合生成，同步是原生的，无需后对齐。
4. **学习全双工行为**：从数据中学习打断、主动说话、监听反馈，而不是依赖手工规则。
5. **训练策略实用**：三阶段训练（预训练→交互训练→蒸馏）有效缩小了训练-测试差距。

## 8. 不足与局限
1. **输出分辨率低**：当前仅 192p，离实际高清应用还有距离，虽然声称可扩展至更高分辨率，但未给出证据。
2. **实验评估不全面**：缺乏生成质量（如 FID、MOS、同步误差）、中断成功率、长时间交互一致性等定量评测；没有用户研究。
3. **资源与训练细节缺失**：未公开 GPU 数量、训练时间、模型参数量，不利于复现和比较。
4. **数据未开源**：使用的数据集不公开，可能包含阿里内部数据，可重复性受限。
5. **仅 v0.1 版本**：论文标明是初步结果，更多改进和评估留待未来。
6. **对比不直接**：延迟对比中，其他系统的测量边界不同（有的是首个 token 延迟，有的是 API TTFB），虽已说明，但严格可比性不足。
7. **网络延迟假设固定**：350ms 双向网络延迟是假设，实际场景中波动会影响总延迟。

（完）
