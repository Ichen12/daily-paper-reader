---
title: One-Step Token-to-Waveform Generation with MeanFlow in Latent Space
title_zh: 基于隐空间MeanFlow的一步式Token到波形生成
authors: "Zheqi Dai, Guangyan Zhang, Zhen Ye, Jingyu Li, Haolin He, Chunyat Wu, Yiwen Guo, Qiuqiang Kong"
date: 2026-06-16
pdf: "https://arxiv.org/pdf/2606.18072v1"
tags: ["query:speech-audio"]
score: 9.0
evidence: 一步式Token到波形生成用于文本到语音合成
tldr: 在基于LLM的语音合成和多模态系统中，低比特率语义编解码器的Token2Wav解码器成为质量和效率的瓶颈，传统多步流匹配解码器迭代采样导致高延迟。本文提出在高度压缩的潜在空间应用MeanFlow建模平均速度而非瞬时速度场，实现一步生成，避免波形级流的内存和稳定性问题。相比多步基线，实时因子提升17倍且质量几乎无损。引入冻结生成器仅微调解码器或端到端联合微调等细化策略进一步改善保真度，且不增加推理代价。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.18072v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1748, \"height\": 810, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.18072v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1662, \"height\": 274, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.18072v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 884, \"height\": 552, \"label\": \"Table\"}]"
motivation: 传统多步流匹配Token2Wav解码器推理延迟高，形成质量-速度严重权衡，亟需一步生成方案。
method: 在潜在空间用MeanFlow建模平均速度场，实现一步Token到波形生成；并采用解码器微调或联合微调缓解潜在不匹配。
result: 相比多步基线，实时因子提升17倍，质量退化可忽略。
conclusion: 潜在空间MeanFlow实现高质量一步Token2Wav生成，兼顾速度与保真度。
---

## 摘要
神经音频编解码器是现代基于大型语言模型（LLM）的文本转语音（TTS）和多模态系统的核心。随着低比特率语义编解码器的兴起，Token到波形（Token2Wav）解码器成为决定感知质量和系统效率的瓶颈。传统的多步流匹配解码器提供了卓越的质量，但由于迭代采样导致推理延迟高，造成了严重的质量-速度权衡。本文提出了一种新颖的Token2Wav架构，通过在高度压缩的隐空间中应用MeanFlow来克服这一限制。通过建模平均速度而非瞬时速度场，MeanFlow实现了真正的一步式生成。在隐域中操作缓解了波形级流的内存和稳定性问题，与多步基线相比，实时因子（RTF）提升了高达17倍，且质量下降可忽略不计。此外，我们引入了减轻隐空间失配的改进策略，包括冻结MeanFlow生成器的仅解码器微调和端到端联合微调，在不增加推理成本的情况下提高了保真度。代码和演示已公开提供。

## Abstract
Neural audio codecs are central to modern LLM-based Text-to-Speech (TTS) and multimodal systems. As low-bitrate semantic codecs gain prominence, the Token-to-Waveform (Token2Wav) decoder becomes a bottleneck determining both perceptual quality and system efficiency. Conventional multi-step flow-matching decoders offer superior quality but suffer from high inference latency due to iterative sampling, creating a severe quality-speed trade-off. In this paper, we propose a novel Token2Wav architecture that overcomes this limitation by applying MeanFlow in a highly compressed latent space. By modeling the average velocity rather than the instantaneous velocity field, MeanFlow enables true one-step generation. Operating in the latent domain mitigates the memory and stability issues of waveform-level flows, yielding up to a 17$\times$ improvement in Real-Time Factor (RTF) compared to multi-step baselines with negligible quality degradation. Furthermore, we introduce refinement strategies that mitigate latent mismatch, including decoder-only fine-tuning with the MeanFlow generator frozen and end-to-end joint fine-tuning, improving fidelity without increasing inference-time cost. Code and demo are publicly available.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **背景**：现代基于 LLM 的 TTS 和多模态系统广泛采用离散 token 范式：上游 LLM 预测语义 token，下游 Token2Wav 解码器将 token 转换为波形。低比特率语义 token 虽提升 LLM 效率，却将大量声学细节恢复负担转移给解码器，使其成为系统质量和延迟的瓶颈。
- **问题**：传统的流匹配解码器通过多步 ODE 迭代采样实现高质量生成，但推理延迟高（数十次网络评估），形成严重的质量‑速度权衡。亟需一种既能保持高保真度又能大幅降低延迟的一步式生成方案。
- **整体含义**：本文提出在高度压缩的隐空间中应用 **MeanFlow**（平均速度场），实现一步式 Token2Wav 生成，兼顾感知质量和实时性，为低延迟、设备端 TTS 系统提供了实用路径。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：将一步式生成从宽长的波形空间转移到低维压缩的隐空间，利用 MeanFlow 建模从先验到数据分布的平均速度，实现单网络评估即可完成 token‑to‑latent 转换，再通过轻量 VAE 解码器确定性还原波形。
- **关键技术细节**：
  1. **波形 VAE**：学习一个轻量编解码器，将 24 kHz 波形下采样为 25 Hz 的低维隐序列 `z ∈ R^{T'×D}`（总下采样率 960）。编码器使用 Oobleck 风格卷积堆栈，解码器为确定性结构。训练使用多分辨率 STFT 重建损失、对抗损失、特征匹配损失和 KL 正则化。
  2. **隐空间 MeanFlow**：采用 rectified flow 路径 `z_t = (1-t)z_data + tε`（反向时间约定）。标准流匹配学习瞬时速度 `v_θ`；MeanFlow 学习区间 `[r, t]` 上的平均速度 `ū`。损失函数为：
     ```
     L_MF(θ) = E_{r,t,x,ε}[ w(r,t) || f_θ(z_t, r, t, c) - sg(ū_gt) ||_2^2 ]
     ```
     其中 `sg` 为停止梯度，`w` 为自适应权重，`ū_gt` 按 [13] 构造。通过 Jacobian‑vector product 计算导数。
  3. **条件注入**：将语义 token 嵌入、时间步嵌入、说话人嵌入（来自 CAM++ 的 192 维向量）融合为条件序列，通过 adaLN‑Zero 调制 1D Diffusion Transformer (DiT‑1D) 的每一层。
  4. **一步推理**：从高斯噪声 `z_1` 出发，一次前向得到 `z_gen = z_1 - f_θ(z_1, 0, 1, c)`，再通过 VAE 解码器得到波形 `ˆx = G_ψ(z_gen)`。
- **改进策略（缓解隐空间失配）**：
  - **Decoder‑only FT**：冻结生成器 `f_θ`，仅微调 VAE 解码器 `G_ψ`，适应生成器产生的隐分布。
  - **Joint FT**：端到端联合微调 `f_θ` 和 `G_ψ`，反向传播波形域损失（MR‑STFT + 对抗 + 特征匹配）通过一步采样步骤，不改变推理流程。

### 3. 实验设计：使用数据集、benchmark、对比方法
- **数据集**：训练在 **LibriTTS** 上，评估在 **LibriSpeech test‑clean** 子集（16 kHz 原生，但模型合成 24 kHz，评估前重采样至 16 kHz）。
- **token 化**：使用 CosyVoice2 的语义 tokenizer（单码本，词汇量 6561，25 Hz）。
- **说话人条件**：预训练 CAM++ 提取 192 维 speaker embedding。
- **对比方法**：
  - **主基线**：CosyVoice2 Token2Wav 模块（token‑to‑mel 流匹配 + 预训练声码器），10 步 Euler 采样。
  - **Oracle 上限**：VAE 重建（编码真实波形再解码）。
- **评估指标**：
  - **WER**（%）：用微调 HuBERT‑Large ASR。
  - **SpkSim**：WavLM‑Large 说话人验证余弦相似度。
  - **UTMOS**：非侵入性语音质量评估。
  - **MOS**：20 人 5 级主观评分（50 句随机）。
  - **RTF**：FP16 batch‑1 在 NVIDIA H20 GPU 上测量，包括生成器和解码器。

### 4. 资源与算力
- 论文**未明确**报告训练的 GPU 型号、数量及总时长。仅在 RTF 测量中提到使用 **NVIDIA H20 GPU**（FP16，batch‑1）。VAE 和生成器训练细节中未提及计算资源。

### 5. 实验数量与充分性
- **主要实验**：一张主表（表 1）对比最佳配置与基线、Oracle 上限。
- **消融实验**：三组（表 2）：
  1. 隐维度 D = 8/16/24
  2. 模型容量 140M vs 600M DiT
  3. 改进策略：无微调、Decoder‑FT、Joint‑FT
- **主观评测**：50 句 20 人 MOS 测试。
- **公平性**：所有系统使用相同的语义 token、说话人嵌入、评估协议。RTF 测量方式一致。存在局限性：仅评测 LibriSpeech clean，未涉及噪声、多说话人、跨语言等场景。

### 6. 论文的主要结论与发现
- **一步生成可行**：隐空间 MeanFlow 在 **D=24、140M DiT、Joint‑FT** 配置下，以 RTF 0.0046（17× 加速）达到 WER 3.41%、SpkSim 0.932、UTMOS 3.64、MOS 3.85，接近多步基线（WER 3.18%, MOS 4.05）且质量退化可忽略。
- **隐维度影响**：D 从 8 到 24 持续改善质量，D=16~24 为实用选择。
- **容量缩放非简单有效**：600M 模型未优于 140M，推测一步生成对积分误差更敏感，大模型可能过拟合。
- **改进策略有效**：Joint‑FT 显著提升感知质量（UTMOS 3.11→3.64, MOS 3.35→3.85），Decoder‑FT 次之。大部分剩余退化源于隐生成而非解码。

### 7. 优点
- **方法新颖**：首次将 MeanFlow 应用于 Token2Wav 任务的隐空间，实现真正一步式生成，兼具速度与质量。
- **系统设计完整**：引入轻量 VAE 压缩波形、合适的条件注入、以及针对隐失配的改进策略，不影响推理成本。
- **评估全面**：涵盖客观（WER、SpkSim、UTMOS、RTF）和主观（MOS）指标，消融实验覆盖维度、容量、训练策略。
- **开源**：提供代码和 demo，可复现。

### 8. 不足与局限
- **实验场景有限**：仅在 LibriSpeech test‑clean（英文干净语音）上评估，未测试噪声、多口音、跨语言、实时性等更具挑战的场景。
- **模型容量结论存疑**：600M 模型表现略差，可能源于训练策略或超参数未充分调优，需更多验证。
- **生成质量仍低于多步基线**：MOS 差距约 0.2 分，WER 略高，在注重极致质量的应用中可能不足。
- **资源消耗未报告**：缺少训练 GPU 数量、时间等信息，难以评估训练成本。
- **仅依赖单一 tokenizer**：仅使用 CosyVoice2 的语义 token，未验证与其他 tokenizer（如 HuBERT、SoundStream）的兼容性。

（完）
