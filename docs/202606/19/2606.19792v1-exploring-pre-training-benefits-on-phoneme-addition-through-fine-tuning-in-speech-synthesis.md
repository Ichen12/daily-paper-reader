---
title: Exploring Pre-training Benefits on Phoneme Addition through Fine-tuning in Speech Synthesis
title_zh: 通过微调探索预训练对语音合成中音素添加的益处
authors: "Masato Murata, Koichi Miyazaki, Tomoki Koriyama, Tomoki Toda"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.19792v1"
tags: ["query:speech-audio"]
score: 9.0
evidence: 文本到语音微调中音素添加的研究
tldr: 低资源语音合成中，微调预训练模型时常需添加训练中未见过的音素，称为“音素添加”。然而预训练对已见音素的能力是否有助于这一过程尚不明确。本文通过LLM生成音素控制语料的模拟实验和英语到日语跨语言真实语音实验，发现微调虽比从头训练自然度更高，但新音素达到可比音素错误率所需的数据量相同或更多。结论表明预训练主要贡献于自然度提升，而对音素添加的泛化能力有限，揭示了预训练在音素层面迁移的局限性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.19792v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 716, \"height\": 333, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19792v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 798, \"height\": 621, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19792v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 789, \"height\": 339, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.19792v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 830, \"height\": 332, \"label\": \"Table\"}]"
motivation: 预训练模型微调时添加新音素能否受益于预训练能力尚不明确。
method: 在LLM生成音素控制语料模拟和英语到日语跨语言迁移两个设置中，对比微调与从头训练的效果。
result: 微调自然度更高，但新音素达到可比PER所需数据量相同或更多。
conclusion: 预训练主要提升自然度，对音素添加的好处有限。
---

## 摘要
迁移学习广泛应用于低资源文本到语音合成。当目标语料包含预训练中未见的音素时，模型必须在微调期间扩展其音素库，我们称此过程为“音素添加”。然而，预训练生成已见音素的能力是否有助于此过程仍不清楚。本研究在两个设置中调查音素添加：(1) 使用大语言模型生成音素可控语料的模拟设置，可在不考虑混杂因素的情况下进行研究；(2) 真实语音跨语言迁移设置（英语到日语），以验证发现是否在实践中成立。两种设置下的实验表明，虽然微调比从头训练实现了更高的自然度，但需要同样多或更多的数据才能在新的音素上达到相当的PER。这些结果表明，预训练主要有助于提高自然度，但对音素添加的益处有限。

## Abstract
Transfer learning is widely used for low-resource text-to-speech. When the target corpus contains phonemes unseen in pre-training, the model must expand its phoneme inventory during fine-tuning; we call the process "phoneme addition." However, it remains unclear whether the pre-trained ability to generate seen phonemes contributes to this process. This study investigates phoneme addition in two settings: (1) a simulation setup using LLM-generated phoneme-controlled corpora that enables investigation without considering confounding factors, and (2) a real-speech cross-lingual transfer setup (English to Japanese) to validate whether the findings hold in practice. Experiments in both settings showed that while fine-tuning achieved higher naturalness than training from scratch, it required as much or more data to achieve comparable PER for new phonemes. These results indicate that pre-training mainly contributes to naturalness improvement, but offers limited benefit for phoneme addition.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：低资源文本到语音合成（TTS）中，迁移学习是常用方法。当目标语言包含预训练中未见的音素时，模型需在微调期间扩展音素库，此过程称为“音素添加”。然而，预训练模型对已见音素的生成能力是否真正有助于添加新音素尚不明确。
- **整体含义**：验证预训练知识在音素添加过程中的实际贡献，以指导更高效的低资源TTS开发。本文挑战了“预训练知识普遍有益”的常见假设。

## 2. 提出的方法论

- **核心思想**：通过对比微调（从预训练模型开始）与从头训练（随机初始化）在相同目标数据集上的表现，分离预训练知识的贡献。研究两个场景：模拟场景（排除语言、说话人等混杂因素）和真实跨语言场景（验证可推广性）。
- **关键技术细节**：
  - **模拟场景**：使用LLM（Claude Opus 4.6）生成两类音素可控语料：
    - **Limited语料**（预训练用）：排除特定目标音素（如塞辅音或前元音），仅使用允许词表（从CMU发音词典提取超过23,000个不含目标音素的词）生成句子。
    - **Full语料**（微调用）：包含所有40个英语音素，且每个句子至少含一个目标音素。
    两类语料均匹配VCTK数据集统计特征（每句平均音素数、词数分布等），并通过espeak-ng转换为IPA验证。使用预训练的TTS模型将文本合成为语音，得到语音–音素配对数据。
  - **真实跨语言场景**：英语（VCTK）预训练→日语（JSUT）微调。添加20个日语特有音素（如/a/、/c/、/ts/等）。
  - **模型架构**：Conformer-FastSpeech2 (CFS2)，使用ESPnet实现。微调时仅随机初始化新增音素的嵌入，其余参数继承预训练权重。从头训练模型所有参数随机初始化。
  - **波形生成**：使用预训练的HiFi-GAN声码器（在VCTK上训练）。
  - **评估指标**：
    - **目标音素错误率（Target PER）**：基于wav2vec 2.0的音素识别模型，仅计算目标音素的PER。
    - **自然度（UTMOS）**：基于预训练UTMOS模型的模拟MOS分数。

## 3. 实验设计

- **数据集与场景**：
  1. **模拟场景**：
     - 预训练数据集：Limited语料（107,000句，55小时，107个说话人，排除目标说话人p299）。
     - 微调/从头训练数据集：Full语料（2,000句，单一说话人p299，包含目标音素）。
     - 目标音素两组：塞辅音（/p/, /b/, /t/, /d/, /k/, /g/）和前元音（/i/, /ɪ/, /eɪ/, /ɛ/, /æ/）。
     - 训练数据量：100、300、500、800、1,000、2,000句（从Full语料中随机抽取）。
     - 测试集：1,000句VCTK文本（含至少一个目标音素，未用于训练）。
  2. **真实跨语言场景**：
     - 预训练数据集：VCTK（英语，44小时，108说话人）。
     - 微调/从头训练数据集：JSUT basic5000（日语，单说话人，约10小时）。
     - 目标音素：20个日语特有音素。
     - 训练数据量：同上6种规模。
     - 测试集：100句JVS语料（日语，未用于训练）。

- **基准方法**：从头训练（Scratch），即随机初始化模型在相同目标数据集上训练。

- **对比方法**：微调（Fine-tuning） vs 从头训练。每种设置下均使用相同数据量进行公平比较。

## 4. 资源与算力

- 论文未明确说明具体使用的GPU型号、数量或训练时长。仅提及使用预训练TTS模型（在VCTK上训练）和ESPnet框架。未提供硬件资源细节。

## 5. 实验数量与充分性

- **实验数量**：
  - 模拟场景：两组目标音素 × 6种数据量 × 2种训练方式 = 24组模型训练。
  - 真实跨语言场景：1组目标音素 × 6种数据量 × 2种训练方式 = 12组模型训练。
  - 此外，进行了初步实验确认学习率影响有限，并分析了语谱图。
- **充分性评价**：
  - **充分**：覆盖了不同音素类型（辅音和元音）和不同数据量（从极低资源到中等资源），对比了微调与从头训练两个最直接的基准。
  - **客观公平**：模拟场景使用相同合成管道生成数据，消除混杂因素；真实场景使用标准公开数据集。评估指标客观（PER和UTMOS）。
  - **局限**：仅测试了英语和日语两个语种，未覆盖其他语言或不同说话人多样性；仅使用单一模型架构（CFS2），未验证其他TTS架构。

## 6. 主要结论与发现

- **音素添加能力**：无论目标音素类型如何，微调和从头训练均能成功添加新音素。
- **音素精确度（PER）**：从头训练在大多数数据量下达到相当或更优的Target PER。微调需要相同或更多的训练数据才能达到可比PER。
- **自然度（UTMOS）**：微调的UTMOS分数始终高于或相当于从头训练，尤其在低资源条件下（100-500句）。
- **核心发现**：预训练知识对音素添加的益处有限，主要贡献在于提高整体自然度；而音素添加本身并未从预训练中显著获益。

## 7. 优点

- **方法论创新**：设计了LLM生成的音素可控语料，实现了对混杂因素（语言、说话人、数据域差异）的有效隔离，使得音素添加过程可独立研究。
- **双场景验证**：通过模拟场景和真实跨语言场景相互验证，增强了结论的可靠性。
- **细致的实验粒度**：系统性地变化训练数据量（6个规模），揭示了数据量对结论的影响。
- **评估指标全面**：同时采用目标音素错误率（专门针对新音素）和自然度MOS，区分了不同维度的表现。

## 8. 不足与局限

- **语言覆盖有限**：仅研究了英语和日语，其他语言（如声调语言、不同音素库大小）是否一致未知。
- **模型架构单一**：仅使用Conformer-FastSpeech2，未验证其他TTS模型（如VITS、Tacotron2）或不同预训练策略。
- **模拟场景合成数据**：使用同一TTS模型合成语音，可能引入系统性偏差（如合成语音与现实语音的声学差异）。
- **未考虑音素上下文依赖**：音素添加可能受音位环境（如协同发音）影响，实验未深入分析。
- **未探索改进方法**：仅观察现象，未提出解决“预训练对音素添加帮助有限”的具体方案（论文仅在结论中提及未来方向）。
- **资源细节缺失**：未报告训练算力消耗，不利于复现和成本评估。

（完）
