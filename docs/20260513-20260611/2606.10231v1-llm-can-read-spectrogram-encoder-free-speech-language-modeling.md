---
title: "LLM can Read Spectrogram: Encoder-free Speech-Language Modeling"
title_zh: LLM能够读取频谱图：无编码器的语音-语言建模
authors: "Ruchao Fan, Yiming Wang, Yuxuan Hu, Bo Ren, Yufei Xia, Xiaofei Wang, Yao Qian, Jinyu Li"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.10231v1"
tags: ["query:speech-audio"]
score: 9.0
evidence: 无编码器的语音-语言模型同时处理ASR和TTS
tldr: 当前语音大语言模型通常依赖预训练语音编码器将音频转为语义表示，增加了端到端系统的设计复杂度。本文探索了绕过专用编码器的可能性，提出了Mel-LLM，将轻量预处理的梅尔频谱图切块并通过线性投影直接送入LLM，让LLM通过自身参数学习语音与文本的对齐。在自动语音识别任务上，该编码器无关方案在OpenASR公开榜及大规模实验中取得了有竞争力的性能，仅比编码器初始化方案有轻微下降。尤其在数据有限时，从多模态模型Phi-4-MM初始化至关重要，消融实验还定位了LLM中与语音编码关联较小的层。文本转语音方面，使用下一令牌VAE方法展示了初步但可行的结果，为完全统一的无编码器自回归语音文本架构奠定了基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10231v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 822, \"height\": 615, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10231v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 805, \"height\": 641, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.10231v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 895, \"height\": 246, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10231v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1843, \"height\": 294, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10231v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1839, \"height\": 291, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10231v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1837, \"height\": 397, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10231v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 898, \"height\": 328, \"label\": \"Table\"}]"
motivation: 传统语音大模型需额外语音编码器，不仅增加系统复杂度，也阻碍了语音与文本的端到端统一处理。
method: 提出Mel-LLM，将梅尔频谱图切为补丁，经线性投影直接送入LLM，由LLM自主学习语音-文本对齐。
result: ASR任务性能接近编码器方案，数据有限时多模态初始化有效，还发现LLM中与语音编码弱相关的层；TTS初步结果可行。
conclusion: 该工作验证了无编码器自回归语音文本建模的可行性，为更简洁统一的语音大语言模型开辟新路。
---

## 摘要
最近具备语音感知能力的大语言模型（Speech-LLM）依赖预训练的语音编码器将音频转换为富含语义的表示，供LLM使用。在本工作中，我们探索了另一种可能性：LLM能否在没有专用语音编码器的情况下，直接学会读取梅尔频谱图？我们提出了Mel-LLM，一种无编码器的Speech-LLM，它通过线性投影将经过轻量预处理的梅尔频谱图块直接馈送到LLM中，使LLM能够完全通过自身参数学习语音与文本的对齐。我们在自动语音识别（ASR）和文本到语音（TTS）任务上进行了广泛实验。对于ASR，我们在OpenASR排行榜公开数据集和生产级规模的实验中进行了评估，结果表明，与使用编码器初始化的方法相比，无编码器方案实现了有竞争力的性能，只有有限的性能下降。我们发现，当数据有限时，从多模态检查点（Phi-4-MM）进行初始化对于保持性能至关重要。我们还通过消融研究揭示了哪些LLM层与语音编码的相关性较低。对于TTS，我们展示了使用下一令牌VAE方法的初步结果。虽然TTS性能尚未达到最优，但这些结果证明了为自回归语音-文本建模建立完全统一的无编码器架构的可行性。

## Abstract
Recent speech-aware large language models (Speech-LLMs) rely on a pre-trained speech encoder to convert audio into semantic-rich representations consumable by LLM. In this work, instead, we explore: can an LLM learn to read Mel spectrogram directly without a dedicated speech encoder? We propose Mel-LLM, an encoder-free Speech-LLM that feeds lightly pre-processed Mel spectrogram patches directly into the LLM through a linear projection, allowing the LLM to learn speech-text alignment purely through its own parameters. We conduct extensive experiments on both automatic speech recognition (ASR) and text-to-speech (TTS) tasks. For ASR, we evaluate on the OpenASR leaderboard public sets and production-level scaling experiments, demonstrating that the encoder-free solution achieves competitive performance with only limited degradation compared to encoder-initialized counterparts. We find that when data is limited, initialization from a multimodal checkpoint (Phi-4-MM) is crucial for maintaining performance. We also present ablation studies revealing which LLM layers are less relevant to speech encoding. For TTS, we show preliminary results with a next-token VAE approach. While TTS performance is not yet optimal, these results establish the feasibility of a fully unified encoder-free architecture for autoregressive speech-text modeling.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：当前的语音大语言模型（Speech-LLM）普遍采用“预训练语音编码器+模态投影器+大语言模型”的范式，其中语音编码器（如Whisper、Conformer）负责将音频转换为高层语义表征。该范式存在**参数量大、计算开销高、编码器与LLM之间可能存在表征不匹配、信息流受编码器瓶颈限制**等问题。
- **整体含义**：受视觉领域无编码器多模态模型（如Fuyu-7B、Tuna-2）的启发，本文提出一个根本性问题——**LLM能否跳过专用语音编码器，直接从梅尔频谱图中读取信息并进行语音理解与生成？** 如果可行，将简化模型架构、降低计算成本，并为语音与文本的完全端到端统一建模铺平道路。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：提出 **Mel-LLM**，完全移除主语音编码器（Transformer/Conformer 模块），仅保留可选的卷积层用于降采样，将轻量预处理后的梅尔频谱图切块，通过线性投影直接送入 LLM，让 LLM 的底层 Transformer 层自主学习语音编码功能。
- **ASR 路径（语音输入）**：
  1. 提取 80 维 log-Mel 特征。
  2. 进行均值方差归一化（MVN）。
  3. 可选卷积层进行时间降采样（ASR 任务使用 8 倍降采样）。
  4. 线性投影到 LLM 隐藏维度。
  5. 与文本提示嵌入拼接，送入 LLM 自回归生成文本。
  6. LLM 使用 LoRA 适配（秩 320, α=640）。
- **TTS 路径（语音输出）**：
  1. 文本输入 LLM，自回归生成语音位置的隐藏状态。
  2. 通过 **VAE 解码器**从隐藏状态预测梅尔频谱：隐藏状态 → 线性层得到 μ 和 log σ² → 重参数化 → 残差 MLP（3 层 tanh+dropout）。
  3. **Postnet 精炼**：5 层 Conv1D 残差后处理网络。
  4. **停止预测**：线性层预测音频结束。
  5. 训练损失包含：回归损失（L1+MSE）、KL 散度、停止令牌损失、频谱通量损失，与 MELLE 保持一致。
- **关键公式（文字描述）**：
  - 投影：E_s = W_proj · e + b_proj，W_proj ∈ R^{d_LLM × d_enc}。
  - ASR 生成：T̂ = LLM(Concat(E_s, E_p))。
  - VAE 步骤：μ, log σ² = split(W_latent · h)；z = μ + σ · ε；预测 m̂ = z + MLP_res(z)；最终 m̂_final = m̂ + Postnet(m̂)。

### 3. 实验设计：数据集 / 场景，benchmark，对比方法

- **ASR 公开榜实验**：训练数据为公开英文语料约 3.1 千万条（约 6.4 万小时），包含 LibriSpeech、GigaSpeech、MLS English、SPGISpeech、CommonVoice 15、VoxPopuli、TED-LIUM、AMI、Earnings-22、FLEURS English。**无任何专有数据**。在 **OpenASR 排行榜** 的多个测试集（AMI, Earnings22, Gigaspeech, LS-clean/other, SPGISpeech, TED-LIUM, VoxPopuli）上评估 WER。对比方法包括 Whisper-Large-V3、Phi-4‑MM（已有的编码器-LLM 架构）、带编码器初始化的 Phi-4‑MM 微调、以及随机初始化的编码器方案。
- **生产级扩展实验**：使用约 10 倍匿名内部数据进行一轮训练，比较编码器初始化与无编码器方案在呼叫中心、对话、听写三个内部测试集上的 WER。
- **TTS 实验**：使用 Libriheavy（约 5 万小时）训练 5 个 epoch，在 LibriSpeech test-clean 上进行零样本合成评估，指标包括 WER（Whisper-large-v3 转录）和 UTMOS（感知质量）。对比随机初始化、输入归一化、不同 dropout 设置和 KL 变体。
- **消融实验**：
  - 降采样率（6.25 Hz 至 100 Hz）对 WER 和训练速度的影响。
  - 逐层冻结 LoRA（从 L16-31 到 L28-31），分析不同层对语音编码的贡献。

### 4. 资源与算力

- **硬件配置**：使用 **16 块 NVIDIA H100 GPU**。
- **训练框架与设置**：采用 **DeepSpeed ZeRO Stage-1** 优化器；AdamW 优化器；峰值学习率 1e-4；线性预热衰减调度（预热 9000 步）；梯度裁剪 1.0；有效批大小 512。
- **训练时长/规模**：公开榜 ASR 数据迭代 3 次（未发现额外收益）；生产级扩展实验仅迭代 1 次；TTS 数据迭代 5 个 epoch。论文未给出具体训练时长（小时/天），但提供了相对于编码器基线的加速比（12.5 Hz 设置下为 1.57 倍）。

### 5. 实验数量与充分性

- **实验数量**：论文包含 **5 个主要结果表**（Table 1–5），分别涵盖 ASR 公开榜主结果、扩展数据比较、降采样率消融、层冻结消融、TTS 初步结果。总体覆盖了 ASR 和 TTS 两个任务、不同数据规模、多种架构变体。
- **充分性与公平性**：
  - **充分性**：实验设计较为系统，有公开榜标准化测试、自身构建的内部测试集、消融实验等，能支撑核心结论。TTS 部分承认仅为初步结果，尚未达到最优。
  - **公平性**：在 ASR 对比中，与经典编码器方案（Whisper-Large-V3、Phi-4-MM 基线）进行了公平比较，并针对数据规模展开对比。消融实验中，控制变量清晰（如固定 LoRA 初始化、仅改变降采样率），结论可靠。
  - **局限性**：TTS 实验尚不充分，对比基线有限，且 ASR 与 TTS 目前分开训练，尚未展示统一的联合训练结果。

### 6. 论文的主要结论与发现

- **无编码器 ASR 可行**：LLM 能够直接从梅尔频谱图学习到语音编码，在 OpenASR 公开榜上平均 WER 达到 7.12%，仅比带编码器的强基线（6.97%）稍差，且差距很小。
- **数据规模是关键**：数据有限时，无编码器模型性能下降明显（相对下降 11.3%）；当数据规模扩大 10 倍后，性能差距缩小至仅 3.8%，表明充足的训练信号能弥补缺少专用编码器的损失。
- **多模态初始化至关重要**：在数据受限时，利用 Phi-4-MM 预训练的 LoRA 权重进行初始化十分关键；随机初始化会导致更大幅度的性能下降。
- **LLM 层角色分化**：LLM 的低层（0–23 层）主要负责学习隐式语音编码，而高层（24 层以上）更关注高层语义和语言建模，其预训练权重对 ASR 也足够有效，冻结它们仅带来极小性能损失。
- **TTS 初步可行**：基于下一令牌 VAE 的无编码器 TTS 可以生成可听的语音，但性能尚未优化（WER 11.03%, UTMOS 3.10），且严重依赖多模态初始化与高 dropout 来避免曝光偏差。
- **架构统一潜力**：该工作证明了使用同一个 LLM 骨干网络同时处理语音理解和生成的可行性，为未来统一的语音‑文本模型奠定了基础。

### 7. 优点：方法或实验设计上的亮点

- **架构极简**：完全移除庞大专用语音编码器，仅用线性投影+轻量卷积，大幅降低模型复杂度和计算开销（ASR 训练速度提升 1.57 倍）。
- **原则性探索**：首次针对 Speech-LLM 进行了**编码器式与无编码器的系统对比**，并详细分析了数据规模、初始化、层角色等因素的影响。
- **可解释的层分析**：通过逐层冻结 LoRA，直观揭示了 LLM 内部不同层次在语音理解中的功能分化，为模型设计提供了有价值的见解。
- **注重数据效率**：明确指出多模态预训练初始化在低资源场景下的关键作用，为实际部署提供了指导。
- **双任务验证**：同时在 ASR 和 TTS 上进行了实验，尽管 TTS 尚不成熟，但展示了统一架构的可行性，拓展了研究边界。

### 8. 不足与局限：实验覆盖、偏差风险、应用限制等

- **ASR 与 TTS 分开展示**：目前仅分别验证了两个任务，未实现论文结尾提到的联合 ASR-TTS 训练或纯语音预训练，统一模型的完整潜力尚未展现。
- **TTS 性能差距明显**：TTS 结果相比现有最优方案（如经过优化的 MELLE 等）仍有较大差距，且对超参数（如 dropout）敏感，实用价值有限。
- **对比可能不够全面**：ASR 主要对比了 Phi-4-MM 及其变体，未与更多流行的编码器‑LLM 系统（如 SALMONN、Qwen-Audio 等）进行广泛对比。
- **训练细节未完全量化**：未报告训练总时长或具体能耗，仅给出加速比；生产级实验数据性质不透明（“匿名内部数据”），降低了可复现性。
- **领域偏差风险**：公开榜 ASR 训练数据均为英文，测试集虽多样但未必覆盖所有语言和极端声学场景；TTS 仅测试了英文朗读风格，推广性待验证。
- **应用限制**：移除编码器后，LLM 直接处理原始频谱块，可能对长音频的远程依赖建模效率不如经过精心设计的编码器结构，实际部署中需权衡。

（完）
