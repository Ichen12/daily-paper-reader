---
title: "Rethinking Depth: A study of the Recursive-Transformer for Speech Recognition"
title_zh: 深度再思考：递归Transformer在语音识别中的研究
authors: "Thomas Rolland, Carlos Carvalho, Alberto Abad"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.09357v1"
tags: ["query:speech-audio"]
score: 9.0
evidence: 用于高效ASR编码器的递归Transformer
tldr: "针对Transformer在语音识别中模型尺寸过大的问题，本文探索了通过深度递归共享层的递归式Transformer编码器。通过在潜在空间中应用有限次数的循环，该架构在保持相当识别性能的同时，将参数量削减66%。研究表明递归式Transformer是ASR中减小模型尺寸的有效方案，为轻量化语音识别模型设计提供了新思路。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.09357v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 714, \"height\": 600, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09357v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1404, \"height\": 448, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09357v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 670, \"height\": 415, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.09357v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 861, \"height\": 371, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09357v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 788, \"height\": 333, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09357v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 842, \"height\": 287, \"label\": \"Table\"}]"
motivation: Transformer ASR模型参数庞大，递归Transformer在ASR中的潜力尚未被充分探索。
method: 系统研究递归深度和层分配对ASR编码器的影响，在潜在空间应用有限次数递归。
result: "在潜在空间递归且限制循环次数时，性能与基线相当，参数量减少66%。"
conclusion: 递归Transformer是ASR的可行轻量化替代方案，特别适用于潜在空间递归。
---

## 摘要
基于Transformer的架构在自动语音识别（ASR）中带来了显著改进，但往往以模型规模大幅增加为代价。解决这个问题的一个有前景的方法是通过深度递归（通常称为递归Transformer）实现层共享，即重复应用模型中的相同层。尽管该方法在其他领域显示出潜力，但在ASR中仍未得到充分探索。本文中，我们对应用于ASR编码器架构的递归Transformer进行了实验研究。我们系统地研究了递归深度和递归Transformer中层分配的影响。实验结果表明，递归Transformer是一个可行的替代方案，特别是当递归被应用于潜在空间且循环次数受限时，可以在参数数量减少66%的情况下获得相当的性能。

## Abstract
Transformer-based architectures have led to significant improvements in Automatic Speech Recognition (ASR), often at the cost of substantially increased model sizes. A promising approach to address this issue is layer sharing through depth recursion, commonly referred to as the Recursive-Transformer, which involves repeatedly applying the same layers within the model. Despite its potential shown in other fields, this technique remains relatively unexplored in ASR. In this paper, we present an experimental study of the Recursive-Transformer applied to ASR encoder architectures. We systematically investigate the impact of recursion depth and layer allocation within the Recursive-based Transformer. Our results demonstrate that the Recursive-Transformer is a viable alternative, especially when recurrence is applied in the latent space with a restricted number of loops, obtaining comparable performance while reducing the parameter count by 66%.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究动机**：当前基于 Transformer 的自动语音识别（ASR）系统性能越强，模型参数规模越大，给端侧部署和计算资源带来极大压力。传统压缩方法（蒸馏、剪枝、量化）通常依赖大预训练模型，且会降低计算预算，不利于高精度场景。
- **核心问题**：能否在不显著损失识别精度的前提下，通过参数共享（即递归 Transformer）大幅减少 ASR 编码器的参数量？该方法在自然语言处理领域已显露潜力，但在 ASR 中缺乏系统研究。
- **整体含义**：论文旨在验证递归 Transformer 作为 ASR 编码器轻量化方案的可行性，重点探索递归深度与层分配对性能的影响，寻找参数效率与识别精度的最优平衡。

### 2. 方法论：核心思想与关键技术细节
- **核心思想**：利用深度递归（层共享）实现“计算深度”与“参数数量”解耦。同一组层被循环执行多次，从而用少量参数达到较深的计算图。
- **关键技术——潜在递归 Transformer（Latent‑Recursive‑Transformer）**：
  - 将编码器划分为三个功能块：
    - **前奏块（Prelude, \(P\)）**：将输入编码为潜在表示。
    - **递归块（Recurrent, \(R\)）**：共享参数，对该潜在表示进行 \(L\) 次循环迭代精炼。
    - **尾声块（Coda, \(C\)）**：将精炼后的潜在状态解码为最终输出。
  - 形式化流程：
    \[
    y_0 = P(x),\quad y_i = R(y_{i-1})\; (i=1,\dots,L),\quad y_{\text{final}} = C(y_L)
    \]
  - 若 \(n_p=n_c=0\)，退化为普通递归 Transformer；\(L=1\) 则对应无递归的标准模型。
- **层分配与递归深度**：系统研究不同 \((n_r, L, n_p, n_c)\) 组合，固定总计算深度为 24 层（即 \(n_p + L\times n_r + n_c = 24\)），对比不同递归配置的效果。
- **动态循环训练策略**：
  - 长度策略：按语句时长动态分配 \(L\)。
  - 调度策略：训练过程中逐步增加 \(L\)，最终以固定大 \(L\) 进行推理。

### 3. 实验设计
- **数据集**：
  - **主实验**：LibriSpeech（960h 英文训练集，test‑clean/test‑other 各约 5h）。
  - **跨语言泛化**：AISHELL‑1（150h 中文普通话，test 约 5h）。
- **基准与对比方法**：
  - 基线：24 层标准 Transformer 编码器（无层共享，75.6M 参数）。
  - 多种递归 Transformer 配置（R1–R4，参数逐级减少）。
  - 潜在递归 Transformer 配置（L1–L5，引入前奏/尾声非共享层）。
  - 还验证了 Branchformer 架构上的适用性。
- **评估指标**：LibriSpeech 用词错误率（WER），AISHELL‑1 用字错误率（CER）。
- **固定控制**：解码器统一使用 24 层 Transformer，仅编码器调整；循环次数 \(L\) 固定，排除自适应提前停止的影响。

### 4. 资源与算力
- 文中**未明确说明**使用的 GPU 型号、数量或具体训练时长。
- 仅提及基于 SpeechBrain 工具包实现，所有配置训练 60 个 epoch，学习率为 \(8\times10^{-3}\)，采用 CTC 和 Seq2Seq 联合损失（权重 0.3 与 0.7）。
- 语言模型为 Transformer LM，基于 LibriSpeech 转录文本 1000 万词训练。

### 5. 实验数量与充分性
- **实验组数丰富**：
  - 多种递归配置（R1–R4）与潜在递归配置（L1–L3）对比。
  - 细粒度消融：在 L1 基础上变化 \(L\) 从 1 到 20（共 5 个以上设定），分析递归深度影响。
  - 两种动态 L 训练策略（长度策略、调度策略）测试。
  - 跨数据集验证（LibriSpeech → AISHELL‑1）和跨架构验证（Transformer → Branchformer）。
- **实验客观性与公平性**：
  - 所有配置保持总计算层数一致（24），参数匹配设置下对比标准 Transformer。
  - 解码器固定不变，仅编码器变量，控制严谨。
- **总体评估**：消融实验和泛化实验均较充分，能够支撑主要结论。不过，缺少与其它压缩方法（如剪枝、蒸馏）的直接性能对比。

### 6. 主要结论与发现
- 递归 Transformer 能以极大参数削减（最高 95%）维持竞争性 WER，但极端递归（单层循环 24 次）会损伤表示能力。
- 潜在递归 Transformer 通过引入非共享的前奏/尾声层，可一致地提升性能；在参数匹配条件下，L3 甚至超越标准 Transformer（test‑clean 2.03% vs 2.12%）。
- 递归深度是关键：过少（L=1,2）性能不足，过多（L≥10）出现退化，存在一个最优区间（如 L=5）。
- 递归带来的增益主要集中在困难样本（长句、高基错误率片段），对简单样本改善有限。
- 调度策略（逐步增加 L）能取得最佳 test‑clean 结果，证明动态训练策略有效。
- 方法在中文 AISHELL‑1 和 Branchformer 上均能保持相当性能且减半参数，泛化性良好。

### 7. 优点
- **首次**分析大规模 ASR 编码器内部的层相似性，揭示中间层冗余，为递归设计提供实证依据。
- 提出潜在递归 Transformer 并应用于 ASR，结构清晰（Prelude‑Recurrent‑Coda），可解释性强。
- 系统探索递归深度、层分配等关键超参，填补该领域在 ASR 的研究空白。
- 在参数减量 66% 的同时保持甚至超越基线性能，实用价值高。
- 跨语言、跨架构验证，证明方法的普适性。

### 8. 不足与局限
- 未探索自适应提前停止或动态深度推理，限制了实际推理效率的讨论（计算量未减少）。
- 仅聚焦编码器，解码器仍为全量参数，整体系统参数量削减比例会降低。
- 缺乏与其他压缩方案（如剪枝、蒸馏、量化）的直接对比，难以判断在相同参数预算下的相对优劣。
- 训练资源与推理延迟数据缺失，影响工程可行性的判断。
- 最大递归深度下性能下降的原因未深入分析（如训练难度、泛化能力等）。
- 仅在 LibriSpeech 和 AISHELL‑1 两个较干净的语料库上测试，噪声场景、多语种、流式 ASR 等场景未覆盖。

（完）
