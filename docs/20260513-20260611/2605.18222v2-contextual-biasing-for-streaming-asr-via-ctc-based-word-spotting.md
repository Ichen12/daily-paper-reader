---
title: Contextual Biasing for Streaming ASR via CTC-based Word Spotting
title_zh: 基于CTC词检测的流式语音识别上下文偏置
authors: "Kai-Chen Tsai, Tien-Hong Lo, Yun-Ting Sun, Berlin Chen"
date: 2026-05-18
pdf: "https://arxiv.org/pdf/2605.18222v2"
tags: ["query:speech-audio"]
score: 9.0
evidence: 用于ASR上下文偏置的流式CTC词发现
tldr: 流式自动语音识别中，实现精准的上下文偏置以识别罕见词和领域词是一大挑战。现有基于CTC的词检测方法虽性能优越，但仅限于离线处理且需完整语音。本文提出流式CTC词检测方法，通过有状态令牌传递算法在连续音频块中维护关键词路径，并引入增量提交机制，仅输出不受未来音频影响的可靠片段，从而兼顾低延迟与稳定性。实验表明，该方法有效降低了整体词错误率并提升了关键词F-score，且无需修改声学模型，可无缝集成至流式ASR管线，具备实际部署价值。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.18222v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 918, \"height\": 409, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.18222v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 914, \"height\": 530, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.18222v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 799, \"height\": 1129, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.18222v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 838, \"height\": 314, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.18222v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1679, \"height\": 446, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.18222v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 920, \"height\": 287, \"label\": \"Table\"}]"
motivation: 现有CTC词检测只能离线处理且需完整语音，无法满足流式ASR实时上下文偏置需求。
method: 通过有状态令牌传递维护跨块关键词路径，并引入增量提交机制只提交稳定片段，确保低延迟。
result: 在流式ASR上，整体词错误率降低，关键词F-score有效提升。
conclusion: 该方法无需额外训练或修改声学模型，能与流式ASR无缝集成，适合实际部署。
---

## 摘要
上下文偏置对于提升自动语音识别系统对罕见和特定领域词汇的识别能力至关重要。近年来虽已涌现众多方法，但多数聚焦于离线场景，并未明确应对流式语音识别的挑战。例如，基于CTC的词检测方法直接利用CTC对数概率检测关键词，表现出色，但仅限于离线处理且需获取完整语音。本文提出流式CTC词检测的扩展方案，用于实时上下文偏置。我们的方法通过有状态令牌传递算法在音频块间维持活跃关键词路径，从而检测跨越多个块的关键词。为降低延迟并确保稳定输出，我们引入增量提交机制，仅放出确定不受未来音频影响的片段，同时推迟不确定区域。该方法自然集成到流式语音识别流水线中，无需修改底层声学模型或额外训练，便于实际部署。实验结果表明，所提方法降低了整体词错误率，并有效提升了关键词F值，验证了其在实时语音识别应用中的有效性。

## Abstract
Contextual biasing is essential to improving the recognition of rare and domain-specific words in an automatic speech recognition (ASR) system. While numerous methods have been proposed in recent years, most of them focus on offline settings and do not explicitly address the challenges of streaming ASR. For example, CTC-based word spotting (CTC-WS) have demonstrated strong performance by directly detecting keywords from CTC log-probabilities, but they are limited to offline processing and require access to the full utterance. In This work, we present a streaming extension of CTC-WS for real-time contextual biasing. Our method maintains active keyword paths across audio chunks using a stateful token passing algorithm, enabling the detection of keywords that span multiple chunks. To ensure low latency and stable output, we introduce an incremental commitment mechanism that only emits segments guaranteed not to be affected by future audio, while deferring uncertain regions. This method naturally integrates with streaming ASR pipelines and does not require modifications to the underlying acoustic model or additional training, making it practical for real-world deployment. Experimental results show that our method reduces overall WER and effectively improves keyword F-score, demonstrating its effectiveness for real-time ASR applications.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究动机**：流式自动语音识别（Streaming ASR）在实时场景（如直播字幕、语音助手）中要求低延迟、按块处理音频，但现有上下文偏置（Contextual Biasing）方法多针对离线设定，难以保证流式场景下罕见词、领域专有名词（人名、地名等）的识别准确率。
- **现存问题**：基于 CTC 的词检测（CTC-WS）可直接从 CTC 对数概率中检测关键词，性能强但需完整语音，无法直接用于流式处理；其他偏置方法（如浅层融合、深层融合）常需修改模型架构或增加延迟，灵活性不足。
- **论文目标**：提出一种无需重训练、不改动声学模型的流式 CTC-WS 方法，在维持低延迟的同时有效提升流式 ASR 的关键词识别率。

### 2. 方法论
- **核心思想**：将离线 CTC-WS 的令牌传递（Token Passing）算法改造为有状态（Stateful）版本，使其能在音频块（Chunk）之间继承活跃关键词路径；并通过“提交前沿”（Commit Frontier）机制实现稳定片段的增量输出。
- **关键技术细节**：
  - **有状态跨块词检测**：每个音频块处理时，继承前一块未完成的关键词路径（活跃令牌），与新初始化令牌一同扩展。当令牌到达偏置词结束时，记录为候选词；若未结束则保留并传递到下一块。
  - **增量提交机制**：定义“提交前沿”为所有仍活跃令牌的最早起始帧，位于该前沿之前的帧不再受未来块影响，其内的候选词可安全输出；前沿之后的区域作为“保持区”暂不提交。若无活跃令牌，前沿直接移至当前帧末尾。
  - **在线关键词替换**：在可提交区域内，对检测到的候选词与贪婪解码结果进行重叠度对比（重叠占比 > 50% 时替换），遵循离线 CTC-WS 的去重叠原则，只保留得分最高的候选词。
- **流程图示**：论文中图1展示整体流水线，图2说明提交与保持机制。
- **无需额外训练**：直接利用流式 ASR 模型输出的 CTC logits，不修改声学模型或解码器，便于集成。

### 3. 实验设计
- **数据集**：
  - **STOP1**：从 STOP 数据集筛选仅含人名的查询，共 3413 句，偏置列表大小 687，时长 3.15 小时。
  - **STOP2**：同样方式构建，但筛选地点名实体，共 7384 句，偏置列表大小 1107，时长 7.21 小时。
- **流式 ASR 模型**：使用 NVIDIA 公开的流式 FastConformer 混合 CTC/RNN-T 模型（约 114M 参数，在 2 万小时英语数据上训练），支持缓存感知的流式推理。
- **对比方法**：
  - **无偏置基线**：纯 CTC 或 RNN-T 贪婪解码，不做任何偏置。
  - **GPU-PB**：基于 GPU 加速的短语增强树，在贪婪解码中注入偏置分数，属于浅层融合的流式变体。
  - **本文提出的 Streaming CTC-WS**。
- **评估指标**：
  - 整体词错误率（WER）。
  - 偏置词 F-score（综合精确率与召回率）。
  - 每块的处理耗时（平均和 P95）。
- **实验变量**：测试了不同 chunk 大小（160 ms、560 ms、1120 ms），分析对性能与延迟的影响。

### 4. 资源与算力
- **推理硬件**：所有评估在 Intel Core i7-8700 + NVIDIA GeForce RTX 2080 Ti 上进行。
- **训练算力**：论文未提供模型训练所用算力、GPU 数量及训练时长等细节；仅使用现成的预训练流式模型，评估重点在推理效率与性能。
- **额外开销**：Streaming CTC-WS 在三种 chunk 大小下，额外处理时间占 chunk 时长的比例平均为 3.2%–4.1%，P95 下 < 9%，表明推理开销极小。

### 5. 实验数量与充分性
- **实验组数**：文章展示了约 4 大组对比（CTC 解码下 3 个方法 × 2 数据集、RNN-T 解码下 3 方法 × 2 数据集），并对 3 种 chunk 大小（160/560/1120 ms）进行了消融实验，还单独分析了运行时，共计约 10 余组性能数据（包含表格与图表）。
- **充分性评价**：实验覆盖了两个专项实体数据集、两种主流流式解码方式（CTC/RNN-T）和多档延迟设置，对比了较新的基线方法（GPU-PB），因此能够较全面地验证方法有效性。
- **客观公平性**：所有方法在同一流式模型和相同解码设定下比较，指标（WER、F-score、延迟）公开透明；但仅在一个预训练模型上实验，且未与其他复杂偏置方法（如深层融合）对比，可能存在一定局限。

### 6. 论文的主要结论与发现
- Streaming CTC-WS 在两个数据集上均取得最低 WER 和最高 F-score，例如在 CTC 解码下 STOP1 的 WER 从 18.36% 降至 12.83%，F-score 从 66.84% 升至 89.61%。
- 相比 GPU-PB 短语增强方法，Streaming CTC-WS 能更显著提升召回率，从而获得更好的 F-score。
- 当 chunk 大小缩小时（如 160 ms），无偏置的 F-score 急剧下降，而所提方法仍能维持高 F-score，说明有状态路径管理能有效恢复被块边界切割的关键词。
- 额外计算开销不到 chunk 时长的 9%，证明其适合实时部署。

### 7. 优点
- **无需重训练或改模型**：可直接嵌入任何输出 CTC logits 的流式 ASR 系统，作为外挂偏置模块。
- **低延迟、高稳定性**：增量提交机制确保输出稳定，避免因块边界导致的召回损失。
- **轻量高效**：每块增加的耗时极少，且不依赖波束搜索，避免了传统浅层融合在流式场景下的延迟问题。
- **鲁棒性强**：在不同 chunk 大小和两种解码方式下均表现一致且显著提升。

### 8. 不足与局限
- **实验覆盖**：仅在两个特定领域（人名、地名）的自建数据集和一个预训练模型（NVIDIA FastConformer）上验证，未在更多样化的领域、多语种或大规模实际商用流式系统上测试，泛化性有待证明。
- **对比基线**：主要比较了无偏置和 GPU-PB（浅层融合），未与需要额外训练的深层融合方法、树结构偏置适配器等方法进行全面对比，优势可能被低估或高估。
- **提交机制依赖阈值**：关键词替换基于帧重叠 50% 的硬阈值，该阈值对不同场景可能需要调整，但论文未进行敏感性分析。
- **仅处理固定偏置列表**：论文未讨论动态更新的偏置列表对算法效率的影响，实际应用中列表可能频繁变化。
- **空白符号与自定义权重**：文章未详细描述扩展令牌时的偏置权重设定（如上下文偏置强度的选择），可能影响实际部署的调优。

（完）
