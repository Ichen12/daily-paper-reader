---
title: Phoneme-First Prediction for LLM-Based Speech Recognition
title_zh: 面向基于大语言模型的语音识别的音素优先预测
authors: "Jakob Poncelet, Hugo Van hamme"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.10864v1"
tags: ["query:speech-audio"]
score: 9.0
evidence: 面向LLM语音识别的音素优先预测
tldr: 语音增强大语言模型（LLM）面临声学特征与语义嵌入对齐困难，易导致识别错误。本文提出在LLM中插入音素预测中间步骤，使模型先学习精细发音知识，再生成最终文本，从而减少声学混淆。实验表明，该方法能有效提升语音识别准确率，尤其在低资源场景下表现突出，且输出更忠实于原始语音声学。该方案成本低廉、实现简单，为提升语音识别可解释性和鲁棒性提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10864v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1875, \"height\": 240, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10864v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 835, \"height\": 460, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.10864v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 925, \"height\": 287, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10864v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 923, \"height\": 474, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10864v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 800, \"height\": 522, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10864v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 905, \"height\": 497, \"label\": \"Table\"}]"
motivation: 对齐语音编码器的声学表征与LLM的语义嵌入是核心挑战，直接产生文本易因声学混淆出错。
method: 在LLM解码过程中强制先预测音素序列，音素目标从已有文本转录自动推导，不增加额外标注成本。
result: 在多个实验中，中间音素预测显著提高识别准确率，特别在低资源条件下，输出文本与语音的声学匹配度更高。
conclusion: 通过音素优先预测，简单廉价地增强了语音LLM的识别性能与声学忠实度，为可解释语音识别提供有效路径。
---

## 摘要
近期研究探索了将大语言模型与语音编码器集成，构建具备上下文语音识别能力的语音增强大语言模型。主要挑战在于对齐大语言模型的语义嵌入与语音编码器的声学表示。我们提出一种新方法，教会大语言模型在生成最终文本之前先从语音特征中预测音素。通过将音素预测步骤直接融入大语言模型，模型得以发展出精细的发音知识，减少声学混淆，提升转录准确性和可解释性。我们的方法成本低且简单，因为音素目标可从现有转录文本中自动推导。全面的实验表明，中间音素预测能够改善语音识别性能，尤其在低资源环境下，并且产生的输出在声学上更忠实于原始语音。

## Abstract
Recent research has explored integrating Large Language Models (LLMs) with speech encoders to create speech-augmented LLMs capable of contextualized speech recognition. The main challenge lies in aligning the semantic embeddings of LLMs with the acoustic representations of speech encoders. We propose a novel approach that teaches the LLM to first predict phonemes from the speech features before generating the final transcript. By integrating a phoneme prediction step directly into the LLM, the model develops a fine-grained knowledge of pronunciation, reducing acoustic confusion and improving transcription accuracy and explainability. Our method is cheap and simple, as phoneme targets can be automatically derived from existing transcripts. Through comprehensive experiments, we show that intermediate phoneme prediction can improve speech recognition, particularly in low-resource settings, and yields outputs that are acoustically more faithful to the speech.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大语言模型（LLM）的语义嵌入（如“bank – money – economy”）与语音编码器的声学表示（如“bank – rank – dank”）之间存在固有差异，直接进行语音到文本的映射容易导致声学混淆，同音异形词（cite/sight）和异读词（cough/though）的识别尤为困难。
- **整体含义**：作者提出在 LLM 解码过程中强制插入“音素优先预测”中间步骤，使模型先学会精细的发音知识，再生成最终文本。该方法旨在缓解声学‑语义鸿沟，并增强语音识别的准确性与可解释性，尤其在标注数据稀缺时优势明显。

## 2. 论文提出的方法论

- **核心思想**：将标准“语音转文本”（S2T）提示改为“先转写音素，再转写文本”的提示，训练 LLM 先输出音素序列，再输出对应词汇。
- **关键技术细节**：
  - **提示格式**：  
    输入：`<speech> Transcribe the previous speech first to phonemes and then to text.`  
    预期输出：`Phonemic transcription: HH AH0 L OW1 G AY1 Z.\nTranscription: Hello guys.`
  - **自动音素目标生成**：利用现有 transcripts 通过强制对齐（forced alignment）和发音词典自动获得音素标签，无需额外人工标注。
  - **联合训练（Joint Training）**：为防止模型过度依赖教师强制（teacher forcing）中的音素信息而忽视语音特征，训练时随机使用标准 S2T 提示与音素优先提示，使模型在两种模式下均能学习。
  - **模型架构**：
    - 语音编码器（Whisper / HuBERT / Conformer）冻结，特征经堆叠和 MLP 投影层映射到 LLM 嵌入空间。
    - LLM 使用 4‑bit QLoRA（低秩适配）高效微调，仅更新投影层和 LoRA 权重。
    - 训练目标：下一个 token 的交叉熵损失。
  - **推理**：可以用 S2T 或 PF‑S2T 提示解码，采用 beam search（束宽 10），并校验输出格式有效性，防止幻觉。

## 3. 实验设计

- **数据集与场景**：
  - LibriSpeech-100h（train-clean-100）和 LibriSpeech-960h（全量）。
  - TED‑LIUM-100h（自发演讲）。
  - Spoken Dutch Corpus（240 小时荷兰语，含手动/自动音素标签）。
- **基准与对比方法**：
  - **基线**：零样本 Whisper（medium/large）、HuBERT‑large CTC 全微调。
  - **标准 S2T**：仅使用“Transcribe the previous speech to text.”提示训练与解码。
  - **PF‑S2T**：仅使用音素优先提示训练和解码。
  - **Joint**：联合训练，解码时使用 S2T 或 PF‑S2T 提示。
- **评价指标**：词错率（WER），音素错误率（PER），以及基于 G2P 的 PER 分析。

## 4. 资源与算力

- **GPU**：单张 NVIDIA RTX 3090（24 GB 显存）。
- **训练细节**：
  - LLM 使用 4‑bit 量化及 QLoRA（rank=4~32, alpha=16~64），约 10.7M‑52M 可训练参数。
  - 优化器：8‑bit Adam，梯度检查点，Flash Attention。
  - 有效批量大小 128 或 256 句（约 30 分钟音频），学习率 5e‑5，余弦衰减，10% 预热。
- **训练时长**：文中未给出具体训练时间，但计算配置表明单卡即可完成训练，成本低廉。

## 5. 实验数量与充分性

- **实验组数**：
  - **表 I**：LibriSpeech‑100h 与 TED‑LIUM‑100h 上 S2T、PF‑S2T、Joint 的解码对比（8 项结果）。
  - **表 II**：LibriSpeech‑960h 上 Whisper‑large 和 HuBERT‑large 编码器，不同训练/解码方案（10 项结果）。
  - **表 III**：荷兰语数据集上不同音素标签质量（手动、自动、组合）的 WER/PER 对比（12 项）。
  - **表 IV 和图 2**：错误样例分析与基于 G2P 的声学对齐对比（PER 下降约 50%）。
- **充分性评价**：覆盖多语种、多数据量、多编码器类型（监督、自监督、CTC‑AED）及音素质量消融，实验对比公平，结果一致性强；但未见对更大规模 LLM 或纯文本 G2P 辅助预训练的探索。

## 6. 论文的主要结论与发现

- 联合音素优先训练在几乎所有测试集上均降低了 WER，尤其在低资源场景下提升显著（TED‑LIUM‑100h 上相对提升达 25%）。
- 模型输出的转录在声学上更忠实于原始语音：G2P 计算的 PER 大幅下降（dev‑clean 上 PER 几乎减半），错误更倾向于音似词而非语义混淆。
- 音素优先解码提供了可解释性（展示识别出的音素），有助于错误分析和发音评估。
- 方法对音素标签质量有一定鲁棒性，自动标签即可带来明显收益，手动标签进一步提升效果。

## 7. 优点

- **方法简洁廉价**：无需额外人工标注，音素目标可全自动生成；训练只需单张消费级 GPU。
- **跨编码器与跨语种通用性**：在监督（Whisper）、自监督（HuBERT）、CTC‑AED 等不同编码器上均有效，适用英语和荷兰语。
- **声学对齐显著改善**：通过模型内部音素预测，减少了同音词误识，提升了转录的声学保真度。
- **可解释性增强**：输出的音素序列为用户和下游任务提供了发音层面的诊断信息。

## 8. 不足与局限

- **推理速度降低**：输出序列变长，导致解码时间增加。
- **依赖对齐质量**：音素标签来自强制对齐，若对齐不准会影响效果；虽自动标签可用，但最优性能仍依赖高质量标注。
- **增益边际递减**：大数据量（960h）下 WER 改善幅度小于低资源场景，且专用 ASR 模型（如全微调 HuBERT）仍可能保持更低 WER。
- **LLM 适配代价**：需要对 LLM 进行低秩微调，虽开销不大但仍有训练步骤；纯文本 G2P 预训练潜力未充分挖掘。
- **实验覆盖有限**：仅测试英语和荷兰语，未在更多语言或嘈杂/多语混杂场景验证；未与更大参数量的 LLM 或全精度微调比较。
- **过拟合风险**：在 PF‑S2T 单独解码时，模型可能过度依赖预测出的音素生成文本，导致联合训练成为必要。

（完）
