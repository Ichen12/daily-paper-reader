---
title: "FlashTTS: Fast Streaming TTS with MTP Acceleration and X-pred Mean Flow Distillation"
title_zh: FlashTTS：基于MTP加速与X-pred均值流蒸馏的快速流式TTS
authors: "Hanke Xie, Xiaming Ren, Dake Guo, Ruonan You, Wenhao Li, Jingbin Hu, Guobin Ma, Huakang Chen, Kejie Xu, Rui Huang, Weiguo Tan, Xianrong Wang, Lei Xi"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.09141v1"
tags: ["query:speech-audio"]
score: 10.0
evidence: 提出低延迟的快速流式TTS，直接推进文本到语音合成
tldr: 现有语音对话系统要求TTS模型具备低延迟和流式处理能力，但主流单码本LLM方法依赖多阶段流水线，缺乏原生流式支持，延迟高。FlashTTS通过滞后的多轨架构原生处理流式文本和语音，结合并行多令牌预测与X-pred均值流匹配解码器，仅需两次函数评估即可实现高保真声学生成。实验表明，FlashTTS将首包延迟降至325毫秒，同时保持强大的零样本声音克隆和跨语种可懂度，为实时对话系统奠定实用基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.09141v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 834, \"height\": 463, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09141v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 746, \"height\": 368, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.09141v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1492, \"height\": 367, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09141v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1489, \"height\": 357, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09141v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 744, \"height\": 479, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09141v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 839, \"height\": 268, \"label\": \"Table\"}]"
motivation: 现代语音对话系统亟需低延迟、支持流式输入输出的TTS模型，但现有单码本LLM方法通常采用非流式多阶段管线，端到端延迟高。
method: 提出滞后多轨架构原生化流式处理，集成并行多令牌预测（MTP）与X-pred均值流匹配解码器，实现两函数评估的快速令牌到梅尔生成。
result: FlashTTS将首包延迟显著降至325毫秒，同时保持高质量零样本声音克隆和跨语种清晰度，优于鲁棒流式基线。
conclusion: FlashTTS通过联合优化输入处理和解码效率，提供了一个开源的低延迟流式TTS框架，可满足实时对话系统需求。
---

## 摘要
语音对话系统的最新进展要求文本到语音（TTS）模型更快、响应更迅速。现代语音对话系统对TTS模型提出两个主要要求：低延迟和对流式输入与输出的支持。然而，大多数现有的基于单码本LLM的TTS方法依赖于缺乏原生流式能力的多阶段流水线。由于自回归预测缓慢和多步流匹配，这些系统通常端到端延迟较高。为解决这些限制，我们提出FlashTTS，一个开源的低延迟流式TTS框架。FlashTTS引入了一种滞后多轨架构，原生处理流式文本和语音输入，从而消除了句子级缓冲的需要。为加速声学生成，我们将并行多令牌预测（MTP）与X-pred均值流匹配解码器相结合。这种配置在恰好两次函数评估（2-NFE）下实现了高保真令牌到梅尔频谱的生成。通过联合优化输入处理和解码效率，FlashTTS为实时语音对话系统提供了实用基础。实验表明，与强大的流式基线相比，FlashTTS大幅将首包延迟降低至325毫秒，同时保持了强大的零样本语音克隆和跨语言可懂度。语音样本可用。模型代码和检查点将作为开源发布。

## Abstract
Recent progress in speech dialogue systems requires Text-to-Speech (TTS) models to be faster and more responsive. Modern speech dialogue systems impose two primary requirements on TTS models: low latency and support for streaming inputs and outputs. However, most existing single-codebook LLM-based TTS methods rely on multi-stage pipelines that lack native streaming capabilities. These systems typically suffer from high end-to-end latency due to slow autoregressive prediction and multi-step flow matching. To address these limitations, we propose FlashTTS, an open-source and low-latency streaming TTS framework. FlashTTS introduces a lagged multi-track architecture that natively processes streaming text and speech inputs, thereby eliminating the need for sentence-level buffering. To accelerate acoustic generation, we integrate parallel Multi-Token Prediction (MTP) with an X-pred mean flow matching decoder. This configuration achieves high-fidelity token-to-mel generation in exactly two function evaluations (2-NFE). By jointly optimizing input processing and decoding efficiency, FlashTTS offers a practical foundation for real-time speech dialogue systems. Experiments show that FlashTTS substantially reduces First-Packet Latency to 325ms compared to robust streaming baselines, all while preserving strong zero-shot voice cloning and cross-lingual intelligibility. Speech samples are available. The model code and checkpoints will be released as open source.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究背景**：语音对话系统（如智能助手、实时交互）对TTS提出了极低延迟和原生流式处理的双重要求。
- **现存痛点**：当前主流的单码本LLM-based TTS（如VALL-E等）多采用“自回归生成+多步流匹配”的级联架构，存在三大瓶颈：
  - 缺乏原生流式输入能力，通常需缓冲完整句子才能开始合成。
  - 自回归token逐帧预测速度慢。
  - 流匹配解码需10步以上函数评估（NFE），端到端延迟高。
- **核心问题**：如何在保持高音质与零样本语音克隆能力的前提下，构建一个能真正支持流式输入/输出、且解码极快的低延迟TTS框架。
- **整体含义**：FlashTTS通过“多轨流式架构+并行多令牌预测（MTP）+两步均值流解码”的联合设计，首次在开源框架中将首包延迟压至325ms，并保持可比的跨语种清晰度与说话人相似度，为实时对话系统提供了可行基础。

### 2. 论文提出的方法论
- **整体框架**：基于Qwen2-0.5B的解码器模型，分两阶段训练。
  - **阶段一**：训练骨干语言模型，输入采用**滞后多轨堆叠结构**（如图2），包含语音轨、文本轨和语种轨，各轨道并行送入Transformer，消除句子级缓冲，实现逐token流式生成。
  - **阶段二**：冻结骨干，引入**并行MTP**（Multi-Token Prediction）加速自回归解码，同时在声学端通过**X-pred均值流蒸馏**（Mean Flow Distillation）实现2步极速mel生成。

- **关键技术细节**：
  - **流式序列方案**：文本与语音token以堆叠方式组织，文本轨用填充token保持对齐，语种轨全程提供语言条件，使得模型见到第一个文本token即可启动生成，从根本上降低首token延迟（FTL）。
  - **MTP加速**：借鉴DeepSeek-V3，在骨干最后隐藏状态后附加\(N\)个轻量MTP模块（各含线性投影+Qwen2.5解码层），并行预测未来\(k+1\)个token的分布，计算交叉熵损失。推理时用骨干模型验证这些“推测token”，既大幅提升每秒生成token数（TPS），又保持稳定性。
  - **X-pred均值流匹配**：融合Mean Flow与JIT思想，网络直接预测干净mel谱\(\hat{x}_\theta\)，再由\(\hat{x}_\theta\)解析计算平均速度\(\hat{u}_\theta\)，与蒸馏目标\(u_{tgt}\)求MSE损失。推理时仅需2次函数评估（2-NFE）：从噪声\(\epsilon\)出发，经一次预测平均速度即可恢复\(x_0\)。同时采用分块注意力（block-wise attention）支持流式输出。
  - **损失函数**：
    - MTP损失：\(L_{MTP} = \sum_{k=1}^{N-1} L_{CE}(S_{0:T-k-1}^k, G_{k+1:T})\)
    - 均值流损失：\(L_{MF} = \mathbb{E}_{t,r,x,\epsilon} \left[ \|\hat{u}_\theta(z_t,r,t) - \text{sg}(u_{tgt})\|^2 \right]\)，边界\(t=r\)退化为CFM损失。

### 3. 实验设计
- **数据集**：
  - **训练集**：约30万小时开源语音数据，包含Emilia、Emilia-Yodas、LibriHeavy、WenetSpeech4TTS。
  - **评估集**：Seed-TTS评估集（test-zh和test-en）以及MiniMax多语言测试集（中文、英文、日语、韩语、法语、德语）。
- **对比方法**：
  - **主基线**：CosyVoice2（0.5B参数，使用相同S3Tokenizer v2以保证公平对比）。
  - **其他参考模型**：Seed-TTS、MaskGCT、F5-TTS、Llasa-8B、Spark-TTS以及商业系统MiniMax*、ElevenLabs*。
- **评价指标**：
  - **流式效率**：首包延迟（FPL）、首token延迟（FTL）、LLM延迟、tokenizer首包解码延迟（TPP）、每秒token数（TPS）、实时因子（RTF）。
  - **音质与智能度**：词错误率（WER）、字符错误率（CER）、说话人相似度（SIM）、对比主观分数（CMOS）。
  - **加速比**：消融实验中用加速比衡量效率提升。

### 4. 资源与算力
- **训练环境**：
  - **阶段一（骨干）**：8块A100 GPU，动态帧批量大小40,000，AdamW优化器，峰值学习率1e-4，20k步热身，余弦衰减，总训练步数100万步。
  - **阶段二（MTP模块）**：4块A100 GPU，冻结骨干，峰值学习率5e-5。
  - **X-pred均值流蒸馏**：8块RTX 4090 GPU，批量大小2,000，梯度累积2，峰值学习率7e-5。
- **推理测试**：所有延迟评估在单块NVIDIA RTX 4090 GPU上进行，模拟上游Qwen2-7B模型动态生成文本流的真实对话管道，且未做任何工程优化。
- **训练时长**：论文未明确给出具体小时数或天数，仅给出了训练步数和硬件配置。

### 5. 实验数量与充分性
- **实验组统计**：
  - **主对比实验**（表1）：CosyVoice2（10-NFE）与FlashTTS的多种配置（Stage 1的2/3-NFE，MTP-3、MTP-5的2/3-NFE）在延迟、TPS、RTF、WER、SIM、CMOS上的对比，至少6组配置。
  - **多语言零样本评估**（表2）：6种语言，涵盖9个模型（含商业和开源），共计54个数据点。
  - **标准测试集评估**（表3）：test-zh和test-en上8个模型的CER/WER/SIM对比。
  - **消融实验**（表4）：移除X-pred、移除MTP、移除语种ID模块，共4组，同时报告WER、SIM和加速比。
- **充分性与公平性**：
  - 实验覆盖了流式效率、多语种泛化、零样本克隆、主观听感，维度较全。
  - 对比基线选取了同参数规模、同tokenizer方案的CosyVoice2，确保公平。
  - 消融实验清晰验证了各模块的贡献。
  - 主观测试（CMOS）邀请了30位听者，随机抽取100对样本，统计置信区间。
  - 局限性：MTP-5性能明显下降，但未深入分析原因；未与更大规模商业系统（如CosyVoice2的更大版本）进行同等效率约束下的对比；仅在单token连续预测框架内对比，未涉及多码本或延迟自回归方案。

### 6. 论文的主要结论与发现
- FlashTTS的**滞后多轨架构**能原生支持流式输入，将首token延迟（FTL）从CosyVoice2的257ms降至60ms级别。
- **MTP-3（2-NFE）配置**达到最优延迟-质量平衡：FPL仅325ms，RTF 0.632，中文WER 18.8%（略高于CosyVoice2的26.2%但更优），CMOS较基线正向提升0.05。
- 增加NFE可提升音质（3-NFE CMOS达0.12），但MTP分支过多（如5个）会损害音质（CMOS -0.08）。
- 模型在英语、韩语上WER显著低于CosyVoice2，并新增支持法语、德语，但SIM略低于离线大模型。
- 联合使用MTP和X-pred能带来约49%的推理加速，单独移除任一模组加速比大幅下降，且移除语种ID会严重损害跨语言WER。

### 7. 优点
- **架构创新**：多轨堆叠+滞后设计真正实现了“随来随生成”的流式TTS，无需句子级缓存，对齐对话场景需求。
- **极低延迟**：通过MTP并行推测解码与均值流2步生成，大幅压缩了自回归和解码两大延迟源，FPL压至325ms。
- **开源实用**：基于0.5B模型+轻量HiFi-GAN声码器，推理可在消费级GPU（RTX 4090）完成，且承诺开源代码与权重，利于社区复现。
- **训练策略合理**：分阶段冻结式训练，MTP模块轻量，蒸馏策略高效，整体资源需求可控。
- **评价全面**：结合流式效能指标和主观听感，进行了公平的同tokenizer对比和消融。

### 8. 不足与局限
- **音质损失**：为换取极低延迟，WER在部分语言上仍偏高（日语10.59%，法语8.62%），SIM弱于离线大模型（如Seed-TTS），存在速度-质量取舍。
- **MTP扩展性有限**：5分支MTP带来性能退化，论文未解释原因，也未提出缓解方案，限制了进一步加速的上限。
- **未覆盖真实对话管道总延迟**：虽然模拟了上游LLM生成文本，但未测量完整对话系统的整体响应时间（如语音活动检测、端到端传输等）。
- **数据与语言覆盖**：仅使用开源数据训练，对低资源语种或特定口音的泛化性未验证；实验语言以中、英、日、韩、法、德为主。
- **主观测试规模**：CMOS仅基于100对样本和30位听者，样本量较小，可能高估或低估实际感知差异。
- **缺乏与更先进流式TTS的直接对比**：CosyVoice2虽为基线，但其本身并非专为极低延迟设计；未与FireRedTTS-2等同样强调流式的新工作比较。

（完）
