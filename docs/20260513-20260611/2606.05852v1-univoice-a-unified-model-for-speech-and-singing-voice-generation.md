---
title: "UniVoice: A Unified Model for Speech and Singing Voice Generation"
title_zh: UniVoice：一种统一的语音与歌声生成模型
authors: "Junjie Zheng, Huixin Xue, Shihong Ren, Chaofan Ding, Hao Liu, Zihao Chen"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.05852v1"
tags: ["query:speech-audio"]
score: 9.0
evidence: 提出一个统一框架，利用因子化条件与流匹配，从符号输入生成语音和歌声。
tldr: "语音合成(TTS)依赖灵活的语言韵律，而歌声合成(SVS)需要精确的旋律与节奏控制，二者需求矛盾使得统一生成模型难以同时兼顾自然度和可控性。UniVoice提出基于条件流匹配的统一框架，通过因子化解构条件为内容、旋律、音色三个模块，并采用共享DiT骨干；歌声使用MIDI序列提供旋律约束，语音则引入可学习的空旋律令牌以释放韵律自由度，并证明其近似旋律边缘化。在30k小时语音和35k小时歌声数据训练后，UniVoice语音PER达5.26%，媲美专用TTS模型(如F5-TTS的5.21%、CosyVoice3的5.30%)；歌声PER为16.22%，大幅超越统一基线Vevo1.5的24.72%。该设计实现了首个真正统一的语音与歌声生成框架，为多模态人声合成提供了新范式。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.05852v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1297, \"height\": 770, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05852v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1306, \"height\": 1124, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05852v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1313, \"height\": 526, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.05852v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1441, \"height\": 348, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05852v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1414, \"height\": 331, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05852v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 874, \"height\": 1073, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05852v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1532, \"height\": 586, \"label\": \"Table\"}]"
motivation: 语音合成要求灵活韵律，歌声合成需要精确旋律与节奏，两者需求冲突导致统一建模困难。
method: UniVoice基于条件流匹配，将条件因子化为内容、旋律和音色，歌声用MIDI控制旋律，语音用空旋律令牌推断韵律，共享DiT骨干。
result: "在30k小时语音与35k小时歌声数据训练后，语音PER达5.26%持平专用TTS，歌声PER达16.22%显著优于统一基线Vevo1.5。"
conclusion: UniVoice通过分解条件并引入空旋律令牌，首次在不牺牲语音自然度的情况下实现统一的语音与歌唱生成，弥合了两者鸿沟。
---

## 摘要
文本到语音（TTS）和歌声合成（SVS）皆旨在从符号输入生成人类声音音频，但它们在生成过程中施加了不同的要求。语音生成依赖于灵活的、语言驱动的韵律，而歌声生成则需要明确的旋律控制和准确的节奏对齐。这种不匹配使得训练既能生成自然语音又能生成可控歌声的单一模型变得具有挑战性，因为与旋律相关的条件应该强烈约束歌声，但不应限制语音韵律。我们提出了UniVoice，一个基于条件流匹配的统一语音与歌声生成框架。UniVoice没有使用单一的未区分条件的表示，而是将条件分解为内容、旋律和音色，这些由适合模态的编码器进行编码，并由一个共享的扩散Transformer（DiT）主干网络来使用。对于歌声，旋律条件由MIDI音符序列表示；对于语音，它被一个学习得到的空旋律令牌替代，允许模型从语言和声学上下文中推断韵律。这种设计为歌声保留了明确的旋律控制，同时避免了对语音施加旋律约束的需要。我们进一步将空旋律令牌分析为条件流中旋律边缘化的近似。在30万小时语音和35万小时歌声数据上训练后，UniVoice的语音音素错误率（PER）为5.26%，与专用TTS系统如F5-TTS（5.21%）和CosyVoice3（5.30%）相当。在歌声生成方面，UniVoice的PER为16.22%，优于统一基线Vevo1.5（24.72%）。

## Abstract
Text-to-speech (TTS) and singing voice synthesis (SVS) both aim to generate human vocal audio from symbolic inputs, but they impose different requirements on the generation process. Speech generation relies on flexible, language-driven prosody, whereas singing generation requires explicit melody control and accurate rhythmic alignment. This mismatch makes it challenging to train a single model that can generate both natural speech and controllable singing, since melody-related conditions should strongly constrain singing but should not restrict speech prosody. We present UniVoice, a unified speech and singing voice generation framework based on conditional flow matching. Instead of using a single undifferentiated conditioning representation, UniVoice factorizes the condition into content, melody, and timbre, which are encoded by modality-appropriate encoders and consumed by a shared Diffusion Transformer (DiT) backbone. For singing, the melody condition is represented by MIDI note sequences; for speech, it is replaced with a learned null melody token, allowing the model to infer prosody from linguistic and acoustic context. This design preserves explicit melody control for singing while avoiding the need to impose melody constraints on speech. We further analyze the null melody token as an approximation to melody marginalization in the conditional flow. Trained on 30k hours of speech and 35k hours of singing data, UniVoice achieves a speech PER of 5.26\%, comparable to dedicated TTS systems such as F5-TTS (5.21\%) and CosyVoice3 (5.30\%). On singing generation, UniVoice achieves a PER of 16.22\%, outperforming the unified baseline Vevo1.5 (24.72\%).

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义  
语音合成（TTS）需要灵活的语言驱动韵律，而歌声合成（SVS）要求精确的旋律控制与节奏对齐。直接混合训练一个统一模型时，旋律相关条件会在两种模态上产生矛盾的要求：歌声须强约束，语音则应自由。这导致梯度冲突和生成质量下降。  
UniVoice 旨在构建一个真正统一的语音与歌声生成框架，以一个共享的生成模型完成两种任务，并通过因子化条件化解上述冲突，使语音自然度不因旋律控制而受损。

## 2. 方法论  

### 2.1 条件流匹配基础  
模型基于条件流匹配（CFM），在最优传输（OT）路径上学习从噪声到音频隐变量的连续速度场：  
ϕₜ(𝑥₀, 𝑥₁) = (1−𝑡)𝑥₀ + 𝑡𝑥₁，训练目标为  
ℒ_CFM = 𝔼‖𝑣ₜ(𝑥ₜ,𝑐) − (𝑥₁−𝑥₀)‖²。  
推断时使用 Euler ODE 求解器（32 步），避免噪声调度与码本量化。

### 2.2 因子化条件与空旋律令牌  
将条件分解为四个独立轴：内容 c_𝒄𝒏𝒕（IPA 音素，ConvNeXt 编码）、旋律 c_𝒎𝒆𝒍、音色 c_𝒕𝒎𝒃（VAE 隐变量，由参考音频提供）、任务令牌 c_𝒕𝒔𝒌（speech/singing）。  
- 歌声：旋律由 MIDI 音符输入 Conformer 编码。  
- 语音：旋律条件替换为一个**可学习的空令牌** e_∅，广播到全时序。该令牌仅接收语音损失梯度，独立优化，从理论上近似旋律边缘化后的最优速度场（命题 2）。  
因子化可消除旋律相关参数的负梯度相关性（命题 1），而空令牌结合任务令牌（通过 AdaLN 调制）实现模态自适应。

### 2.3 网络架构  
- 骨干：24 层 DiT（Wan‑DiT 风格），隐藏维 1024，16 头注意力，参数量约 0.3B。  
- 融合：噪声隐变量与四个条件向量在通道维拼接后线形投影，使用 FlashAttention‑2、RoPE、AdaLN（由时间嵌入与任务令牌共同调制）。  
- 训练策略：随机丢弃文本、旋律、音频提示条件（概率 0.1），以支持轴选择式无分类器引导。语音与歌声按自然混合比例批次训练。优化器 AdamW，学习率峰值 7.5×10⁻⁵，2k 步预热后余弦衰减。

## 3. 实验设计  

### 3.1 数据集与基准  
- 训练数据：约 30k 小时语音（Emilia，中英各半） + 35k 小时歌声（网络歌曲 + 专业干声，经分离、对齐、MIDI 提取）。  
- 评测基准：  
  - 语音沿用 Emilia 测试集（跨句生成协议）。  
  - 歌声提出 **UniSinging‑Eval**：60 首歌曲，12 种音乐风格（Pop、Rap、Jazz 等），900 个片段共 2 小时；分三级难度（Minor 原词、Major 重写词、Elastic 可变词数），带对齐 MIDI 与 IPA 歌词，全面评测风格适应性、韵律与歌词对齐。

### 3.2 对比方法与指标  
- 对比系统：F5‑TTS（0.3B，语音专用）、CosyVoice3（0.5B，语音专用）、Vevo1.5（1B，统一模型）、Soul‑X‑Singer（0.7B，歌声专用）。  
- 指标：音素错误率 PER（FireRedASR），说话人相似度 SIM（WavLM），主观 S‑MOS（相似度）、N‑MOS（自然度），各 20 名评分者。

## 4. 资源与算力  
训练配置：8 块 A800 80GB GPU，每 GPU 批次帧数 32,000，总训练轮数 100 epoch。推理采用 32 步 Euler ODE。论文未明确给出总训练时长（如 GPU 天），但提供了完整的训练超参数与模型规模。

## 5. 实验数量与充分性  
实验覆盖主结果对比（表 1，4 个基线），消融实验（表 2，4 种配置：去除因子化条件、去掉任务令牌、空令牌换固定零、移除旋律编码器）。另提供代表分析、空令牌训练动态的理论证明，以及新基准的多风格多难度设计。实验在统一训练公式下公平比较了各变体，基本充分验证了核心模块的必要性。但未在更多语言或极大规模下进行扩展分析。

## 6. 主要结论与发现  
- UniVoice 在语音 PER 5.26% 与专用 TTS 媲美（F5‑TTS 5.21%），歌声 PER 16.22% 大幅优于统一基线 Vevo1.5（24.72%）和专用歌声模型 Soul‑X‑Singer（26.22%）。  
- 因子化条件与空旋律令牌是统一生成质量的关键；任务令牌的 AdaLN 调制有效缓解模态干扰；旋律编码器仅影响歌声质量，不影响语音。  
- 理论证明空令牌可逼近旋律边缘化速度场，为语音的韵律自由提供形式化解释。

## 7. 优点  
- **统一架构**：一个 0.3B 模型，无需任务专用头或分开的生成器，参数效率高。  
- **因子化条件**：优雅解决模态冲突，使语音不受旋律约束、歌声可精确控制。  
- **空旋律令牌**：将缺失条件处理为可学习的“无信息”表示，相比固定零向量更优，且有理论支撑。  
- **新基准**：UniSinging‑Eval 覆盖多风格、多难度，填补了统一歌声评估的空白。  
- **强基线对比**：在与更大模型、更多数据的对比中展现优势。

## 8. 不足与局限  
- **说话人相似度差距**：SIM 低于专用 TTS（如 67.42 vs 72.73），联合训练对音色克隆有一定折衷。  
- **语言覆盖**：仅中英双语，多语言与低资源场景未验证。  
- **混合风格缺失**：未显式建模说唱、诵唱等边界风格。  
- **推理延迟**：32 步 ODE 采样慢于少步生成方法，实时应用受限。  
- **训练算力**：虽给出配置，但未提供总训练时长，难以直接评估训练成本。  
- **理论分析依赖简化的独立性假设**（如旋律与语音条件独立），实际可能不完全成立。

（完）
