---
title: "From A to B to A: Palindromic Zero-Shot Voice Conversion with Non-Parallel Data"
title_zh: 从 A 到 B 再到 A：基于非平行数据的回文零样本语音转换
authors: "Moshe Mandel, Shlomo E. Chazan"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.08843v1"
tags: ["query:speech-audio"]
score: 10.0
evidence: 提出使用WavLM表示的KNN检索实现零样本语音转换
tldr: 监督语音转换通常需要昂贵平行语料，阻碍零样本跨语言泛化。在零样本场景下，非平行数据对齐与说话人身份保持是关键挑战。本文提出基于WavLM与KNN检索的语音对齐框架，通过合成到真实训练范式，无需平行数据即实现零样本语音转换，并引入说话人损失保持身份一致。跨语言实验表明，方法在自然度与相似度上超越现有基线，且仅用英语训练即可泛化多语言，为低资源、多语言语音转换提供了实用高效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08843v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 793, \"height\": 729, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08843v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1709, \"height\": 547, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.08843v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 690, \"height\": 355, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08843v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1767, \"height\": 787, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08843v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 838, \"height\": 343, \"label\": \"Table\"}]"
motivation: 针对语音转换对平行数据的依赖导致跨语言泛化困难，以及零样本场景下说话人身份一致性挑战。
method: 利用WavLM特征和KNN检索对齐非平行语音，构建合成输入与真实目标输出对，通过合成到真实训练范式结合说话人验证损失实现零样本语音转换。
result: 跨语言实验表明，方法在自然度和说话人相似度指标上超越基线模型，即使在仅使用英语数据训练时也能泛化到多语言。
conclusion: 提出的框架通过KNN检索和合成到真实训练，实现了无需平行数据的零样本语音转换，展现出良好的跨语言泛化能力和实用前景。
---

## 摘要
我们提出了一个语音转换（VC）框架，该框架利用在 WavLM 表示上的 K 近邻（KNN）检索来对齐非平行的源语音和目标语音，构建用于监督学习的合成训练对。检索到的片段作为合成输入，而真实的目标音频提供真实输出，形成了一种合成到真实的训练范式，该范式自然支持多语言数据，无需平行语料库或显式对齐。为了确保一致的目标说话人身份，我们引入了一个从预训练说话人验证模型得到的说话人损失。跨多种语言的实验表明，所提出的方法实现了高自然度和强说话人相似度，优于竞争性的 VC 基线，尽管仅使用英语数据进行训练。样本可在 https://palindromic-vc.github.io 访问。

## Abstract
We present a voice conversion (VC) framework that utilizes K-Nearest Neighbors (KNN) retrieval over WavLM representations to align non-parallel source and target speech, constructing synthetic training pairs for supervised learning. The retrieved segments serve as synthetic inputs, while real target audio provides ground-truth outputs, forming a synthetic-to-real training paradigm that naturally supports multilingual data without requiring parallel corpora or explicit alignment. To ensure consistent target-speaker identity, we incorporate a speaker loss derived from a pretrained speaker verification model. Experiments across multiple languages demonstrate that the proposed approach achieves high naturalness and strong speaker similarity, outperforming competitive VC baselines, despite being trained exclusively on English data. Samples can be accessed at: https://palindromic-vc.github.io.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：零样本、任意到任意的语音转换，要求仅利用非平行语料和极短的目标说话人参考音频，实现高自然度且说话人身份一致的转换。
- **研究背景**：
  - 传统 VC 依赖平行语料，成本高且难以扩展。
  - 基于解耦的方法（PPG、自监督特征）往往存在语言依赖或说话人信息泄漏，影响泛化。
  - KNN-VC 等简单检索方法在目标说话人数据稀少（低资源）时性能显著下降。
- **整体含义**：提出一种无需平行数据、通过构建合成训练对实现可控监督学习的框架，在零样本多语言场景下保持高说话人相似度和自然度。

### 2. 论文提出的方法论

- **核心思想**：采用“回文”式训练策略，利用 KNN 检索生成合成源特征，再训练模型将其映射回真实目标语音，形成“合成→真实”的监督。
- **关键技术细节**：
  - **WavLM 特征提取**：使用预训练 WavLM 的第6层输出作为中间表示。
  - **KNN 检索生成合成训练对**：对目标音频 a₁ 和目标说话人参考集 Bᵣₑf₎ 提取特征，通过 KNN 将 Bᵣₑf₎ 中与 a₁ 最相似的特征替换，构造合成特征 ˆB₁，形成合成输入，真实目标音频作为监督输出。
  - **三阶段训练**：
    1. **Vocoder 预训练**：训练 HiFi-GAN 类声码器，将 WavLM 特征重建为音频，损失含多分辨率 STFT 及对抗损失。
    2. **Transformer 转换模型训练**：6 层 Transformer，16 注意力头，隐维 1024，同时使用 L1 损失（预测特征与真实特征）和波形级说话人损失（ECAPA-TDNN 提取的说话人嵌入相似度 + L1），强约束说话人身份。
    3. **Vocoder 后训练**：在 Transformer 输出特征上再训练一个声码器，适配转换特征分布，消除听觉伪影。
  - **推理**：可直接输入真实（非合成）语音，泛化至零样本转换。

### 3. 实验设计

- **数据集**：
  - 训练：LibriSpeech（960 小时英语），非平行配对。
  - 多语言评估：Multilingual LibriSpeech（荷兰语、英语、法语、德语、意大利语、波兰语、葡萄牙语、西班牙语），仅用英语训练，不做微调。
- **对比方法**：
  - KNN-VC（骨干方法）
  - Seed-VC
  - Vevo
  - O-O-VC
- **评估指标**：
  - 客观：说话人相似度、EER、WER、CER、DNS-MOS
  - 主观：MOS（自然度）、SMOS（说话人相似度）
- **评估设置**：按目标说话人提示时长分为 3s、10s、30s、60s，每种时长随机采 50 对源-目标，重复 3 次种子。

### 4. 资源与算力

- 论文未明确给出 GPU 型号、数量及训练总时长。
- 仅说明了各阶段训练步数：声码器预训练 100k 步，Transformer 训练 800k 步（优化器 Adam，学习率 3×10⁻⁴），声码器后训练 100k 步。具体的硬件配置和总时间未提及。

### 5. 实验数量与充分性

- **实验组数**：包含主实验（英文，对比 4 个基线，4 种提示时长）、多语言泛化实验（8 种语言，每种 3 种时长）、消融实验（声码器后训练前后）。
- **充分性**：实验覆盖了零样本多场景、多语言、不同数据量，且与 SOTA 基线对比，包含客观和主观评测，整体较充分。
- **公平性**：使用与训练不同的说话人验证模型评估，避免过拟合；基线选取涵盖不同数据规模（Vevo、Seed-VC 训练数据量远大于所提方法），但作者未刻意追求数据量平衡，属于事实对比。

### 6. 论文的主要结论与发现

- 提出的回文训练结合合成输入的方法，在非平行、零样本条件下有效。
- 波形级说话人损失显著提升说话人相似度和 EER，且不损害自然度与可懂度。
- 仅用英语训练即可在多语言上实现较低的 WER，展现强跨语言泛化。
- 相较于骨干方法 KNN-VC，在低资源（3s 提示）时优势尤其明显，主观 MOS 和 SMOS 大幅领先。
- 声码器后训练能有效减少转换伪影，提升 DNS-MOS。

### 7. 优点

- **无需平行数据**：通过合成-真实训练范式，彻底摆脱对齐数据依赖。
- **说话人相似度约束强**：直接利用说话人验证损失监督波形，效果稳定。
- **出色的低资源表现**：在极短目标参考（3s）下性能不退化。
- **跨语言泛化**：仅英语训练即可在多语言保持竞争力，具实用价值。
- **方法简洁**：相比大型解耦或扩散模型，结构清晰，三阶段训练可控。

### 8. 不足与局限

- **计算消耗未明确**：缺乏硬件和时间开销报告，不便于复现和成本评估。
- **训练数据规模较小**：仅用 LibriSpeech 960 小时，与部分基线（6 万小时以上）有差距，可能影响上限。
- **语音风格与表现力受限**：未探讨情感、韵律或高度表现力的转换，在戏剧性或歌唱等场景可能不足。
- **依赖 WavLM 和 KNN 检索质量**：基础特征和检索策略固定，对噪声或域外数据的鲁棒性未验证。
- **主观评估样本量有限**：每种时长仅 3 个样本，虽评分人数足够，但样本多样性受限。

（完）
