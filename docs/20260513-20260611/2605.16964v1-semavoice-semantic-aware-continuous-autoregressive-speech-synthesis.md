---
title: "SemaVoice: Semantic-Aware Continuous Autoregressive Speech Synthesis"
title_zh: SemaVoice：语义感知的连续自回归语音合成
authors: "Huimeng Wang, Hui Lu, Jiajun Deng, Haoning Xu, Youjun Chen, Xueyuan Chen, Zhaoqing Li, Shuhai Peng, Shiyin Kang, Xunying Liu"
date: 2026-05-16
pdf: "https://arxiv.org/pdf/2605.16964v1"
tags: ["query:speech-audio"]
score: 9.0
evidence: 语义感知的连续自回归语音合成用于零样本TTS
tldr: "现有连续自回归语音合成存在语义-韵律建模与重建驱动语音表示失配，导致模型过度聚焦声学细节而损害语义连贯性，加剧自回归误差累积。SemaVoice提出语义感知框架，通过语音基础模型引导的对齐机制优化连续表示，捕获局部语义一致与全局结构，并利用补丁式扩散头合成高质量语音。在Seed-TTS上，英文WER低至1.71%，且在不同表示粒度下均显著提升，为高保真零样本TTS提供了有效方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.16964v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1448, \"height\": 592, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.16964v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1447, \"height\": 855, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.16964v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1212, \"height\": 376, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.16964v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1052, \"height\": 222, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.16964v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1333, \"height\": 434, \"label\": \"Table\"}]"
motivation: 现有连续自回归TTS中语义-韵律建模与低层声学重建的不匹配，导致语义连贯性受损、误差在自回归过程中累积。
method: 提出SemaVoice，采用语音基础模型（SFM）引导对齐优化连续语音表示，并结合补丁式扩散头在自回归框架内合成语音。
result: "在Seed-TTS基准上，SemaVoice取得1.71%的英文WER，客观与主观评测中与顶尖开源系统竞争，且不同表示粒度下均有效提升。"
conclusion: SFM引导的对齐机制显著增强了语义一致性，提升了连续自回归语音合成的质量和鲁棒性，为高保真零样本TTS开辟新方向。
---

## 摘要
连续自回归语音合成最近已成为零样本文本到语音（TTS）有前景的方向。然而，现有方法仍然受到语义-韵律建模与重建驱动的连续语音表征之间的根本性不匹配的困扰。这种不匹配导致TTS模型过度关注低层声学纹理，而牺牲了高层语义连贯性，进一步加剧了自回归生成中的误差累积。为解决这一挑战，我们提出SemaVoice，一个用于高保真零样本TTS的语义感知连续自回归框架。SemaVoice引入了一种语音基础模型（SFM）引导的对齐机制，该机制优化连续语音表征，以更好地捕捉局部语义一致性和全局结构关系。这些表征在自回归框架内为逐块扩散头提供条件，以实现高质量语音合成。在Seed-TTS基准上的实验结果表明，SemaVoice实现了1.71%的英文词错误率（WER），并且在客观和主观评估中与最先进的开源系统保持高度竞争力。通过固定信息速率约束下不同表征粒度的实验，进一步证实了SFM引导对齐的有效性，带来了显著改进。

## Abstract
Continuous autoregressive speech synthesis has recently emerged as a promising direction for zero-shot text-to-speech (TTS). However, existing methods still suffer from a fundamental mismatch between semantic-prosodic modeling and reconstruction-driven continuous speech representations. This mismatch causes TTS models to focus excessively on low-level acoustic textures at the expense of high-level semantic coherence, further exacerbating error accumulation in autoregressive generation. To address this challenge, we propose SemaVoice, a semantic-aware continuous autoregressive framework for high-fidelity zero-shot TTS. SemaVoice introduces a Speech Foundation Model (SFM) guided alignment mechanism that refines continuous speech representations to better capture both local semantic consistency and global structural relationships. These representations condition a patch-wise diffusion head within the autoregressive framework for high-quality speech synthesis. Experimental results on the Seed-TTS benchmark show that SemaVoice achieves an English WER of 1.71\% and remains highly competitive with state-of-the-art open-source systems in both objective and subjective evaluations. The effectiveness of SFM guided alignment is further confirmed by significant improvements under varying representation granularities with a fixed information-rate constraint.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **问题背景**：连续自回归（AR）语音合成在零样本 TTS 中潜力巨大，但其根本瓶颈在于语义‑韵律建模与重建驱动的连续语音表征之间存在**本质失配**。
- **具体表现**：变分自编码器（VAE）学到的连续表示主要为保真重建优化，缺乏与文本语义的显式对齐。这导致 TTS 模型过度关注低层声学纹理，而牺牲高层语义连贯性，加剧自回归生成中的**误差累积**。
- **整体含义**：提出 **SemaVoice** 框架，旨在通过语义感知的对齐机制，在连续 AR 语音合成中同时强化语义‑韵律一致性与声学细节保真度，从而提升零样本 TTS 的质量与鲁棒性。

### 2. 论文提出的方法论

SemaVoice 的核心是**语义感知的连续自回归语音合成**，主要包含两大模块：

#### 2.1 语义对齐的连续语音表征（SFM‑guided Alignment）
- **基础VAE**：采用 **σ‑VAE** 变体（额外引入可学习的方差参数 σ），将 24 kHz 波形压缩到 15 Hz 连续隐变量 `z`，保证高压缩与高还原。
- **SFM 特征提取**：使用冻结的语音基础模型（WavLM‑large）提取高层语义特征 `e`，经投影和下采样得到与 `z` 同尺寸的序列 `s`。
- **双重对齐损失** `Lalign`：
  - **帧级对齐** `Lframe`：最大化 `z` 与 `s` 逐帧余弦相似度，保证**局部语义一致性**。
  - **成对结构对齐** `Lpair`：对齐两个序列的自相似矩阵（`Dz`, `Ds`），保持**全局时序结构**。
- **自适应权重**：根据梯度模长比动态缩放 `Lalign`，平衡对齐与重建目标。最终 VAE 损失为 `Ltotal = LVAE + λalign·Lalign`。

#### 2.2 连续自回归建模与扩散头生成
- **AR 建模**：将连续表示分成长度 `L=2` 的补丁（patch）作为序列单元，由预训练 LLM（Qwen2.5‑1.5B）逐块预测下一补丁的隐藏状态 `hi-1`。
- **扩散头**：不是直接回归补丁，而是用轻量 **LocDiT（局部扩散 Transformer）**对目标补丁 `pi` 做条件扩散去噪。
  - 条件包含：LLM 隐藏状态 `hi-1`、前一个已生成补丁 `pi-1`，实现上下文感知的补丁生成。
  - 扩散目标为标准噪声预测损失 `Ldiff`。
- **分类器自由引导**：训练时随机丢弃 LLM 条件，推断时组合条件与无条件预测，引导尺度 `w=2.5`，提升生成质量。

### 3. 实验设计

- **数据集**：
  - **大规模训练**：150K 小时双语语料（100K 小时 Emilia 开源 + 50K 小时内部数据，中英各半）。小规模版本（Emilia‑only）使用约 46.8K 小时英文子集。
  - **VAE 训练**：20K 小时双语子集（均取自 Emilia）。
- **评测基准**：**Seed‑TTS 基准**，含英语（EN）、中文（ZH）和困难（Hard）三个子集。
- **对比方法**：大量开源 SOTA 系统，涵盖离散/连续、AR/NAR/MLLM 等范式，包括 F5‑TTS、MaskGCT、CosyVoice 系列、Spark‑TTS、FireRedTTS 系列、IndexTTS 2、VoxCPM、VibeVoice、Qwen2.5‑Omni、HiggsAudio‑v2 等。
- **评价指标**：
  - **客观**：WER/CER（Whisper‑large‑v3 / Paraformer‑zh）、说话人相似度（WavLM‑large，SIM）。
  - **主观**：自然度 MOS（N‑MOS）、说话人相似度 MOS（S‑MOS），12 名评分员，20 句。
  - **VAE 重建**：STOI、PESQ、UTMOS。

### 4. 资源与算力

- **VAE 训练**：8 块 NVIDIA A800 GPU，全局批大小 320 秒，训练 280K 步。
- **全量 TTS 模型训练**：8 块 NVIDIA H200 GPU，全局批大小 8192 秒，训练 150K 步（SemaVoice‑Emilia）或 300K 步（SemaVoice）。
- **消融实验 TTS 模型**：8 块 NVIDIA A800 GPU，全局批大小 1024 秒，训练 100K 步。
- 论文**明确给出了 GPU 型号、数量和训练步数/时长**，算力资源交代充分。

### 5. 实验数量与充分性

实验设计较为丰富且系统，包括：
- **大规模客观对比**（表 1）：与 10 余个 SOTA 系统对比，覆盖多语言、多子集。
- **主观评测**（表 2）：EN 和 ZH 两语言的自然度与说话人相似度 MOS，对比强劲基线。
- **消融实验**（表 3）：分别移除 SFM 对齐和历史补丁条件，检验各组件贡献。
- **表示粒度影响分析**（表 4）：固定信息率下改变帧率和维度，考察对齐在不同建模难度下的作用，兼顾重建与生成评估。
- 实验在**统一基准 Seed‑TTS** 上执行，指标全面，比较对象多样，且包含足够数量的消融和细粒度控制实验，**充分性、客观性与公平性较高**。

### 6. 论文的主要结论与发现

- SemaVoice 在 Seed‑TTS 上获得 **1.71% 英文学词错误率**，与最优开源系统竞争；中文 CER 1.18%，说话人相似度接近领先水平。
- SFM 引导的对齐机制**显著提升语义连贯性**和说话人相似度：消融实验中，移除对齐使 WER 从 2.97% 升至 3.40%，SIM 下降约 0.01。
- 历史补丁条件对局部连贯性**至关重要**，移除后 WER 飙升至 8.46%。
- 在固定信息率下，表示越细粒度（帧率越高），AR 建模压力越大，**SFM 对齐的增益也愈发明显**，验证了其在困难建模条件下的稳定作用。

### 7. 优点

- **创新性**：首次明确诊断连续 AR TTS 的语义表示失配问题，并提出 SFM 引导对齐的针对性解决方案，无需改动下游 TTS 架构。
- **技术融合**：结合 σ‑VAE 高压缩率、补丁级扩散生成与历史条件，有效缓解自回归误差累积并提升生成鲁棒性。
- **实验扎实**：在公开基准 Seed‑TTS 上与众多开源 SOTA 全面比较，主观、客观证据充分，消融和粒度分析角度新颖、结论有说服力。
- **计算透明**：明确给出 GPU 型号、批大小、训练步数，可复现性较好。

### 8. 不足与局限

- **语言覆盖**：评测限于中英双语，未涉及其它语种或更多样化的领域数据，方法的跨语言泛化性未经验证。
- **自回归固有问题**：作为连续 AR 模型，仍存在**序列推理延迟**和长序列上的**误差累积**风险，文中并未给出针对推理效率的优化或讨论。
- **SFM 依赖**：对齐效果受限于所选的 WavLM‑large 模型，未探讨不同 SFM 或替换为非自监督特征的影响。
- **规模限制**：消融实验仅基于 Emilia‑EN 约 50K 小时训练，且仅用 1.5B 参数 LLM，未尝试更大规模带来的潜力或边际效益。
- **主观评估**：仅 12 名评分员、20 句测试，样本量有限，可能引入一定统计偏差。

（完）
