---
title: "PS4: Proxy-Supervised Joint Training for Real Target Speaker Extraction"
title_zh: PS4：基于代理监督的联合训练实现真实目标说话人提取
authors: "Wanyi Ning, Wei Zhou, Yingpeng Li, Yinshang Guo, Haitao Qian, Yiming Cheng"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08111v1"
tags: ["query:speech-audio"]
score: 8.0
evidence: 构建大规模语料库用于目标说话人提取
tldr: 针对真实对话混合场景中缺乏大规模训练数据和干净目标语音的问题，提出PS4代理监督联合训练框架。构建包含71771样本的多数据集语料，采用ASR交叉熵、说话人相似性、帧级VAD和感知音频质量四种可微目标微调BSRNN模型。在REAL-T挑战赛中获得第二名，并取得最佳说话人相似度和时序F1。该方法证明了代理监督策略在真实场景目标说话人提取中的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.08111v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 899, \"height\": 803, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08111v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 903, \"height\": 257, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.08111v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 850, \"height\": 311, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08111v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 870, \"height\": 279, \"label\": \"Table\"}]"
motivation: 真实对话混合中缺少大规模标注语料和干净目标语音，导致目标说话人提取模型训练困难。
method: 构建大型多数据集语料，并利用ASR、说话人相似性、VAD和感知质量四种代理任务联合微调BSRNN分离器。
result: 在REAL-T挑战赛中综合排名第二，说话人相似度和时序F1两项指标均位列第一。
conclusion: 代理监督框架能有效利用现有弱标签数据，提升真实场景下目标说话人提取性能。
---

## 摘要
针对真实对话混合场景训练目标说话人提取模型仍具挑战性，因为缺乏大规模训练语料库和用于监督的干净目标语音。我们提出PS4，一种用于真实对话混合场景中目标说话人提取的代理监督训练框架，主要贡献有两方面。首先，我们从四个公开数据集中构建了包含71,771个训练样本的大规模语料库，覆盖中文和英文场景。每个样本包含重叠语音混合、每个说话人的注册音频、真实文本标注和帧级语音活动标签。其次，我们提出一种代理监督联合训练策略，使用四个互补的可微目标（ASR交叉熵、说话人相似度、帧级语音活动检测和感知音频质量）对基于BSRNN的目标说话人提取模型进行微调。从公开可用的预训练检查点开始，微调期间仅更新BSRNN分离器。在REAL-T挑战排行榜上，PS4整体排名第二，在所有提交系统中实现了最佳说话人相似度和时序F1分数。

## Abstract
Training target speaker extraction (TSE) models for real conversational mixtures remains challenging because large-scale training corpora and clean target speech for supervision are unavailable. We present PS4, a proxy-supervised training framework for TSE in real conversational mixtures, with two main contributions. First, we construct a large-scale corpus of 71,771 training samples derived from four public datasets, covering both Chinese and English scenarios. Each sample contains an overlapping speech mixture, per-speaker enrollment audio, a ground-truth transcript, and frame-level voice activity labels. Second, we propose a proxy-supervised joint training strategy that fine-tunes a BSRNN-based TSE model using four complementary differentiable objectives: ASR cross-entropy, speaker similarity, frame-level voice activity detection, and perceptual audio quality. Starting from a publicly available pre-trained checkpoint, only the BSRNN separator is updated during fine-tuning. On the REAL-T challenge leaderboard, PS4 ranks 2nd overall, achieving the best speaker similarity and timing F1 among all submitted systems.

---

## 论文详细总结（自动生成）

### 论文核心问题与整体含义（研究动机和背景）

- **核心问题**：目标说话人提取（TSE）在真实对话混合场景中面临两大挑战：缺乏大规模带干净目标语音的标注训练语料；现有模型通常基于人工混合的模拟数据（如WSJ0-2mix、LibriMix）训练，在真实环境（混响、噪声、不规则重叠）中性能严重下降。
- **研究动机**：REAL-T 基准虽提供了真实混合录音和评估标注，但不提供匹配的训练语料或干净目标语音。如何有效利用真实对话录音（仅含弱标签如文本、说话人注册、VAD）来训练TSE模型是一个开放问题。
- **整体含义**：提出一种无需干净参考信号的代理监督框架 PS4，利用真实数据中可直接获取的语言、说话人、时序和感知质量信号联合训练，从而弥合模拟与真实场景之间的差距。

### 方法论：核心思想、关键技术细节

- **核心思想**：构建大规模代理监督语料库 REAL-PS4，再利用四个互补的可微代理目标对预训练 BSRNN-ECAPA 模型进行联合微调，只更新分离器（BSRNN），保持说话人编码器（ECAPA-TDNN）冻结。
- **关键技术细节**：
  1. **语料库构建**：从 AISHELL-4、AliMeeting、AMI、CHiME-6 四个公开数据集出发，分别提取注册语音（单说话人片段≥5s，每说话人最多5段）和混合样本（重叠区域≥2说话人，合并<0.5s间隙，片段5-100s），并经过质量过滤（目标说话人占比>20%、转录文本≥2有效字符、混合片段≤30s、注册语音≤10s），最终得到71,771个训练样本。
  2. **代理监督目标**（公式见论文 Eq.1-5）：
     - **语言监督**：冻结 Whisper large-v3 作为可微教师，计算预测 token 分布与真实转录的交叉熵损失（\( L_{CE} \)）。
     - **说话人监督**：使用余弦边距排名损失，鼓励提取语音与注册语音的相似度超过原始混音与注册语音的相似度一定边距（\( L_{SIM} \)），说话人嵌入通过冻结的 ResNet34 提取。
     - **时间监督**：基于帧级能量预测与来自话语活动标签的二元交叉熵损失（\( L_{VAD} \)）。
     - **感知监督**：使用可微 DNSMOS 模型，最小化负的整体感知质量分数（\( L_{DNSMOS} \)）。
  3. **联合训练**：总损失 \( L = \lambda_{ce} L_{CE} + \lambda_{sim} L_{SIM} + \lambda_{dns} L_{DNSMOS} + \lambda_{vad} L_{VAD} \)，通过梯度反向传播优化 BSRNN 分离器。

### 实验设计

- **数据集与场景**：
  - **训练**：REAL-PS4 语料库（包含上述四个数据集的中英文会议/谈话场景）。
  - **评估**：REAL-T 开发集（1,991个样本），涵盖 AISHELL-4、AliMeeting、AMI、CHiME-6、DipCo 五个真实对话语料子集。
- **Benchmark**：REAL-T 挑战赛官方评估协议，四个指标：Token Error Rate (TER，越低越好)、Speaker Similarity (SIM，越高越好)、DNSMOS OVRL 或 DNSMOS-P808（感知质量，越高越好）、Timing F1（越高越好）。
- **对比方法**：REAL-T 官方提供的两个 BSRNN 基线（BSRNN_TFMAP 和 BSRNN_EMB），两者均在模拟混合上训练，未在真实数据上微调。
- **提交结果**：PS4 系统提交至 REAL-T 挑战官方排行榜，与包括 MERL、CARTSE 在内的其他参赛系统竞争。

### 资源与算力

- **未明确说明**：论文未提及 GPU 型号、数量、训练时长等具体计算资源。仅提到使用公开预训练检查点（BSRNN-ECAPA），并利用四个代理模型（Whisper large-v3、ResNet34、DNSMOS）均为冻结状态，仅微调 BSRNN 分离器，但并未报告训练耗时或硬件配置。

### 实验数量与充分性

- **实验数量**：
  - 在开发集上进行了两轮对比：① 与两个基线在四个指标上的整体对比（图2、表I）；② 每个子数据集上的详细数值结果（表I）。
  - 提交至官方排行榜（表II），与排名第一的 MERL、第三的 CARTSE 等竞争。
- **充分性评估**：
  - **优点**：覆盖了5个不同领域、中英文、远场/近场的真实数据集，验证了跨场景泛化能力；四个指标全面衡量语言、说话人、时间和感知质量。
  - **不足**：缺乏消融实验（如分别去除每个代理目标对最终性能的影响），无法定量评估各监督信号的贡献；未与除 BSRNN 外的其他 TSE 架构（如 Conv-TasNet、SepFormer）进行对比；未报告在模拟数据上的微调结果作为对照；仅进行了开发集和官方验证集上的评估，没有测试集结果（排行榜使用验证集）。

### 主要结论与发现

- PS4 在 REAL-T 开发集上显著优于官方 BSRNN 基线，在所有五个子数据集所有四项指标上均有提升，尤其在感知质量（DNSMOS OVRL 从<2.0 提升至>3.1）和说话人相似度上提升明显。
- 在官方排行榜（验证集）上，PS4 综合排名第二，并取得 **最佳 Speaker Similarity (0.565)** 和 **最佳 Timing F1 (0.871)**，证明了代理监督策略在真实场景中保持说话人身份和提取时序准确性方面的有效性。
- AMI 数据集表现最好（TER 0.388，SIM 0.714），AISHELL-4 最困难（TER 0.567），表明远场条件和高度重叠会增加难度。

### 优点

- **创新性**：首次提出无需干净目标语音的代理监督训练框架，充分利用真实数据中的弱标签（转录、说话人注册、VAD），解决了真实场景 TSE 训练数据稀缺问题。
- **实用性**：构建了大规模真实语料库 REAL-PS4（71,771样本），并公开提供，可复现。
- **全面性**：四个代理目标覆盖语言、说话人、时间、感知四个维度，互补性强。
- **性能突出**：在权威挑战中取得最佳说话人相似度和时序 F1，综合排名第二。
- **可迁移性**：仅微调 BSRNN 分离器，冻结其他预训练模型，计算效率较高（但未具体量化）。

### 不足与局限

- **缺乏消融分析**：未评估每个代理目标的单独贡献，无法确定哪些目标最关键。
- **架构泛化性**：仅测试了 BSRNN 一种分离器，未证明框架对其他分离架构（如 SepFormer、Conv-TasNet）的适用性。
- **预训练依赖**：依赖多个外部预训练模型（Whisper、ResNet34、DNSMOS），这些模型可能存在领域偏见或错误传播。
- **语料库偏差**：REAL-PS4 仅来自四个特定类型的会议/晚餐数据集，可能无法代表所有真实对话场景（如电话、街头、多语言）。
- **计算开销未报告**：未说明训练所需 GPU 数量、时间，影响可复现性和资源评估。
- **指标局限**：排行榜上感知质量（DNSMOS-P808）略低于第一名，TER 也略高于第一名，说明部分指标仍有提升空间。

（完）
