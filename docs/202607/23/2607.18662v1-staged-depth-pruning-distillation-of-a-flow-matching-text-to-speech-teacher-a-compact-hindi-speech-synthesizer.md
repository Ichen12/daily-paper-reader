---
title: "Staged Depth-Pruning Distillation of a Flow-Matching Text-to-Speech Teacher: A Compact Hindi Speech Synthesizer"
title_zh: 流匹配文本转语音教师模型的分阶段深度剪枝蒸馏：一个紧凑的印地语语音合成器
authors: Sivateja Trikutam
date: 2026-07-19
pdf: "https://arxiv.org/pdf/2607.18662v1"
tags: ["query:speech-audio"]
score: 10.0
evidence: 印地语文本转语音合成
tldr: 数据预算仅17.6小时的印地语TTS任务中，从头训练小模型失败。本文通过深度剪枝蒸馏大流匹配教师IndicF5 (337M参数)，逐步从22块剪至6块并行微调，结合ASR的WER检查。学生模型在249M和190M参数时WER 0.00，131M仍鲁棒，102M出现能力悬崖（归因于数据预算）。该方法生成高质量印地语语音，在6GB笔记本GPU实时运行，提供了紧凑TTS的实用方案。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.18662v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1121, \"height\": 162, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18662v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1481, \"height\": 259, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18662v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1489, \"height\": 311, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18662v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1644, \"height\": 261, \"label\": \"Table\"}]"
motivation: 面对仅17.6小时的极低数据预算，从头训练小印地语TTS模型失败，需探索从大模型蒸馏的有效紧凑方案。
method: 保持教师模型的宽度、文本维度等，仅逐阶段剪枝Transformer块数（22→16→12→8→6），每次剪枝后微调，并用ASR的WER评估门控。
result: 学生模型在249M和190M参数时WER 0.00，131M仍鲁棒，102M出现能力悬崖；在6GB GPU实时运行，质量高。
conclusion: 提出深度剪枝蒸馏方法在极低数据预算下成功构建紧凑印地语TTS，输出高质量语音，可在消费级GPU实时推理。
---

## 摘要
我们提出了一种构建紧凑型印地语文本转语音（TTS）模型的实用方法，通过蒸馏一个大型流匹配教师模型（IndicF5，3.37亿参数DiT），在严格的数据预算（约17.6小时）下进行。在这种数据量下从头训练一个小模型会完全失败。相反，我们通过仅剪枝深度来从教师模型热启动学生模型：保持教师模型的宽度、文本维度、注意力头数以及mel/文本输入输出不变，使得所有非块张量一对一复制，并保留均匀间隔的transformer块子集。我们首先测量教师模型能容忍多少深度（在减少27%块时仍接近可用，但超过50%后崩溃），然后逐步下降（22 -> 16 -> 12 -> 8 -> 6个块），每次剪枝后重新微调，每一步通过一个客观的ASR词错误率（WER）检查来把关。最终的学生模型在249M和190M参数时对未见句子达到WER 0.00，并保持鲁棒性至131M；在102M时我们观察到明显的容量悬崖，我们将其归因于数据预算而非方法。我们还记录了两种训练/推理特征和库的不一致性（mel滤波器组和旋转嵌入库版本）会静默地降低音频质量，以及一种版本无关的修复方法。该方法产生的高质量印地语语音可在6GB笔记本电脑GPU上实时运行。一个独立的50句FLEURS基准测试将已发布的190M学生模型与其教师模型和MMS-TTS-hin进行了比较。

## Abstract
We present a practical recipe for building a compact Hindi text-to-speech (TTS) model by distilling a large flow-matching teacher (IndicF5, 337M-parameter DiT) under a severe data budget (~17.6 hours). Training a small model from scratch on this much data fails outright. Instead we warm-start the student from the teacher by pruning depth only: keeping the teacher's width, text dimension, attention heads, and mel/text I/O fixed so all non-block tensors copy one-to-one, and retaining an evenly-spaced subset of transformer blocks. We first measure how much depth the teacher tolerates (it remains near-functional at -27% blocks but collapses past -50%), then descend gradually (22 -> 16 -> 12 -> 8 -> 6 blocks), re-fine-tuning after each prune, with each step gated by an objective ASR word-error-rate (WER) check. The resulting students reach WER 0.00 on unseen sentences at 249M and 190M parameters, and remain robust down to 131M; at 102M we observe a clear capacity cliff that we attribute to the data budget rather than the recipe. We also document two train/inference feature- and library-parity failures (mel filterbank and rotary-embedding library versions) that silently degrade audio, and a version-independent fix. The method yields a high-quality Hindi voice that runs in real time on a 6 GB laptop GPU. An independent 50-sentence FLEURS benchmark compares the released 190M student against its teacher and MMS-TTS-hin.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在极低数据预算（仅约17.6小时）下，构建一个紧凑、高质量的印地语文本转语音（TTS）模型。现有的多语言TTS模型要么过大（如IndicF5约3.5亿参数、Indic Parler-TTS约8.8亿参数），无法在设备端或低成本部署；要么过小且质量差（如MMS-TTS约3600万参数、Kokoro约8200万参数且印地语覆盖弱）。目标是在约1–1.9亿参数的中等规模内实现清晰、可泛化的印地语语音合成。

- **研究意义**：提出了一种实用的知识蒸馏方法，通过深度剪枝将大型流匹配教师模型压缩为小型学生模型，同时通过逐级微调和ASR门控确保质量，为数据稀缺场景下的紧凑TTS构建提供了可行方案。

- **背景**：从头训练小模型失败，因为流匹配TTS通常需要大量数据和训练步数。本文的创新在于利用教师模型权重进行“热启动”，仅剪枝深度（保留宽度），使学生模型继承教师的大部分能力。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：保持教师模型的宽度（dim=1024）、注意力头数（16个）、文本嵌入维度（512）、mel/text输入输出结构不变，仅通过减少transformer块数（depth）来缩小模型。所有非块张量（输入嵌入、文本编码器、时间嵌入、旋转嵌入、输出归一化和投影）形状一致，可直接复制教师权重。块的选择采用均匀间隔采样，保留首尾块。

- **关键技术细节**：
  - **深度剪枝容忍度分析**：先测量未训练的剪枝学生模型输出RMS幅度，发现移除约27%块（16块）时输出接近教师，移除≥50%时崩溃（输出近静音）。这决定了必须逐步剪枝，而非一次性大幅剪枝。
  - **分阶段蒸馏阶梯**：从教师22块开始，逐步剪枝至16→12→8→6块，每步从上一阶段微调后的学生模型热启动，再进行条件流匹配目标微调（学习率5e-5，500步预热，动态batch，Euler ODE求解器，EMA权重）。每个阶段用ASR词错误率（WER）门控：只有当前阶段WER恢复至接近上一阶段才继续剪枝。
  - **训练数据**：仅17.6小时教师生成的音频（9999个话语），包含真实印地语文本和模板句（数字、日期、领域短语）。学生使用教师相同的2545字符词汇表。
  - **评估协议**：使用IndicWav2Vec-Hindi ASR计算WER，对已知和未见句子各生成两次，WER归一化去除标点和nukda变音符号。

- **公式/算法流程（文字说明）**：
  - 教师IndicF5 → 测量剪枝容忍度 → 从22块开始微调（阶段0）→ 剪枝至16块（保留均匀间隔的16个块）→ 热启动学生 → 重新微调 → ASR-WER门控检查 → 若通过，继续剪枝至12块 → 重复直至目标深度（6块）。

## 3. 实验设计：数据集、benchmark、对比方法

- **数据集**：
  - 训练：教师生成的多风格印地语音频，约17.6小时，9999个话语。
  - WER门控测试：两个句子（一个已知、一个未见），每个生成两次。
  - 独立基准测试：FLEURS hi_in测试集50句未见句子（10–22个词），与教师和MMS-TTS-hin对比。

- **Benchmark**：
  - 主要指标：WER（词错误率，通过ASR转录比较）、CER（字符错误率）、SIM（说话人相似度，ECAPA-TDNN余弦相似度）、UTMOS（预测自然度MOS分数）、RTF（实时因子，合成时长/音频时长）。
  - ASR模型：门控时用IndicWav2Vec-Hindi；独立基准用Vakyansh Hindi wav2vec2（因IndicWav2Vec受限）。

- **对比方法**：
  - 教师模型：IndicF5（337M参数），多语言流匹配TTS。
  - MMS-TTS-hin（36M参数），基于VITS的轻量级多语言TTS，不支持声音克隆。
  - 学生模型：各剪枝深度（249M、190M、131M、102M参数）的自评估。

## 4. 资源与算力

- **算力信息**：
  - 训练：文中未明确指定GPU型号、数量及具体训练时长。仅提到每个阶段微调使用动态batch（24,000 mel帧）和500步预热；阶段A（22→16）在1个epoch内损失降至约0.49，可理解语音在2000步出现。最终6块模型训练了150 epoch（42.9k步），但未报告总GPU小时数。
  - 部署：在6GB笔记本电脑GPU上实时运行（RTF<1？文中只说“runs in real time on a 6 GB laptop GPU”）。独立基准RTF在Apple M4 MPS后端测得：190M学生RTF=3.13（MPS），MMS-TTS-hin在CPU上RTF=0.31。

- **结论**：论文未详细披露完整训练所需的GPU数量和时间，这是一个信息缺口。

## 5. 实验数量与充分性

- **实验组数**：
  - 剪枝容忍度分析：测试了16、11、6块三个深度，测量RMS幅度（表2）。
  - 阶段蒸馏：共4个阶段（16、12、8、6块），每个阶段微调后报告WER（表3）。
  - 独立基准测试：50句FLEURS测试，比较了3个模型（教师、190M学生、MMS-TTS-hin），包含WER/CER/SIM/UTMOS/RTF五个指标（表4）。
  - 部署问题：记录了三种训练/推理不一致（mel滤波、旋转嵌入、torch.compile前缀），每种都有描述和修复。

- **充分性与公平性**：
  - 正面：门控机制确保每个阶段质量不退化；独立基准使用相同句子、固定种子、统一ASR评估；对比了教师和同类小模型，覆盖质量、速度、可克隆性。
  - 不足：仅使用ASR-WER和UTMOS（预测MOS）而非人类评价；仅针对印地语；数据预算单一（17.6小时），未探索更大数据的影响；消融实验仅做了剪枝容忍度，未测试不同剪枝策略（如随机选择块 vs 均匀选择）或不同微调超参。另外，只报告了2次随机生成的WER范围，随机性影响可能较大。

## 6. 论文的主要结论与发现

- **主要结论**：在极低数据预算下，通过从教师模型深度剪枝并逐步微调，可以成功构建高质量紧凑印地语TTS。在249M和190M参数时，学生模型对未见句子达到WER 0.00；131M仍鲁棒；102M出现容量悬崖（归因于数据预算不足，而非方法本身）。最佳操作点推荐190M学生（平衡大小与质量）。

- **其他发现**：
  - 深度剪枝存在容忍度阈值（约27%块移除可用，50%崩溃），必须逐步下降。
  - 热启动使得微调快速收敛（2000步即可生成可理解语音）。
  - 训练/推理的库或特征不一致（mel滤波版本、旋转嵌入库版本、torch.compile前缀）会静默降低质量，需版本冻结或显式修复。
  - 190M学生保留了教师约96%的自然度和说话人相似度，参数减少56%，速度提升1.8倍。

## 7. 优点：方法或实验设计亮点

- **方法亮点**：
  - **深度仅剪枝+热启动**：保持教师宽度，无需处理维度不匹配，直接复制非块权重，极大简化蒸馏过程。
  - **分阶段门控策略**：每个剪枝步骤后通过WER检查，防止质量不可逆下降，保证渐进恢复。
  - **轻量评估指标**：用ASR-WER替代主观听测，客观可重复。
  - **实用部署经验**：详细记录了三个训练/推理不一致性的发现和修复，对社区有参考价值。

- **实验设计亮点**：
  - 剪枝容忍度预先分析，避免了盲目剪枝。
  - 独立FLEURS基准覆盖多个维度（WER、CER、SIM、UTMOS、RTF），且与教师和当前最佳小模型对比。
  - 公开了模型权重和每句基准结果（Hugging Face），促进可复现性。

## 8. 不足与局限

- **实验覆盖不足**：
  - 仅针对印地语一种语言，泛化性未知。
  - 数据预算单一（17.6小时），未探索更大数据量下是否会缓解102M容量悬崖或进一步提升小模型质量。
  - 未进行人类MOS评估，仅依赖预测自然度模型（UTMOS），可能存在偏差。
  - 评估句子数有限：门控阶段仅两个句子（每个两次生成），独立基准仅50句，统计稳定性有限。
  - 未与其他蒸馏方法（如logit蒸馏、特征蒸馏）对比，无法判断本方法的相对优势。
  - 未测试不同剪枝模式（如随机选取块 vs 均匀选取）对比。

- **偏差风险**：
  - ASR WER评估依赖于特定的ASR模型（IndicWav2Vec/Vakyansh），可能对某些发音或变体有偏。
  - 训练数据来自教师模型的合成音频，可能引入教师本身的偏差和错误，学生无法超越教师上限。

- **应用限制**：
  - 生成音频末尾有残余伪影（能量修剪但不完全消除），句首令牌质量不稳定（是参考条件流匹配TTS的已知问题）。
  - 102M模型在长句上表现差，仅适合短话语。
  - 教师模型本身（IndicF5）可能未开放所有训练细节，复现依赖其特性。
  - 部署时需要冻结特征提取库和数学库版本，否则可能静默降质，增加了迁移难度。

（完）
