---
title: Large Audio Language Models for Spoofing-Aware Speaker Verification
title_zh: 面向欺骗感知说话人验证的大规模音频语言模型
authors: "Sofya Savelyeva, Mariia Perunova, Evgeny Kushnir, Artem Dvirniak, Dmitrii Korzh, Oleg Y. Rogov"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.14753v1"
tags: ["query:speech-audio"]
score: 8.0
evidence: 使用大型音频语言模型进行欺骗感知说话人验证，应对TTS和语音克隆威胁。
tldr: 文本转语音和语音克隆技术的进步使得高质量欺骗低成本可扩展，严重威胁自动说话人验证系统。现有防御主要依赖二分类反欺骗检测或模块化融合的欺骗感知说话人验证流水线。本文首次系统评估大型音频语言模型在欺骗感知说话人验证中的表现，采用零样本提示、监督适应、推理导向训练和强化学习优化。结果表明零样本下性能接近随机，但任务特定适应后与传统级联系统竞争力相当。工作表明大型音频语言模型可作为统一、可审计的说话人验证基础，同时揭示传统方法仍具优势。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.14753v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 923, \"height\": 543}, {\"url\": \"assets/figures/arxiv/2607.14753v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 873, \"height\": 707}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.14753v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 544, \"height\": 249}, {\"url\": \"assets/tables/arxiv/2607.14753v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1406, \"height\": 345}, {\"url\": \"assets/tables/arxiv/2607.14753v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 910, \"height\": 230}]"
motivation: 现有欺骗感知说话人验证系统多采用模块化融合或级联流水线，缺乏自然语言推理能力，大型音频语言模型的潜力尚未探索。
method: 系统评估四种策略：零样本提示、监督微调、推理导向训练和强化学习优化，对比传统流水线。
result: 零样本下大型音频语言模型接近随机；任务特定适应后性能与传统竞争系统相当，多条路径可达。
conclusion: 大型音频语言模型可作为统一、可审计的欺骗感知说话人验证基础，但传统级联系统在部分场景仍领先。
---

## 摘要
文本转语音和语音克隆的最新进展使得高质量欺骗变得廉价且可扩展，威胁到语音认证系统，尤其是自动说话人验证（ASV）。现有防御主要通过用于深度伪造检测的二元反制措施（CM）或欺骗感知说话人验证（SASV）来应对这一威胁，当前系统以模块化ASV-CM融合和级联流水线为主。尽管大规模音频语言模型（LALM）在包括CM和ASV在内的相关音频任务上展现出潜力，但其用于SASV的情况尚未被探索，尽管它们能够生成自然语言理由以进行审计，并且具有超越判别性预测的鲁棒性。本文系统评估了LALM在SASV任务中相对于传统流水线的表现，涉及零样本提示、监督适应、面向推理的训练以及基于强化学习的优化。我们的结果表明，预训练LALM在零样本设置下几乎为随机水平，证实它们并非天然适合SASV，但任务特定的适应缩小了这一差距。我们进一步发现，可以通过几种不同的途径实现具有竞争力的SASV性能。这些发现将LALM定位为统一SASV的有前景且可审计的基础，同时澄清了传统级联系统仍然领先的领域。

## Abstract
Recent advances in text-to-speech and voice cloning make high-quality spoofing inexpensive and scalable, threatening voice authentication systems, especially automatic speaker verification (ASV). Existing defenses mainly address this threat through binary countermeasures (CMs) for deepfake detection or spoofing-aware speaker verification (SASV), where current systems are dominated by modular ASV-CM fusion and cascaded pipelines. Although large audio language models (LALMs) have shown promise on related audio tasks, including CM and ASV, their use for SASV remains unexplored, despite their capacity to produce natural-language rationales for auditing and robustness beyond discriminative predictions. This work systematically evaluates LALMs for SASV against conventional pipelines under zero-shot prompting, supervised adaptation, reasoning-oriented training, and reinforcement-learning-based optimization. Our results show that pretrained LALMs are near chance in the zero-shot setting, confirming that they are not natively suited to SASV, but that task-specific adaptation closes this gap. We further find that competitive SASV performance can be achieved through several distinct routes. These findings position LALMs as a promising and auditable foundation for unified SASV, while clarifying where conventional cascade systems still lead.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：文本转语音（TTS）和语音克隆技术的最新进展使得高质量合成语音的生成成本低廉、易于规模化，严重威胁语音认证系统，特别是自动说话人验证（ASV）。现有防御主要依赖两类方案：一是二分类反欺骗检测（CM），仅区分真实/伪造；二是欺骗感知说话人验证（SASV），要求系统同时拒绝冒充者和伪造语音。当前SASV系统以模块化ASV-CM分数融合或级联流水线为主流范式，缺乏自然语言层面的可审计性和推理能力。
- **核心问题**：大规模音频语言模型（LALM）已在CM和ASV等独立任务上展现潜力，但它们在统一的SASV任务上的表现尚未被探索。本文首次系统评估LALM在SASV中的适用性，研究能否通过适应策略使其达到或超越传统级联系统。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **任务定义**：将SASV表述为三分类决策问题：输入一对音频（注册语音enrollment和待验证语音trial），输出标签target（真实目标说话人）、non-target（冒充者）或spoof（伪造）。
- **核心思想**：利用LALM的音频理解和指令跟随能力，通过不同适应策略将其改造为端到端SASV系统，并引入复合训练目标和推理监督。
- **关键技术细节**：
  - **模型选择**：SALMONN-7B（Whisper+BEATS编码器+Vicuna LLM）和Qwen2-Audio-7B。SALMONN通过波形拼接处理双音频，Qwen支持多音频输入。
  - **参数高效微调**：使用LoRA（秩128, α=256, dropout 0.05）对注意力层进行微调，同时训练音频编码器。
  - **复合损失函数**：
    - \(L_1\)：三分类交叉熵（SASV决策）。
    - \(L_2\)：附加角度间隔（AAM）损失，应用于真实样本的说话人区分，避免将伪造语音拉入说话人流形。
    - \(L_3\)：二分类交叉熵，用于欺骗检测（bona fide vs spoof）。
    - 总损失 \(L = \lambda_1 L_1 + \lambda_2 L_2 + \lambda_3 L_3\)，权重为1.0、0.5、1.0。
  - **硬样本挖掘**：随着训练推进，采样器逐步聚焦于困难对（相同说话人但低相似度、不同说话人但高相似度、靠近注册说话人的伪造语音）。
  - **推理导向训练**：
    - **CoT-SFT**：利用Step-Audio R1生成推理轨迹，经正确性和自一致性过滤（Jaccard相似度≥0.35）后得到约90k对带推理标注的数据，微调模型输出包含`<think>`、`<reasons>`、`<answer>`的格式化响应。
    - **GRPO**：在CoT-SFT基础上应用组相对策略优化，奖励正确答案和格式合规性。
  - **评分转换**：从模型输出token的后验概率中提取接受分数 \(s_{sasv} = p_{target}\)，用于计算EER和a-DCF。

## 3. 实验设计

- **数据集**：
  - 训练：ASVspoof 5训练集 + VoxCeleb中的真实语音（补充说话人身份多样性），构建约180万对训练对（涵盖500个说话人）。
  - 评估：ASVspoof 5评估集（按Trakc 2协议，采用open condition），由于计算开销，从完整评估集中按原始类别分布采样20k对。
- **基准（baseline）**：
  - ECAPA2 + WavLM分数融合（复现自ASVspoof 5顶级方案[50]）。
  - ECAPA2 + W2V2-AASIST CM模型，融合方式为决策树或固定阈值。
- **对比方法**：零样本LALM、LoRA SFT、各项消融配置、推理模型（CoT-SFT、GRPO），以及数据量匹配的硬标签SALMONN（90k对）。
- **评价指标**：
  - 总体准确率（Acc）、ASV准确率（ASV Acc）、CM准确率（CM Acc）。
  - 等错误率（EER）、最小架构无关检测代价函数（min a-DCF，参数：\(\pi_{tar}=0.9405, \pi_{non}=0.0095, \pi_{spf}=0.05, C_{miss}=1, C_{fa}=10\)）。
  - 注意：推理模型仅报告准确率，因CoT输出导致分数校准不佳。

## 4. 资源与算力

- **未明确说明具体GPU型号、数量和训练时长**。文中仅提到训练配置：使用AdamW优化器，学习率1e-5，1000步预热，有效批量大小1024，训练10–20个epoch。模型为7B参数级别（SALMONN-7B, Qwen2-Audio-7B），LoRA训练通常需要多GPU（如8×A100），但本文未提供具体细节。
- **需注意**：由于LALM推理计算成本高，评估仅用20k对采样，而非完整测试集。

## 5. 实验数量与充分性

- **实验组数**：
  - 零样本vs适应对比（表I）：2个模型×2种设置。
  - 消融研究（表II）：SALMONN-7B上的5个配置（LoRA SFT、+AAM头、+欺骗检测头、+硬样本挖掘、对照基线）。
  - 推理模型对比（表III）：4种方法（常规融合、硬标签、CoT-SFT、GRPO），均在90k小规模数据上训练。
  - 共约10组主要对比实验。
- **充分性与公平性**：
  - 消融充分：逐步添加各组件，清晰显示trade-off。
  - 对照公平：基线在相同评估协议下复现；推理模型与硬标签在相同数据量上对比。
  - **局限**：仅使用ASVspoof 5一个评估集（尽管是公认困难benchmark），未在SASV 2022、SpoofCeleb等上报告结果；使用单enrollment（比官方三enrollment更苛刻）；仅20k采样可能引入偏差；推理模型未进行完整指标评估。

## 6. 论文的主要结论与发现

- **RQ1**：预训练LALM无法零样本完成SASV（准确率接近随机27%-44%），但经过LoRA SFT适应后可提升至85%以上，证明适应是必要的。
- **RQ2**：适应后的LALM系统与强传统基线（ECAPA2+WavLM融合）在总体准确率和a-DCF上达到甚至超越，但在EER（等错误率）上传统系统仍更优（0.13 vs 0.22），说明LALM的分数校准不如传统方法。
- **RQ3**：SASV适应存在ASV与CM任务的trade-off。单一LoRA SFT对CM好但对ASV差；加入AAM头提升ASV但降低CM；加入欺骗检测头+硬样本挖掘后取得最佳平衡（Acc 89.30%, min a-DCF 0.19）。
- **RQ4**：推理导向训练（CoT-SFT和GRPO）在90k数据上未超越硬标签SFT（Acc 83.92% vs 86.02%），但GRPO在CM准确率上最高（89.04%），且能产生可解释推理轨迹。需更大规模、更高质量标注才能发挥推理优势。

## 7. 优点

- **首次将LALM系统用于SASV**，填补了领域空白。
- **系统化的适应策略对比**：覆盖零样本、多种监督训练、推理训练和强化学习，分析全面。
- **设计精巧的复合损失**：结合三分类、说话人区分（AAM）和欺骗检测，并针对真实样本单独施加AAM以避免伪造语音污染流形。
- **硬样本挖掘**有效提升决策质量，思路值得借鉴。
- **推理轨迹自动标注+过滤**：利用强模型（Step-Audio R1）和外部特征（CoLMbo、emotion2vec、Parselmouth）生成并筛选，为LALM可解释性探索提供参考。
- **开源潜力**：论文结构清晰，实验可复现（虽未提供代码，但细节充分）。

## 8. 不足与局限

- **计算开销巨大**：LALM推理和训练成本远高于传统融合模型，难以实际部署。
- **评估覆盖有限**：
  - 仅使用ASVspoof 5一个数据集，未在SASV 2022、SpoofCeleb、WildSpoof等多场景验证。
  - 仅采用单enrollment，与官方三enrollment标准不符，可能低估实际性能。
  - 测试仅采样20k对，完整集统计性质未知。
- **推理质量未验证**：生成的CoT轨迹仅通过自动过滤，缺乏人工评估，其可解释性和忠实性存疑。
- **推理模型性能未超越硬标签**：作者归因于高质量推理数据规模有限（仅90k），且模型可能产生“推理税”（reasoning tax），即生成推理损害核心判别能力。
- **分数校准问题**：LALM决策token的后验概率缺乏校准，导致EER偏高，而a-DCF（代价函数）更优，说明其适合特定代价场景但通用性不足。
- **基线复现差异**：官方顶级系统[50]因代码未公开而自行复现，性能与官方不完全一致，对比需谨慎。

（完）
