---
title: "SoundscapeAgent: Agentic Soundscape Construction for Controllable Synthesis and Scalable Audio-Language Supervision"
title_zh: SoundscapeAgent：基于代理的声音场景构建以实现可控合成与可扩展的音频-语言监督
authors: "Hao Zhang, Yiwen Zhao, Yixuan Zhang, Yiwen Shao, Steve Yves"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.21857v1"
tags: ["query:speech-audio"]
score: 8.0
evidence: 可控文本到音频生成框架
tldr: 现有文本到音频合成模型隐式处理场景规划与渲染，缺乏可控性和可解释性。SoundscapeAgent框架利用LLM代理显式执行场景规划、源选择、时间布局和逐步渲染，支持用户通过工具选择和场景编辑进行交互。生成性能与主流基线相当，且使用代理生成数据训练的模型在下游音频推理任务中持续超越仅使用真实数据的基线。该框架为可控音景合成和规模化音频-语言数据构建提供了可检查、可复用的解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.21857v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1861, \"height\": 1344, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21857v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1816, \"height\": 612, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.21857v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 934, \"height\": 659, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21857v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1853, \"height\": 255, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21857v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1680, \"height\": 348, \"label\": \"Table\"}]"
motivation: 现有文本到音频模型隐式处理场景规划，缺乏可控性和可解释性，难以满足复杂音景构建需求。
method: 基于LLM的agent框架，将用户意图转为可执行场景计划，通过检索与生成获取音频资产，可控渲染多事件混合并导出结构化元数据。
result: 客观指标和听众研究显示生成质量与基线竞争；agent数据训练模型在下游音频推理中持续优于真实数据基线。
conclusion: 提供可检查、可复用的音景合成方法，支持人在回路交互，有效提升可控生成与下游任务性能。
---

## 摘要
我们提出了一个基于代理的声音场景构建框架，用于可控的合成式音频生成，该框架明确地处理了通常由单次文本到音频模型隐式处理的场景规划、声源选择、时间布局和渲染步骤。基于LLM的代理将用户意图转化为可执行的场景计划，通过检索和按需生成获取素材，渲染可控的多事件混合，并导出对齐的场景元数据。该框架还支持通过用户引导的工具选择和可编辑的场景计划进行人在回路交互。这些组件共同提供了一种可检查且可重用的方法，用于可控的声音场景合成和可扩展的音频-语言数据构建。听众研究和客观指标表明，与文本到音频基线相比，生成性能具有竞争力，而使用代理生成数据训练的模型在下游音频推理中始终优于仅使用真实数据的基线。代码、演示和听力测试结果可在 https://haozhang6720.github.io/SoundscapeAgentDemoPage/ 获取。

## Abstract
We present an agentic soundscape construction framework for controllable compositional audio generation that makes explicit the scene planning, source selection, temporal layout, and rendering steps typically handled implicitly by single-shot text-to-audio models. An LLM-based agent converts user intent into an executable scene plan, acquires assets through retrieval and on-demand generation, renders controllable multi-event mixtures, and exports aligned scene metadata. The framework also supports human-in-the-loop interaction through user-guided tool selection and editable scene plans. Together, these components provide an inspectable and reusable approach to controllable soundscape synthesis and scalable audio-language data construction. Listener studies and objective metrics demonstrate competitive generation performance against text-to-audio baselines, while models trained with agent-generated data consistently outperform real-only baselines in downstream audio reasoning. Code, demos, and listening-test results are available at https://haozhang6720.github.io/SoundscapeAgentDemoPage/.

---

## 论文详细总结（自动生成）

# SoundscapeAgent 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：当前主流的单次文本到音频生成模型（如 AudioLDM、TangoFlux 等）将场景规划、声源选择、时间布局和渲染等步骤隐式处理，导致生成过程不透明、难以检查、编辑或重用于大规模标注数据构建。声学场景本质上是组合的、时序的、多声源的，但现有系统缺乏对事件组合、源角色和时序组织的显式建模。
- **研究动机**：音频-语言模型需要更丰富的结构化监督数据（如事件组成、时间关系），而手动标注难以规模化。同时，可控合成需求日益增长。作者旨在通过代理（agent）框架显式揭示场景结构，实现可控合成和可扩展的音频-语言数据生产。
- **整体含义**：提出一个可检查、可重用的代理框架，将生成分解为可解释的阶段，既支持直接生成质量评估，也提供结构化训练信号提升下游音频推理。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将组合式声音场景生成视为由 LLM 驱动的代理构建问题，而非单次文本到波形映射。代理显式规划场景、获取资产、渲染并导出对齐的元数据。
- **关键技术细节**：
  - **四阶段流程**：
    1. **多模态意图解析与可执行场景规划**：代理将用户条件（文本/图像）转化为结构化场景计划 $E = \{e_i\}_{i=1}^N$，每个事件 $e_i$ 包含：事件标签 $\tau_i$、场景角色 $r_i \in \{\text{bg}, \text{mid}, \text{fg}\}$、起始时间 $t_i^{\text{start}}$、持续时间 $d_i$、选定的资产 $s_i$、相对响度 $\ell_i$、可选渲染控制 $\phi_i$。
    2. **混合资产获取**：资产库 $L = L_{\text{open}} \cup L_{\text{priv}} \cup L_{\text{gen}}$。使用 CLAP 相似度（阈值 >0.32）和生产复杂度（PC 分数 <4）过滤。三种获取模式：模糊检索（用于宽泛环境音）、严格检索（用于语义明确事件）、按需生成（当检索失败时，用户可选择 TTA 后端）。生成资产加入 $L_{\text{gen}}$ 复用。
    3. **结构化渲染**：确定性渲染器执行事件渲染 $y(t) = \sum_i \alpha_i R(e_i)(t - t_i^{\text{start}})$，包括裁剪、循环/扩展、响度归一化、淡入淡出、空文件重试等。支持用户引导的编辑（替换、添加、修改轨迹）。
    4. **元数据导出与描述生成**：导出元数据 $m = M(u, E, y)$，并使用 LLM 或固定格式生成对齐描述 $d = D(u, E, m)$。
  - **离线先验模式**：用于大规模数据构建，代理离线推断可复用场景先验 $\pi$，然后轻量采样、渲染并生成描述，无需每次在线推理，提高吞吐量。
- **公式与算法流程**：代理过程 $(\mathcal{E}, \mathcal{A}, \mathbf{c}) = \mathcal{A}(u)$；渲染过程如上；离线模式 $E \sim p(E|\pi), y = \mathcal{R}(E), m = \mathcal{M}(E, y), d = \mathcal{D}(E, m)$。

## 3. 实验设计：数据集、基准、对比方法

- **Track A（生成质量评估）**：
  - 评估场景：5 类——AudioCaps 标准提示、环境音密集、事件丰富、时间指定、情感/抽象。每类 8 个提示，共 40 个提示，生成 10 秒音频，总计 160 个样本（4 个系统 × 40 提示）。
  - 基准对比方法：单次文本到音频模型 TangoFlux、EzAudio、AudioLDM 2。
  - 指标：人类对齐评分（5 分制，22 名参与者，每样本 10 个评分）；CLAP 相似度作为辅助指标。
- **Track B（数据效用与音频推理）**：
  - 训练数据：真实数据混合（CaptionStew-400k + 173k 副语言示例）。代理生成数据：50k、100k、200k 子集。
  - 下游模型：冻结 Qwen2.5-Omni 音频编码器 + 可训练 2 层 MLP 投影器（约 38.5M 参数）+ 冻结 Qwen2.5-7B-Instruct。使用指令无关对齐训练（配方来自 [40]）。
  - 评估基准：MMAU test-mini（1000 个音频问答样例，涵盖 Sound、Speech、Music）。
  - 指标：总体准确率、任务级准确率、难度级准确率（Easy/Medium/Hard）。

## 4. 资源与算力

- 论文中未明确说明使用的 GPU 型号、数量及训练时长。
- 仅提及训练使用 AdamW 优化器、峰值学习率 $10^{-3}$、余弦衰减、bf16 混合精度。所有实验在相同架构下进行，训练迭代上限 30 万步。
- 结论：算力信息不透明，无法评估资源需求。

## 5. 实验数量与充分性

- **Track A**：4 个系统 × 5 类场景 × 8 提示 = 40 次生成，每样本 10 个独立评分，总计 22 名参与者。实验覆盖多种场景类型，但提示数量较少（每类仅 8 个），统计显著性有限。人类评估设计较为合理（平衡分配、重复评分）。
- **Track B**：一个基线 + 3 个消融（50k/100k/200k 代理数据），每个条件在 3 个检查点（200k/250k/300k 步）取最佳。评估在 MMAU test-mini 上进行，包含任务和难度细分。实验比较充分，但仅使用单一下游任务（音频推理）和单一模型架构，泛化性需更多验证。
- **总体充分性**：实验设计覆盖了生成质量（主观+客观）和下游效用两条主线，消融比较了数据量影响，但缺少更多领域（如音乐、语音）的深度分析。Track A 的 CLAP 指标论文自认不完美捕捉组合特性，依赖人类评估，但提示量少可能带来偏差。Track B 的增益显著但非单调，需要更多解释（如数据质量、平衡问题）。整体上实验数量合理，公平性较好（系统间对比，使用相同协议），但全面性有提升空间。

## 6. 论文的主要结论与发现

- **Track A**：SoundscapeAgent 在整体对齐评分上优于所有单次基线（3.63 vs. TangoFlux 3.41、EzAudio 2.98、AudioLDM 2 2.91）。最大优势在情感/抽象（+0.64）、环境音密集（+0.45）和时间指定（+0.08）场景。事件丰富场景略低于最佳基线（0.11 差距）。在常规 AudioCaps 提示上保持竞争力。
- **Track B**：使用代理生成数据增强训练可提升 MMAU 准确率：+50k → 53.30%，+100k → 56.50%（最佳），+200k → 55.40%，而仅用真实数据为 51.05%。最大任务增益在 Sound（+7.63 点），最大难度增益在 Hard（+8.90 点）。增益非单调，最优点为 100k。子任务分析显示增益集中在事件推理、生态声学、时间推理等结构化能力上，同时向音乐推理迁移（如音乐流派+20.59），但语音任务提升有限。
- **核心结论**：代理框架可控生成质量可比或优于现有基线，且其结构化输出能有效提升音频推理模型性能，尤其在需要组合、时序和上下文理解的任务上。

## 7. 优点：方法或实验设计上的亮点

- **方法创新**：将场景规划、资产获取、渲染显式分离，提供可检查、可编辑、可复用的流程。支持多模态输入和人在回路交互。
- **离线先验模式**：兼顾代理规划的灵活性与大规模数据构建的效率。
- **实验设计双轨制**：同时评估生成质量和数据效用，比单一轴心评估更全面。
- **Track B 的消融设计**：控制真实数据混合不变，仅变化合成数据量，干净地分离了合成数据的影响。
- **开源可用**：提供代码、演示和听力测试结果，促进可复现研究。

## 8. 不足与局限

- **对资产库和生成器质量的依赖**：框架性能受限于开源录音质量和按需生成模型的鲁棒性。域间差距（合成 vs 真实音频）可能影响数据效用。
- **实验覆盖局限**：Track A 提示数量少（每类8个），统计力受限；Track B 仅评估单一架构（Qwen2.5系列）和单一基准（MMAU），未涉及其他音频理解任务（如事件检测、定位）。MMAU 可能未覆盖所有结构化信息维度，因此报告结果可能低估合成数据价值。
- **资源信息缺失**：未提供 GPU 型号、数量、训练时间，难以评估可复现性和实际成本。
- **结果非单调性**：200k 数据不如 100k 的原因未深入分析（如数据冗余、质量下降或真实/合成平衡问题），需要更多诊断实验。
- **语音和音乐覆盖不足**：资产库以非语音事件为主，导致语音任务增益有限，未来需扩展。
- **评价指标偏差**：CLAP 相似度在 Track A 中未被视为主指标，但人类评估受限于提示数量和参与者样本（22人，每样本10次），可能存在评分者间差异。

（完）
