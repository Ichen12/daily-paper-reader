---
title: Memory Efficient Audio Synthesis with Decoupled Temporal Depth Diffusion Transformers
title_zh: 基于解耦时间深度扩散Transformer的高效内存音频合成
authors: "Dongseong Hwang, Prasanth Yadla, Kaan Elgin, Shifas Padinjaru Veettil, Sivanand Achanta, Dipjyoti Paul, Ramya Rasipuram, Tyler Johnson, Emad Soroush, Chung-Cheng Chiu, Zhifeng Chen"
date: 2026-07-26
pdf: "https://arxiv.org/pdf/2607.23811v1"
tags: ["query:speech-audio"]
score: 9.0
evidence: 从语义标记进行设备端语音合成
tldr: 设备端实时语音合成面临严格内存和计算限制。提出解耦时间与深度处理的扩散Transformer架构，通过因果滑动窗口注意力和单个深度解码器生成所有RVQ级别，将内存复杂度降为常数。在AMX上达到16倍实时速度，峰值内存仅21MB，支持连续合成320秒音频。相比先前系统，MOS提升0.28，实现了高效高保真合成。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23811v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1732, \"height\": 585, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23811v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 785, \"height\": 883, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23811v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 796, \"height\": 872, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23811v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 827, \"height\": 912, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23811v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 788, \"height\": 881, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23811v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 799, \"height\": 877, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23811v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 790, \"height\": 872, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.23811v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1077, \"height\": 286, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23811v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1756, \"height\": 572, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23811v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1353, \"height\": 326, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23811v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1718, \"height\": 309, \"label\": \"Table\"}]"
motivation: 在设备上实现高保真实时语音合成，需要克服内存和计算的严格限制。
method: 解耦时间与深度处理的扩散Transformer，使用因果滑动窗口注意力和单个深度解码器逐步生成所有RVQ级别。
result: 16倍实时合成，峰值内存21MB，连续合成320秒音频，MOS提升0.28。
conclusion: 架构以常数小内存替代线性/二次缩放，验证了关键组件，实现了高效高保真合成。
---

## 摘要
Siri 表现力语音利用苹果最强大的设备端基础模型 AFM 3 Core Advanced，实时且完全在设备上合成丰富、可配置的语音。本文介绍了该能力背后内存高效的音频合成架构：一种去令牌化器，将基础模型发出的语义音频令牌在苹果矩阵协处理器（AMX）严格的计算和内存预算内转换为高保真音频。我们通过三组件设计将语义音频令牌转换为残差向量量化（RVQ）表示：流式编码器、时间解码器和深度解码器，系统性地解耦了时间与深度处理。一个可重用的单个深度解码器，采用扩散Transformer（DiT）式阶段条件化，自回归地生成所有RVQ层级，取代了先前多解码器架构中专用的逐层级解码器，同时因果滑动窗口注意力结合固定窗口键值缓存实现了与序列长度无关的恒定内存复杂度。部署在AMX上，该去令牌化器每生成步骤约持续10毫秒，比实时快约16倍，峰值运行时内存仅21 MB，设备端资产329 MB，支持连续流式合成20-320秒的音频。这种恒定的小内存占用取代了传统基于Transformer和GAN方法中的线性和二次内存扩展。消融研究验证了关键架构组件，音频质量评估确认该架构在保持合成保真度的同时实现了相对于现有方法的效率提升。在AFM 3 Core Advanced中工作的10亿参数激活规模下，相比于之前的设备端文本转语音系统，它在整体平均意见得分上提高了+0.28（4.15对3.87），在对话语音上提高了+0.42（4.24对3.82）。

## Abstract
Siri Expressive Voices synthesize rich, configurable speech in real time and entirely on device, powered by AFM 3 Core Advanced, Apple's most powerful on-device foundation model. This work presents the memory-efficient audio synthesis architecture behind that capability: a detokenizer that converts the semantic audio tokens emitted by the foundation model into high-fidelity audio within the tight compute and memory budget of the Apple Matrix Coprocessor (AMX). We convert semantic audio tokens to a residual vector quantization (RVQ) representation with a three-component design, a streaming encoder, a temporal decoder, and a depth decoder, that systematically decouples temporal and depth processing. A single reusable depth decoder with Diffusion Transformer (DiT)-style stage conditioning generates all RVQ levels autoregressively, replacing the dedicated per-level decoders of prior multi-decoder architectures, while causal sliding window attention with fixed-window key-value caching yields constant memory complexity independent of sequence length. Deployed on the AMX, the detokenizer sustains roughly 10 ms per generation step, about 16x faster than real time, with a peak runtime memory of only 21 MB and 329 MB of on-device assets, enabling continuous streaming synthesis of 20-320 seconds of audio. This constant, small footprint replaces the linear and quadratic memory scaling of conventional transformer- and GAN-based approaches. Ablation studies validate the key architectural components, and audio quality assessment confirms that the architecture maintains synthesis fidelity while achieving efficiency gains over existing methods. Operating at a 1-billion-parameter activation size within AFM 3 Core Advanced, it improves Mean Opinion Score by +0.28 overall (4.15 vs. 3.87) and by +0.42 on conversational speech (4.24 vs. 3.82) over the prior on-device text-to-speech system.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义

**研究动机**：在设备端（如手机、平板）进行高保真、可配置的实时语音合成面临严格的内存与计算预算限制。传统的Transformer和GAN方法存在线性或二次内存扩展，无法支持长序列音频的流式合成。

**核心问题**：如何将基础大语言模型（如AFM 3 Core Advanced）输出的语义音频令牌高效地转换为高保真音频，同时满足设备端协处理器（Apple AMX）的内存和延迟约束。

**整体含义**：提出一种内存高效的音频合成架构，作为“Siri Expressive Voices”的音频合成器。该架构通过解耦时间与深度处理、采用固定窗口键值缓存，实现了与序列长度无关的恒定内存复杂度，从而在资源受限的设备上实现实时、流式、高保真语音合成。

## 2. 方法论

- **核心思想**：设计一个三组件架构（流式编码器、时间解码器、深度解码器），系统性解耦时间维度和深度维度的处理。深度解码器在所有残差向量量化（RVQ）层级之间完全共享参数，通过扩散Transformer（DiT）式阶段条件化（rotary positional encoding标识码本书索引）替代传统的逐层级专用解码器。

- **关键技术细节**：
  - **流式编码器**：使用因果滑动窗口自注意力处理语义令牌，提取条件隐变量。
  - **时间解码器**：基于DiT块，结合编码器条件和来自深度解码器的平均RVQ嵌入（`q̅t`），预测下一个时间帧的表示。
  - **深度解码器**：复用同一组参数（2个DiT块），对每个时间帧自回归生成K个RVQ级别。每个级别输入初始化为可学习的BOS令牌，通过旋转位置编码注入码本级索引（`RoPE(yt, k)`）。深度解码器输出RVQ令牌（`rt,k`）及其嵌入（`qt,k`），嵌入的均值反馈给时间解码器。
  - **因果滑动窗口注意力**：保证因果性且支持长序列，固定窗口大小经实验确定为128。
  - **固定窗口键值缓存**：使运行时内存与序列长度完全无关（恒定内存复杂度）。
  - **时间前瞻（lookahead）机制**：为编码器提供未来的语义令牌（6帧，约240ms）以获得更好语境。

- **公式/算法流程**（文字描述）：  
  编码器 `h_t = f_θ(x_t)`；  
  时间解码器 `y_t = g_ϕT(z_t^T, h_t)`；  
  给 `y_t` 加上旋转位置编码得 `y_t,k = RoPE(y_t, k)`；  
  深度解码器 `(r_t,k, q_t,k) = g_ϕD(z_t,k_D, y_t,k)`；  
  下一深度输入 `z_t,k+1_D = q_t,k`；  
  时间反馈 `q̅t = mean(q_t,1:K)`，下一时间输入 `z_t+1_T = q̅t`。  
  起始令牌为BOS。

## 3. 实验设计

- **数据集**：内部录制的语音助手交互数据集（大量小时数，具体组成因保密未公开）。
- **Benchmark方法**：
  - 基线模型：Moshi/Mimi解码器、内部Transformer解码器、内部自回归GAN方法（类似Chunked Autoregressive GAN）。
  - 评估指标：词汇音素可区分性（within-speaker ABX错误率）、感知质量（VisQOL、MOSNet）、信号失真比（SI-SNR）、UTMOS（无参考MOS）。
  - 生产环境：通过众包平均意见得分（MOS）对比先前生产系统，覆盖Siri、对话、导航三类场景，5个生产语音。
- **对比条件**：跨架构算法缩放实验（表2）在单张NVIDIA H100 GPU上以单位批量进行，比较生成时间、内存、实时因子（RTF）随序列长度的缩放行为。

## 4. 资源与算力

文中**未明确说明训练所使用的算力**（GPU型号、数量、训练时长等）。仅在算法缩放对比实验（表2）中提及使用单张NVIDIA H100 GPU（单位批量），但该实验仅用于隔离各架构的缩放行为，并非训练设置。推断训练是在苹果内部集群上进行，具体细节因保密未公布。

## 5. 实验数量与充分性

- **消融实验**（表3）：6组配置，依次添加“时间前瞻”、“统一深度解码器”、“DiT条件化”、“输出归一化”、“固定KV缓存”，验证每个组件的贡献。
- **其他分析实验**：
  - 条件化方法比较（DiT vs. 交叉注意力）及交叉注意力窗口大小的影响（图2）。
  - 时间前瞻窗口大小（1～24帧）和滑动窗口大小（32～256）的扫描（图3）。
  - 层深度比分析（4-6-2最优）和输出归一化的影响（图4）。
  - 设备端性能测量（表1）和算法缩放比较（表2）。
  - 音频质量评估（表4）：UTMOS、SI-SNR对比基线。
  - 生产环境MOS评估（5个语音×3域）。
- **充分性**：实验覆盖了主要设计选择（架构组件、超参数、缩放行为、质量评估），充分验证了方法有效性。主观评估（MOS）和客观指标（ABX、UTMOS等）结合，结果可信。但训练数据未公开，且与Moshi的参数量化对比不够直接，可能存在可复现性不足。

## 6. 主要结论与发现

- 提出的三组件解耦架构实现了**恒定内存和恒定计算时间**，与音频序列长度无关：在AMX上每步约10ms（16倍实时），峰值运行时内存仅21MB，支持20-320秒音频的流式合成。
- 相比Moshi等基线，该方法在内存缩放上具有显著优势（常数 vs. 线性/二次）。
- 音频质量评估：UTMOS达3.97（Moshi为3.92），SI-SNR达9.47（Moshi为7.45）。ABX错误率5.3%（Moshi解码器约7%重建？原文给出Mimi性能，但未直接对比ABX）。
- 生产MOS：整体MOS 4.15 vs. 前期系统3.87（+0.28），对话语音MOS 4.24 vs. 3.82（+0.42），导航音频100%干净。
- 消融确认了所有组件的有效性：时间前瞻、统一深度解码器、DiT条件化、输出归一化、固定KV缓存都带来显著改进。

## 7. 优点

- **内存效率革命性提升**：常数内存复杂度彻底克服了传统方法在长序列上的扩展瓶颈，使得设备端高保真长音频合成成为可能。
- **架构设计精巧**：通过DiT式阶段条件化实现单个深度解码器全参数共享，消除了Moshi中保留的逐层级输入/输出投影，进一步节省参数和计算。
- **部署验证**：实际产品（Siri Expressive Voices）中验证了性能，MOS提升明显，实用性极强。
- **消融系统全面**：系统性地分离了各个设计选择的影响，为未来研究提供可靠指导。
- **多种质量评估**：既包含客观指标（ABX、SI-SNR、UTMOS），又包含主观MOS，评估全面。

## 8. 不足与局限

- **数据与训练细节未公开**：训练数据集、超参数搜索细节、训练算力均未披露，限制了复现性和生态贡献。
- **与Moshi的对比不够充分**：论文声称完全共享参数优于Moshi，但未提供参数量化对比（如参数总数、MACs），也未在同一硬件上直接比较延迟和内存（仅在H100上比较算法缩放，但Moshi用于AMX的延迟未知）。
- **通用性有限**：针对苹果AMX优化，其他硬件平台（移动CPU、NPU）的迁移性未讨论。且依赖于特定语义令牌格式（AFM Core的输出），架构的通用性待验证。
- **未讨论失败案例**：未分析合成失败或退化的情形（如噪声、口吃、情感表达不足等），仅报告了平均分数。
- **ABX实验仅对比内部基线**：未与Moshi的ABX直接对比，削弱了音素保持优势的说服力。

（完）
