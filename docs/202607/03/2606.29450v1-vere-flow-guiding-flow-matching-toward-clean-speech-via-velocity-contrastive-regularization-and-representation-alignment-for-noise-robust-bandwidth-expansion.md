---
title: "VeRe-Flow: Guiding Flow Matching toward Clean Speech via Velocity Contrastive Regularization and Representation Alignment for Noise-Robust Bandwidth Expansion"
title_zh: "VeRe-Flow: 通过速度对比正则化与表示对齐引导流匹配以生成清洁语音，实现噪声鲁棒的带宽扩展"
authors: "Sujin Koo, Sangyoon Kim, Ji Sub Um, Hoirin Kim"
date: 2026-06-28
pdf: "https://arxiv.org/pdf/2606.29450v1"
tags: ["query:speech-audio"]
score: 8.0
evidence: 流匹配用于语音带宽扩展
tldr: 噪声鲁棒带宽扩展需从含噪低分辨率语音重建高保真宽带语音。现有流匹配方法在噪声输入下速度估计存在歧义。提出VeRe-Flow，通过速度对比正则化吸引预测速度趋向干净轨迹，并利用表示对齐对齐中间特征与干净自监督表示。在LSD和DNSMOS指标上超越所有基线，生成MOS最高。贡献在于引导流匹配生成干净语音，提升噪声环境下的带宽扩展性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.29450v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1739, \"height\": 542}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.29450v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 842, \"height\": 612}, {\"url\": \"assets/tables/arxiv/2606.29450v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 844, \"height\": 812}, {\"url\": \"assets/tables/arxiv/2606.29450v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 831, \"height\": 199}, {\"url\": \"assets/tables/arxiv/2606.29450v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 845, \"height\": 364}]"
motivation: 现有流匹配方法在噪声输入下速度估计存在歧义，导致难以恢复干净语音。
method: 提出VeRe-Flow，引入速度对比正则化吸引预测速度靠近干净轨迹，并利用表示对齐对齐中间特征与干净SSL表示。
result: 在LSD和DNSMOS OVRL上取得最低/最高值，在生成基线中MOS最高。
conclusion: 多级干净监督引导流匹配有效提升噪声鲁棒带宽扩展的语音质量。
---

## 摘要
噪声鲁棒的带宽扩展旨在从带噪低分辨率输入中重建高保真宽带语音。尽管流匹配在语音生成中表现出色，但由于噪声下速度估计的模糊性，从带噪输入中准确恢复清洁语音仍具挑战。本文提出VeRe-Flow，一种清洁引导的流匹配框架，引入多层级清洁监督以引导生成过程朝向清洁语音。在速度层面，我们引入速度对比正则化，吸引预测速度朝向清洁轨迹而远离噪声轨迹。在表示层面，我们融入表示对齐，将中间特征与清洁自监督学习表示对齐。结果表明，所提方法在所有基线中实现了最低的LSD和最高的DNSMOS OVRL，并在生成基线的MOS中达到最高。

## Abstract
Noise-robust bandwidth expansion aims to reconstruct high-fidelity wideband speech from noisy low-resolution inputs. While flow matching has shown strong performance in speech generation, accurately recovering clean speech from noisy inputs remains challenging due to the ambiguity of velocity estimation under noise. In this work, we propose VeRe-Flow, a clean-guided flow matching framework that introduces multi-level clean supervision to guide the generative process toward clean speech. At the velocity level, we introduce velocity contrastive regularization, which attracts the predicted velocity toward the clean trajectory while repelling it from noisy trajectories. At the representation level, we incorporate representation alignment that aligns intermediate features with clean self-supervised learning representations. The results demonstrate that the proposed method achieves the lowest LSD and highest DNSMOS OVRL among all baselines, and the highest MOS among generative baselines.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
噪声鲁棒的带宽扩展（Noise-Robust Bandwidth Expansion, NR-BWE）旨在从带噪的低分辨率输入（如8 kHz采样）中重建高保真宽带语音（如16 kHz）。传统BWE方法通常假设输入干净，在噪声环境下性能严重下降；而语音增强方法虽能抑制噪声，却无法修复缺失的高频成分。流的匹配（Flow Matching）在干净BWE中展现出强大的生成能力，但在带噪输入下，由于噪声导致速度估计模糊，生成轨迹容易偏离干净语音流形。为此，论文提出**VeRe-Flow**，通过引入多级干净监督（速度和表示层面）来引导生成过程朝向干净语音，实现同时进行带宽扩展与噪声抑制。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：在条件流匹配（CFM）框架基础上，加入双向速度对比正则化和表示对齐，使模型预测的速度场既靠近干净轨迹又远离噪声轨迹，并让中间表示与干净语音的自监督特征对齐。
- **关键技术细节**：
  - **条件流匹配基础**：定义时间依赖概率路径，通过线性插值生成中间样本 \(x_t = (1 - (1-\sigma_{\min})t)x_0 + t x_1\)，最优条件速度场 \(u_t^* = x_1 - (1-\sigma_{\min})x_0\)。模型学习条件速度场 \(v_\theta(x_t, t \mid x_{LR}, f_{\text{noisy SSL}})\)。
  - **速度对比正则化（VeCoR）**：定义干净目标速度 \(u_t^{\text{clean}}\) 和噪声目标速度 \(u_t^{\text{noisy}}\)，损失函数为 \(L_{\text{VeCoR}} = \mathbb{E}\left[ \|v_\theta - u_t^{\text{clean}}\|^2 - \lambda_{\text{VeCoR}}\|v_\theta - u_t^{\text{noisy}}\|^2 \right]\)，其中第一项即CFM目标，第二项提供排斥力。
  - **表示对齐损失（REPA）**：将模型第一个Transformer层的输出 \(h\) 通过3层MLP投影到SSL特征空间，与干净音频的XEUS帧级特征计算余弦相似度，最大化相似度：\(L_{\text{align}} = -\mathbb{E}\left[ \sum_t \text{sim}(f_{\text{clean SSL},t}, \phi(h_t)) \right]\)。
  - **模型架构**：采用DiC风格的1D卷积残差块（Conv ResBlock）和Transformer块的夹层结构（卷积前级→Transformer→卷积后级）。条件输入包含噪声低分辨率梅尔谱和经过时间对齐的噪声鲁棒SSL特征（来自XEUS），并融合噪声-鲁棒SSL条件。
  - **总损失**：\(L_{\text{total}} = L_{\text{VeCoR}} + \lambda_{\text{align}} L_{\text{align}}\)，其中 \(\lambda_{\text{VeCoR}}=0.05, \lambda_{\text{align}}=0.25\)。

### 3. 实验设计
- **数据集**：Valentini-Botinhao并行干净-嘈杂语料库（48 kHz采样）。使用84个说话人（合并28+56子集）训练，测试集含2个未见说话人、20种噪声条件。输入经Chebyshev Type-I低通滤波后下采样至8 kHz（训练时滤波参数随机，测试时固定）。
- **基准（Benchmark）**：与生成式基线（NU-Wave2†、FLowHigh†，均重新在NR-BWE设置下训练）以及非生成式方法（UEE、MTL MBE、EP-WUN等，结果来自原论文）比较。
- **评估指标**：对数谱距离（LSD）、DNSMOS（SIG/BAK/OVRL）、5点平均意见得分（MOS）。
- **对比方法**：NU-Wave2（扩散模型，NFE=48）、FLowHigh（流匹配，NFE=2/6等）、非生成式基线。
- **消融实验**：
  - 不同SSL特征（XEUS vs WavLM vs Wav2Vec 2.0）
  - 模型组件逐步添加（基线→+Conv ResBlock→+XEUS→+REPA→+VeCoR）
  - 不同先验分布（高斯 vs 数据依赖）、ODE求解器（Euler vs Midpoint）、NFE。

### 4. 资源与算力
论文未明确说明所使用的GPU型号、数量及训练时长等算力信息。

### 5. 实验数量与充分性
- **实验数量**：包含主对比实验（表1）、超参数/求解器/NFE实验（表2）、SSL特征消融（表3）、组件消融（表4）。共4张表格，覆盖主要指标。
- **充分性**：消融实验逐步验证了每个组件的贡献；对比了多种生成式与非生成式基线，基线重新训练保证公平。但缺少在不同SNR条件下的细粒度分析，也未在更多数据集上验证（仅一个数据集）。整体相对充分，可认为客观。

### 6. 论文的主要结论与发现
- 提出的VeRe-Flow在所有比较方法中取得最低LSD和最高DNSMOS OVRL；在生成模型中取得最高MOS。
- 速度对比正则化和表示对齐分别提升了背景噪声抑制（BAK）和总体质量（OVRL），XEUS SSL特征对LSD改善最大。
- 采用高斯先验与欧拉求解器（NFE=2）配置最优，证明了多级干净监督对NR-BWE有效。

### 7. 优点
- **创新性**：首次将速度对比正则化应用于语音生成任务，结合表示对齐，形成了层次化的干净监督策略。
- **有效性**：在LSD和感知指标上同时超越现有方法，且推理步数少（NFE=2），效率高。
- **消融设计完整**：逐步添加组件，清晰展示了各模块的贡献。
- **公平对比**：重新训练生成式基线并在相同条件下比较。

### 8. 不足与局限
- **数据集单一**：仅在Valentini-Botinhao上验证，缺乏在真实场景或更多噪声类型/SNR下的评估，泛化性未经充分检验。
- **非生成式基线结果引用自原论文**：可能因不同训练/测试设置导致比较不完全严格。
- **未分析计算复杂度与资源消耗**：缺少模型参数、推理时间等效率指标。
- **未讨论失效情况或失败样例**：例如极端低SNR或特殊噪声下的表现。
- **表示对齐损失依赖于干净SSL特征**：推理时仍需提取干净特征？实际上训练中使用干净特征，推理时仅用噪声输入，但需要确保SSL模型泛化。

（完）
