---
title: Optimal Transport-based Semantic Alignment for LLM-based Audio-Visual Speech Recognition
title_zh: 基于最优传输的语义对齐方法用于基于大语言模型的音视频语音识别
authors: "Xugang Lu, Peng Shen, Yu Tsao, Hisashi Kawai"
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.09001v1"
tags: ["query:speech-audio"]
score: 8.0
evidence: 基于最优传输的语义对齐用于大模型视听语音识别
tldr: 基于大语言模型的音视频语音识别（LLM-AVSR）在复杂声学环境下展现出鲁棒性，但现有方法未显式处理音频、视觉与文本模态间的表示差异。本文提出最优传输（OT）语义对齐框架，通过OT估计模态特征与语言嵌入的概率耦合矩阵，并作为软伪标签监督对比学习，将多模态特征对齐到LLM语言空间。在LRS3-TED基准上，该方法在干净和噪声条件下均取得一致提升，达到最先进性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.09001v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1554, \"height\": 929, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09001v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 621, \"height\": 363, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.09001v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 771, \"height\": 247, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09001v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 771, \"height\": 248, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09001v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 785, \"height\": 137, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09001v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 724, \"height\": 271, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09001v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 859, \"height\": 314, \"label\": \"Table\"}]"
motivation: 现有LLM-AVSR方法忽略音频、视觉与文本间的表示差异，导致跨模态融合效果受限。
method: 使用最优传输估计模态特征与语言嵌入的耦合矩阵，作为软伪标签监督对比学习，对齐多模态表示到LLM语言空间。
result: 在LRS3-TED上，该方法在干净和噪声（多种SNR）条件下均超越强基线，达到最先进性能。
conclusion: 通过显式语义对齐，最优传输框架有效提升LLM-AVSR的多模态融合与解码效果。
---

## 摘要
基于大语言模型（LLM）的音视频语音识别（LLM-AVSR）最近通过利用互补的音频和视觉信息，在恶劣声学环境中展现出强大的鲁棒性。现有方法通常采用独立预训练的音频和视觉编码器，将其输出投影并融合为软提示，以条件化LLM进行语音识别。然而，大多数方法在执行多模态融合时，没有明确解决音频、视觉和文本模态之间的表示差异，这可能限制了跨模态集成的效果。本文提出了一种基于最优传输（OT）的语义对齐框架用于LLM-AVSR。所提方法通过在多模态融合之前，将音频和视觉表示相对于LLM的语言嵌入空间进行对齐，从而显式地弥合模态差距。具体而言，利用OT估计概率耦合矩阵，该矩阵刻画了模态特定特征与语言嵌入之间的结构化对应关系。由此得到的OT耦合进一步用作软伪标签来监督对比学习，促进提取语义连贯且跨模态一致的音视频表示。通过将多模态特征锚定到LLM的语言空间，所提框架促进了更有效的多模态融合和解码。我们使用基于Whisper的音频编码器、基于AV-HuBERT的视觉编码器和LLaMA3.2-3B解码器实现了所提框架。在LRS3-TED基准上进行的实验表明，与强基线相比，该方法在干净和噪声评估条件下，在广泛的信噪比（SNR）范围内均取得了一致的改进，并达到了最先进的性能。

## Abstract
Large language model (LLM)-based audio-visual speech recognition (LLM-AVSR) has recently demonstrated strong robustness in adverse acoustic environments by leveraging complementary audio and visual information. Existing approaches typically employ independently pretrained acoustic and visual encoders, whose outputs are projected and fused as soft prompts to condition an LLM for speech recognition. However, most methods perform multimodal fusion without explicitly addressing the representational discrepancy between audio, visual and text modalities, potentially limiting the effectiveness of cross-modal integration. In this paper, we propose an optimal transport (OT)-based semantic alignment framework for LLM-AVSR. The proposed method explicitly bridges the modality gap by aligning the acoustic and visual representations with reference to the linguistic embedding space of the LLM before multimodal fusion. Specifically, OT is used to estimate probabilistic coupling matrices that characterize structured correspondences between modality-specific features and linguistic embeddings. The resulting OT couplings are further utilized as soft pseudo-labels to supervise contrastive learning, encouraging the extraction of semantically coherent and cross-modal consistent audio-visual representations. By anchoring multimodal features to the linguistic space of the LLM, the proposed framework facilitates more effective multimodal fusion and decoding. We implement the proposed framework using a Whisper-based acoustic encoder, an AV-HuBERT-based visual encoder, and a LLaMA3.2-3B decoder. Experiments conducted on the LRS3-TED benchmark demonstrate consistent improvements over strong baselines and achieve state-of-the-art performance under both clean and noisy evaluation conditions across a wide range of signal-to-noise ratios (SNRs).

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有的基于大语言模型（LLM）的音视频语音识别（LLM-AVSR）系统通常直接融合独立预训练的音频和视觉编码器的输出，而没有显式地解决音频、视觉和文本模态之间的**表示差异（Representational Discrepancy）**。这种差异导致简单的投影与拼接策略无法充分挖掘模态间的互补信息，限制了LLM对多模态信息的语义理解与利用。此外，时间同步并不等同于语义同步，直接融合时间对齐的特征难以有效降低歧义。
- **整体含义**：本文提出了一种**基于最优传输（Optimal Transport, OT）的语义对齐框架**，旨在多模态融合之前，将音频和视觉表示与LLM的语言嵌入空间进行显式对齐，从而弥合模态鸿沟，提升跨模态融合的有效性，最终在干净和噪声环境下均实现更优的鲁棒语音识别。

### 2. 论文提出的方法论

- **核心思想**：将音频特征、视觉特征和文本token嵌入建模为经验概率分布，通过最优传输学习它们之间的结构化对应关系（概率耦合矩阵），并利用这些耦合作为软伪标签来监督对比学习，促使模型提取语义连贯且跨模态一致的表示。对齐后的多模态特征再融合作为LLM的软提示进行解码。
- **关键技术细节**：
  - **特征提取**：使用Whisper（medium）作为音频编码器，AV-HuBERT（large）作为视觉编码器，LLaMA3.2-3B作为LLM解码器。特征经过线性投影映射到LLM的嵌入维度。
  - **OT对齐**：计算音频-文本和视觉-文本之间的熵正则化OT，通过Sinkhorn算法求解概率耦合矩阵 \(Q^{a\to t}\) 和 \(Q^{v\to t}\)。相似度矩阵由余弦相似度构成。
  - **虚拟桶（Virtual Bucket）**：在OT匹配中引入虚拟桶，用于吸收非线性信息或噪声帧（如静音、背景噪声），避免不相关的特征被强制对齐。
  - **对齐损失**：将OT耦合作为软标签，用交叉熵损失监督音频-文本、视觉-文本的对比学习，损失函数为 \(L_{\text{align}}\)。
  - **多模态融合**：测试了四种融合方法（通道拼接、加法、交叉注意力、Q-Former），最终选择通道拼接作为基线，融合后线性投影到LLM空间。
  - **总损失**：\(L_{\text{Total}} = L_{\text{AR}} + \alpha L_{\text{align}}\) ，其中 \(L_{\text{AR}}\) 是LLM自回归交叉熵损失。

### 3. 实验设计

- **数据集与场景**：使用**LRS3-TED**基准数据集（433小时训练集、1小时验证集、1小时测试集）。在**干净**和**模拟噪声**（添加babble噪声，SNR范围 -5dB, 0dB, 5dB）条件下评估。
- **Benchmark**：对比方法包括：
  - 作者自己构建的基线（不同融合策略：C Concat, Add, Cross att, Q former, T Concat）
  - 现有先进方法：Whisper-Flamingo, LLaMA-AVSR, MMS-LLaMA
  - 消融实验：无对齐模块、无虚拟桶、不同对齐权重α
- **参数设置**：LoRA秩16，缩放因子32，Adam优化器，初始学习率1e-4，5k warm-up，总30k训练步。

### 4. 资源与算力

- 论文**未明确说明**具体使用的GPU型号、数量或训练时长。仅描述了模型规模（Whisper medium、AV-HuBERT large、LLaMA3.2-3B），并使用LoRA微调LLM，编码器冻结。未提供算力量化信息。

### 5. 实验数量与充分性

- **实验数量**：共进行了多组实验，包括：
  - 不同融合方法的基线对比（表I、表II）
  - 虚拟桶消融实验（表III）
  - 对齐权重α消融实验（表IV）
  - 与三种先进方法的对比（表V，包含三组参数设置）
  - 未列出对其他超参数（λ、τ、边际相似度）的详细敏感性分析，仅提及存在交互作用。
- **充分性评估**：实验覆盖了干净和多种噪声条件，消融实验验证了OT对齐和虚拟桶的有效性，与多个现有方法公平比较（使用相同数据分割和类似参数）。但缺乏对更多噪声类型、更多SNR级别（如-10dB, 10dB等）的测试，也未进行跨数据集验证。总体而言，实验较为充分但并非极其详尽。

### 6. 论文的主要结论与发现

- 提出的OT语义对齐框架在**所有测试条件**下的词错误率（WER）均优于无对齐的基线以及三个现有先进方法（Whisper-Flamingo, LLaMA-AVSR, MMS-LLaMA），在SNR-5dB、0dB、5dB和干净条件下分别最低达到7.16%、2.26%、1.07%、0.71%的WER。
- 虚拟桶的引入带来轻微但一致的性能提升，尤其在高噪声条件下。
- 对齐权重α在0.1~0.5范围内表现较好，过大或过小均会降低性能。
- 多模态融合方法中，通道拼接（C Concat）在噪声训练条件下表现最好。
- 显式将多模态特征对齐到LLM语言空间优于直接融合。

### 7. 优点

- **方法创新**：首次将最优传输引入LLM-AVSR中的语义对齐，利用概率耦合显式建立模态间结构化对应关系，而非仅依赖时间对齐。
- **模块化设计**：OT对齐模块可插入现有编码器与LLM之间，无需修改预训练模型，易于集成。
- **鲁棒性提升**：在低SNR条件下性能提升显著（如SNR-5dB下WER从8.34%降到7.16%），验证了语义对齐对噪声的抑制能力。
- **实验设计全面**：对比多种融合方法、消融了虚拟桶和损失权重，并与多个SOTA方法比较，证明了方法有效性。

### 8. 不足与局限

- **超参数敏感性**：OT估计中的熵正则化λ、相似度边际s̃、温度τ等超参数存在交互作用，论文未给出系统调优结果，仅凭经验选择，可能未达最优。
- **实验覆盖有限**：仅使用单数据集的单一噪声类型（babble），未测试其他噪声（如音乐、混响）或真实噪声场景；缺乏跨语言或跨领域的泛化验证。
- **计算资源未报告**：未提供训练时间或GPU型号，无法评估方法的计算成本。
- **虚拟桶机制简略**：仅引入单向虚拟桶，未讨论与不平衡OT的深入联系，也未分析不同桶容量设置的影响。
- **融合策略对比**：虽然比较了四种融合，但未给出与时间轴拼接（T Concat）以外更复杂的融合方式（如门控机制）的对比。
- **基线优势分析**：虽然最终性能优于其他方法，但提升幅度在某些条件下（如干净语音）较小（0.89% → 0.71%），实际收益可能有限。

（完）
