---
title: "SpeechGym: An Audio-Native Gym for Training Voice Agents via Reinforcement Learning"
title_zh: SpeechGym：一个用于通过强化学习训练语音代理的音频原生环境
authors: "Jiajun Fan, Jingyuan Li, Prashanth Gurunath Shivakumar, Jia-Hong Huang, Qi Luo, M. Maruf, Ivan Bulyko, Ge Liu, Roger Ren"
date: 2026-08-26
pdf: "https://arxiv.org/pdf/2608.26432v1"
tags: ["query:speech-audio"]
score: 7.0
evidence: 用于语音智能体训练的音频原生环境，不依赖外部ASR/TTS
tldr: 语音智能体需通过语音调用工具并多轮对话，但现有范式在文本中训练，级联外部合成与识别或专有接口使梯度无法回传。SpeechGym构建音频原生的端到端可训练环境，令两个全模态模型直接以音频交互，仅改变模态而任务、工具与检查不变。训练发现语音错误源于感知而非推理，误听参数导致级联失败，且仅结果奖励稀疏，于是引入逐过程奖励恢复梯度方差。训练后模型无需微调迁移至独立语音基准，任务成功率翻倍以上，开放权重模型升至榜单第二，且消耗更少轮次与词元。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.26432v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1095, \"height\": 402, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.26432v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1552, \"height\": 302, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.26432v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1728, \"height\": 659, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.26432v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1572, \"height\": 722, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.26432v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1568, \"height\": 578, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.26432v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1461, \"height\": 224, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.26432v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1136, \"height\": 222, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.26432v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1120, \"height\": 265, \"label\": \"Table\"}]"
motivation: 语音智能体必须原生处理语音交互，但当前主流在文本上训练，且外部ASR/TTS或专有API阻碍梯度回传，导致模型无法真正改进。
method: SpeechGym让两个全模态模型在音频中直接对话，任务、工具和成功检查沿用文本基准，并对每次成功工具调用给予过程奖励，以对抗稀疏结果奖励。
result: 使用GRPO训练后，模型零微调迁移到独立语音基准，任务成功率翻倍以上，开放权重模型从榜单末位升至第二，同时减少交互轮次和词元消耗。
conclusion: 语音智能体的核心挑战是感知错误而非推理，原生音频强化学习配合过程奖励能有效提升能力并实现高效迁移。
---

## 摘要
语音代理必须完全通过语音调用工具并进行多轮对话，然而主流范式却是在文本中训练它们。现有框架要么在专有语音 API 周围级联 TTS 和 ASR，导致梯度无法流动，且每次调用的成本使得基于策略的强化学习难以实现；要么停留在文本层面：它们可以评估语音代理，却无法改进它们。我们提出了 SpeechGym，一个音频原生的智能体环境，其中两个全模态模型以原生音频进行对话，没有外部 ASR 或 TTS，也没有 API 边界，基于已建立的文本智能体基准的未修改任务、工具和成功检查，从而使交互模态成为唯一变量，并且循环保持本地化且可端到端训练。音频智能体能力并非源自音频理解。语音引入的失败是感知性而非推理性缺陷：代理选择了正确的工具和正确的参数槽，但却用从波形中听错的值填充了它，而这个单一错误会级联导致调用失败、重试同一调用以及浪费步骤预算。第二个失败是行为性的：在坚持的呼叫者面前，代理执行了未授权的写入，并在相信自己在帮忙的情况下结束了这一回合。这两者都是可训练的，因为环境免费为它们打上标签：一个听错参数的调用在数据库中会失败，而正确的调用则会成功。障碍在于稀疏性，而非信号。仅基于结果的 GRPO 在这里缺乏梯度，因为几乎每个 rollout 组都以相同方式失败，而一个逐轮过程奖励（对每个成功的工具调用给予奖励）则几乎为每组恢复了方差。通过这种方式训练，代理无需进一步调优即可迁移到独立实现的语音基准上，任务成功率提高了一倍多，并将一个开放权重模型在该排行榜上从最后一名带到第二名，同时使用的轮次和令牌比训练前更少。

## Abstract
Voice agents must call tools and hold multi-turn dialogue entirely through speech, yet the dominant paradigm trains them in text. Existing frameworks either cascade TTS and ASR around a proprietary voice API, where gradients cannot flow and per-call cost makes on-policy reinforcement learning prohibitive, or stay in text: they measure voice agents but cannot improve them. We present SpeechGym, an audio-native agentic environment in which two omni-modal models converse in native audio, with no external ASR or TTS and no API boundary, over the unmodified tasks, tools and success check of an established text agentic benchmark, so that the interaction modality is the only variable and the loop stays local and trainable end to end. Audio agentic capability does not follow from audio understanding. The failures speech introduces are perceptual rather than reasoning deficits: the agent picks the right tool and the right argument slot but fills it with a value misheard from the waveform, and that single error cascades into a failed call, a retry of the same call, and a wasted step budget. A second failure is behavioural: under an insistent caller the agent performs an unauthorised write and ends the episode believing it helped. Both are trainable, because the environment labels them for free: a call with a misheard argument fails against the database while a correct one succeeds. The obstacle is sparsity, not signal. Outcome-only GRPO is gradient-starved here, since almost every rollout group fails identically, while a per-turn process reward crediting each successful tool call restores variance to nearly every group. Trained this way, the agent transfers with no further tuning to an independently implemented voice benchmark, more than doubling task success and carrying an open-weights model from last place to second on that leaderboard, while using fewer turns and tokens than before training.