---
title: "MeloCodec: Harnessing Melodic Priors for High-Fidelity Singing Voice Representation"
title_zh: MeloCodec：利用旋律先验实现高保真歌唱声音表征
authors: "Yizhong Geng, Wenxin Fu, Kecan Mao, Qifei Li, Yingming Gao, Ruimin Wang, Chunfeng Wang, Hao Li, Ya Li, Wei Chen"
date: 2026-08-04
pdf: "https://arxiv.org/pdf/2608.03021v1"
tags: ["query:speech-audio"]
score: 8.0
evidence: 面向高保真歌声表示的神经音频编解码器，利用旋律先验支持基于大模型的音频合成。
tldr: 神经音频编解码器是LLM音频生成的基础，现有工作多利用语义先验提升语言可懂度，却忽视了旋律等显式声学先验，导致歌唱等频率敏感场景的合成保真度受限。为此提出MeloCodec，引入旋律先验作为关键声学信息，并设计Tokenize-then-Fuse范式：先预训练离散旋律分支以锁定结构，再与主分支融合，同时采用两阶段训练策略避免码本崩溃。实验表明，MeloCodec在歌唱声音表示上超越基线，显著提升音高一致性，并能以最小音色退化实现可控音高操作，为音频编解码器集成显式声学先验提供了有效范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.03021v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1657, \"height\": 1056}, {\"url\": \"assets/figures/arxiv/2608.03021v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 824, \"height\": 576}, {\"url\": \"assets/figures/arxiv/2608.03021v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 917, \"height\": 573}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.03021v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1885, \"height\": 628}, {\"url\": \"assets/tables/arxiv/2608.03021v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 927, \"height\": 222}, {\"url\": \"assets/tables/arxiv/2608.03021v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1885, \"height\": 566}]"
motivation: 现有音频编解码器忽略旋律等显式声学先验，限制了歌唱合成在频率敏感域的保真度。
method: 提出MeloCodec，采用Tokenize-then-Fuse范式预训练离散旋律分支再融合，配合两阶段训练稳定收敛。
result: 在歌唱声音表示上优于基线，提升音高一致性，并支持可控音高操作且音色退化最小。
conclusion: 验证了旋律先验的有效性，为高保真歌唱声音表示提供了新思路。
---

## 摘要
神经音频编解码器作为基于LLM的音频生成的基本分词器。虽然语义先验被广泛用于增强语言可懂度，但显式声学先验的整合仍未被充分探索，限制了频率敏感领域的合成保真度。为解决这一空白，我们引入了MeloCodec，一种新颖的框架，旨在有效整合旋律先验，这是歌唱中关键的声学信息形式。为解决这种显式先验直接融合通常导致的优化不稳定性，我们提出了一种“先分词后融合”范式，该范式在特征融合前预训练一个离散旋律分支以锁定结构。为稳健实现这一范式，我们进一步提出了一种两阶段训练策略，以阻止码本坍缩并确保稳定收敛。实验表明，MeloCodec在歌唱声音表征上优于基线，提高了音高一致性，并实现了可控的音高操控，且音色退化最小。

## 速览
**TLDR**：神经音频编解码器是LLM音频生成的基石，现有方法多利用语义先验提升语言清晰度，却忽略了声学先验（如旋律）对合成保真度的重要性。为此提出MeloCodec，采用Tokenize-then-Fuse范式，先预训练离散旋律分支以锁定结构，再融合特征，并通过两阶段训练防止码本崩溃。实验表明，MeloCodec在歌声表征上优于基线，提升音高一致性并支持可控音高操作。 \
**Motivation**：现有神经音频编解码器忽视声学先验，尤其在歌声等频率敏感领域，直接融合显式旋律先验会导致优化不稳定。 \
**Method**：提出Tokenize-then-Fuse范式，先预训练离散旋律分支锁定结构，再融合特征，配合两阶段训练避免码本崩溃。 \
**Result**：MeloCodec在歌声表征上优于基线，提升音高一致性，支持可控音高操作且音色退化最小。 \
**Conclusion**：验证了显式旋律先验对歌声表征的有效性，为高保真音频生成提供新思路。

---

## Abstract
Neural audio codecs serve as fundamental tokenizers for LLM-based audio generation. While semantic priors are widely exploited to enhance linguistic intelligibility, the integration of explicit acoustic priors remains underexplored, limiting synthesis fidelity in frequency-sensitive domains. To address this gap, we introduce MeloCodec, a novel framework designed to effectively incorporate melodic priors, a critical form of acoustic information for singing. To address the optimization instability typically caused by the direct fusion of such explicit priors, we propose a Tokenize-then-Fuse paradigm that pre-trains a discrete melodic branch to lock in structures before feature fusion. To robustly realize this paradigm, we further propose a two-stage training strategy that prevents codebook collapse and ensures stable convergence. Experiments show that MeloCodec outperforms baselines in singing voice representation, improving pitch consistency and enabling controllable pitch manipulation with minimal timbre degradation.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **核心问题**：神经音频编解码器是 LLM 音频生成的基础分词器，现有工作主要通过整合语义先验（如 SSL 向量、ASR 监督）来提升语言可懂度，但忽视了旋律等显式声学先验，导致在歌唱等频率敏感场景中合成保真度不足。同时，直接融合显式声学先验会引发“捷径学习”（Shortcut Learning）和优化不稳定等问题。
- **整体含义**：论文提出 MeloCodec，通过“Tokenize-then-Fuse”（先分词后融合）范式，将旋律先验以量化令牌形式嵌入离散潜在空间，从而在高保真歌唱声音表征中实现更好的音高一致性和可控性，为后续 LLM 驱动的音频生成提供结构化、可解耦的令牌接口。

## 2. 论文提出的方法论

- **核心思想**：利用显式旋律先验（Chromagram）作为结构引导，通过信息瓶颈约束，避免解码器忽略旋律特征或产生梯度冲突，实现结构与纹理的解耦建模。
- **总体架构**（双流结构）：
  - **Tokenize Branch（分词分支）**：从波形提取 Chromagram，经过旋律编码器 \(E_{mel}\) 得到连续特征 \(M\)，再用 RVQ 量化为离散旋律令牌 \(M_q\)，形成信息瓶颈；之后由旋律解码器 \(D_{mel}\) 重建 Chromagram。
  - **Fuse Branch（融合分支）**：声学编码器 \(E_{ac}\) 处理原始波形得到声学特征 \(A\)，与线性投影后的旋律令牌 \(M_p = \psi(M_q)\) 做拼接，再经 RVQ 得到统一令牌 \(U_q\)，最后通过声学解码器重建波形。
- **关键技术细节**：
  - 公式 1–6 分别给出了旋律特征提取、RVQ 量化、重建损失、统一特征融合、辅助投影重建 Chromagram 的损失定义。
  - 辅助投影头 \(\phi(\cdot)\) 从统一令牌预测旋律信息，并通过预训练的旋律解码器施加监督，确保旋律结构注入而不破坏声学纹理。
- **训练策略（两阶段）**：
  - **Stage 1**：仅训练 Tokenize Branch（旋律编码器 + RVQ + 旋律解码器），使用 VQ-VAE 目标锁定离散旋律结构。
  - **Stage 2**：冻结旋律编码器和其 RVQ，微调旋律解码器，端到端优化 Fuse Branch；总损失为重建损失、对抗损失（MSD + hinge + feature matching）和辅助旋律损失之和。
  - 通过这种“先锁定结构，再联合优化”的方式，避免码本坍缩和优化发散。

## 3. 实验设计

- **数据集**：
  - 训练集：内部 5,500 小时中文歌唱语音（44.1 kHz）。
  - 测试集：公开的 Opencpop（held-out）。
- **Benchmark 设置**：
  - 高比特率 ≈6.0 kbps（\(N_q = 8\)，码本大小 1024，token 率 75 Hz）。
  - 低比特率 ≈1.5 kbps（\(N_q = 2\)，码本大小 1024，token 率 75 Hz）。
- **对比方法**：
  - EnCodec、DAC、X-Codec（均在内部数据上微调）。
  - MuCodec（使用官方权重，作为零样本低比特率音乐参考）。
- **评价指标**：
  - 音频质量：ViSQOL、STOI、MUSHRA 主观测试。
  - 旋律与音高：F0-RMSE、Chroma-Sim。
  - 音色：SPK-SIM（ResNet-34）。
  - 可控性：Target F0-Corr / Target F0-RMSE（音高移位 ±2、±4 半音）。
- **消融实验**：
  - Acoustic-only 单流编解码器；
  - + Auxiliary Loss（增加旋律重建辅助损失）；
  - Direct Fusion（双流端到端直接融合）；
  - Ours（Tokenize-then-Fuse）。

## 4. 资源与算力

- 文中仅提到使用 NVIDIA H800 GPU 进行训练，采用 AdamW 优化器和标准 EMA 码本更新；**未明确说明 GPU 数量、训练总时长、总计算量等具体资源信息**。

## 5. 实验数量与充分性

- **主要实验组数**：
  - 主对比实验：高比特率（5 个模型）和低比特率（6 个模型）共两组；
  - 主观评估：MUSHRA 测试（15 名专家、30 个样本）覆盖两种比特率；
  - 可控性实验：音高移位 ±2/±4 半音，对比 Direct Fusion；
  - 消融实验：4 种训练策略变体，在两种比特率下比较；
  - 可视化分析：F0 轨迹对比图展示模型对颤音等细节的捕捉。
- **充分性评价**：
  - 实验覆盖了客观指标、主观听感、可控性分析和消融研究，较为全面；
  - 对比基线覆盖主流编解码器（通用型、语义增强型、音乐专用型），公平性较好；
  - 消融实验有效验证了“Tokenize-then-Fuse”的不可替代性，并给出了 Direct Fusion 失败模式的诊断（码本利用率 <5%、验证损失发散）；
  - 不足：训练数据仅限于中文歌唱语音，未涉及其他语言/风格；未提供推理效率、模型参数量的比较；主观测试样本量相对较小（30 个样本）。

## 6. 论文的主要结论与发现

- **性能更优**：MeloCodec 在高、低比特率下均优于所有基线，尤其在低比特率下优势显著（F0-RMSE 1.64 vs. DAC 4.12、MuCodec 2.68），说明旋律先验可作为结构锚点，防止极端压缩下的音高失真。
- **稳定可控**：音高移位测试显示，MeloCodec 在保持较高轮廓相关性（0.70–0.72）的同时，音色退化极小（SPK-SIM 0.91–0.93，退化率 1.6%–3.9%），远优于 Direct Fusion。
- **结构必要性**：消融表明仅靠辅助损失无法从根本重塑潜在空间；直接融合会发生码本坍缩和优化发散，而“Tokenize-then-Fuse”策略是稳定融合显式先验的关键。
- **范式意义**：该框架为可解释物理模型与生成式神经网络的结合提供了可扩展蓝图，离散瓶颈是保证显式先验有效注入的关键。

## 7. 优点

- **动机清晰**：精准指出语义先验无法覆盖频率敏感需求、隐式解耦缺乏结构保证的问题，并选择旋律先验作为明确的切入口。
- **方法创新**：提出“Tokenize-then-Fuse”范式，利用信息瓶颈和两阶段训练同时解决捷径学习与优化不稳定，思想简洁且可扩展。
- **实验扎实**：同时包含客观指标、主观听感、消融诊断和可控性验证，证据链完整；对 Direct Fusion 失败原因给出了深入分析。
- **实际价值**：在低比特率下保持稳健音高一致性，对高压缩场景下的音频生成与传输具有重要意义；支持可控音高操作，便于下游应用。

## 8. 不足与局限

- **语言与场景局限**：仅在中文歌唱语音上训练和测试，未覆盖多语言、多风格或说话人交错场景，泛化性有待验证。
- **绝对音高精度受限**：音高移位测试中 RMSE 相对较高（3.85–4.50），说明固定声学流限制了对目标音高的绝对控制，其与轮廓相关性的权衡尚未完全解决。
- **计算资源信息不足**：未报告 GPU 数量、训练时长、参数量、推理速度等，难以为资源预算提供参考。
- **指标覆盖有限**：缺少对音频时长、帧率、码本利用率等底层细节的深入报道；主观测试样本量较小，统计显著性未充分讨论。
- **上位应用未验证**：论文聚焦于编解码器本身，未展示与 LLM 生成模型端到端集成的实际效果，验证了表示质量但未验证生成式下游任务上的收益。

（完）
